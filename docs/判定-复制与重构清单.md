# 判定清单 · 复制 / 改造 / 重构 / 丢弃

> **读这张表的用法**:它是施工时唯一的"这段代码怎么处理"依据。源项目 `E:\MetaGPT\metagpt` 是**供体**,不是模具——所以本表不要求新栈文件数与源一致,只要求**能力去向明确**。
> 已经逐行核实过的源位置会带行号;没带行号的表示"整文件级判定",动手前先读一遍该文件。

## 零、不做清单（2026-09-14 定案，先于一切判定读）

**源项目的「预算管理」不移植。** 精确边界是**砍预算强制、保留计量**：

| 处理 | 内容 |
|---|---|
| **不做** | 预算上限判断与拦截（`max_budget`/`total_budget`/`check_budget`/`is_within_budget`/`update_budget`）、`NoMoneyException`、图上的 `budget_guard` 节点、`TeamState.budget_used`、建会话时的「预算（美元，超限自动停止）」输入、前端的「$已用 / $预算」与「每月使用限制」进度条 |
| **保留** | token 计数（上下文压缩按 `DEFAULT_MAX_TOKENS` 触发，**删不得**）、每轮 token/成本累计、trace 里的耗时与 token 归因。成本数字**只读**：展示、观测、事后统计，绝不作为"这一问值不值得发"的判据 |
| **拆出去** | N5 原本把两件事捆在一起——**分布式限流保留**（多 worker 平台必须有，归到 N1 的配额项），**每用户每会话预算作废**。N5 条目按此拆分，不再是一个整体 |

**为什么砍**：预算/配额属于计费域。多用户平台真要做限额，将来落在网关或计费外壳（按 user_id 计数、超限时在 HTTP 入口拒绝），不该进 agent 编排内核——内核一旦被 `max_budget` 污染，`team_graph` 就凭空多一个没有信息量的节点。

**现存代码里这条线从未生效，所以删除零功能损失**（2026-09-14 实测）：
- `environment/team_graph.py:126` 的 `budget_guard` 比的是 `state["budget_used"] >= settings.max_budget`，而 `budget_used` 全仓库**只有初始化写 0.0、没有任何累加写入**，条件恒假；这个节点既不调 `CostManager` 也不抛 `NoMoneyException`，纯粹是个空转的图节点。
- `provider/cost.py` 的 `check_budget` / `is_within_budget` 在生产路径**零调用方**，只有 `tests/s2_gateway.py` 在自证。
- `server/runner.py:151` 把 `c.total_budget` 当 `max_budget` 发给前端，而 `total_budget` 只有 `update_budget()` 会动（无人调用），恒为 0 → 前端 4 处预算展示从来没显示过预算。
- S3 施工文档从未要求过 `budget_guard`——它是绕过判定表长出来的。

## 五种判定

| 标记 | 含义 | 你的动作 |
|---|---|---|
| `复` | 纯资产/纯函数,与新栈无冲突 | cp 过去,只改 import 前缀 |
| `改` | 业务逻辑可留,上下层调用要换 | cp 当底稿,替换 `self.llm.*` / `Config.*` / `logger` / 落盘调用 |
| `重` | 源实现与新栈**功能重叠**,复制会绕过你的栈 | **不许 cp**。按新栈原生形态自己写 |
| `弃` | 平台不需要 | 不搬,在表里留理由 |
| `新` | 源没有、平台必须有 | 自写 |

---

## 一、`重`：源有实现、但你必须自己写的（这是全表最要紧的一段）

这 9 处是"用你的技术栈"这句话的实际落点。复制它们 = 声明作废。

| # | 源 | 行数 | 为什么不能复制 | 你要写什么 |
|---|---|---|---|---|
| R1 | `provider/` 整包(`base_llm.py` 412、`general_api_base.py` 581 + 16 家客户端) | 3,614 | 16 家厂商全部手写 HTTP 客户端。复制过来 LangChain 一行都用不到 | `provider/gateway.py`:以 `init_chat_model` 为底座,暴露源的 `aask` / `aask_batch` / `aask_code` 签名;统一挂 callbacks(计数/trace)、retry、缓存 |
| R2 | `actions/action_node.py` + `action_graph.py` + `action_output.py` + `action_outcls_registry.py` | 985 | 自研字段级结构化引擎,与 `with_structured_output` 重叠 | `base/action.py` 里做**逐字段 validator + 定向重试**:pydantic v2 `model_validator` 收集缺失字段 → 只对缺失字段发二次请求。**这是源最被低估的实现,`重` 不代表放弃能力,见 §五** |
| R3 | `roles/role.py` 596 + `team.py` 138 + `environment/base_env.py` 248 | 982 | 手写轮次循环。核实过:`base_env.py:198-212` 是 `for _ in range(k): for role in self.roles.values(): if role.is_idle: continue; futures.append(role.run())` —— 全员轮询、无条件边 | `environment/graph.py`:`StateGraph` + 订阅关系转成条件边 + `Send` 精准激活。角色私有记忆放 state reducer |
| R4a | `utils/serialize.py` 中的 `serialize_message`(:60) / `deserialize_message`(:74) | 34 | 只解决"**存到哪儿**" | checkpointer(SQLite 起步 → Redis/Postgres),`thread_id=sid` |
| ~~R4b~~ | ~~`base/base_serialization.py` 67~~ | 67 | **误判已撤回**：它解决"**还原成哪个类**"(多态标签)，与存储后端正交，checkpointer 一样需要 | 改判 `复`——零 metagpt 依赖，逐字复制，见 §一 末警示 |
| R4c | `utils/serialize.py` 中的 `actionoutout_schema_to_mapping`(:11) / `actionoutput_mapping_to_str`(:43) / `actionoutput_str_to_mapping`(:50) | 49 | ⚠ **同类误判的第二处**（原随 R4a 一起判了 `重`）：这三个不是存储层，是**结构化输出的格式编解码**——被源 `schema.py:66-68` 自己 import，用在 `Message.check_instruct_content`(:255) / `ser_instruct_content`(:294) 里 | 改判 `复`。搬进 `utils/serialize.py` 或 `provider/repair.py`；S6 若要让源 `instruct_content` 的 `{"class","mapping","value"}` 三件套路线可用，它们缺一不可 |
| R5 | `utils/human_interaction.py` 107 | 107 | 阻塞 `input()`,web 场景不可用 | `interrupt()` + `Command(resume=...)`;节点必须显式声明 `RunnableConfig` 参数 |
| R6 | `utils/report.py` 330 | 330 | 自有上下文管理器事件总线 | **要保的 surface 有两层，原先我只写了第一层**：① 九值 `BlockType`(:31) 词汇表逐字对齐；② **12 个 Reporter 类名与签名**——`ResourceReporter`(:49 基类) / `Terminal`(:192) / `Browser`(:211) / `Server`(:232) / `Object`(:246) / `Task`(:258) / `Thought`(:264) / `File`(:270) / `Notebook`(:296) / `Docs`(:302) / `Editor`(:308) / `Gallery`(:314)，Action 按类名 + `async_report`/`report` 调用（如 `Plan._update_current_task` 调 `TaskReporter().report(...)`）。**类名一个都不能少**，否则 S6 逐字复制 Action 时全 NameError。通道换 `astream_events` + sink 槽，签名不变 |
| R7 | `config2.py` 182 + `utils/yaml_model.py` 48 + `configs/*.py` 里 `to_url()` 那套 | ~250 | 自有 yaml 配置体系 | `configs/settings.py` 用 pydantic-settings 嵌套模型(`LLM__API_KEY` 双下划线);`models_config.py`/`redis_config.py` 等**只搬字段定义** |
| R8 | `tools/tool_registry.py` 194 + `tool_recommend.py` 243 | 437 | 注册表机制与 LangChain tool 重叠 | 保留源"注册表 + 按 profile 选工具"的语义,底座换 LangChain tool/`StructuredTool`;`@register_tool` 装饰器签名照抄 |
| R9 | `document_store/base_store.py` + `qdrant_store.py` | 176 | 核实过:`qdrant_store.py:94-124` 只有单 dense 向量 `query_vector=` + `k=10`,`write()` 是空 stub,裸 `except:` ×2,用废弃的 `recreate_collection` | Qdrant **named vectors**(dense + sparse → hybrid 融合)+ payload 多租户过滤 + scalar quantization。**这是投入产出比最高的一处升级** |

> **R1–R9 合计约 6,833 行源代码不复制。** 这是"文件数不等源项目"的主要来源,也是你的项目是平台而不是 fork 的分界线。

### ⚠ 判 `重` 的纪律（此处已踩过一次）

**一个源文件若同时承担两件正交的事，必须拆开判，不能整体判 `重`。**

实例：`base_serialization.py` 原与 `utils/serialize.py` 合并判为 R4「→ checkpointer」。但两者管的是不同的事——

| 关注点 | 谁负责 | 新栈是否替代 |
|---|---|---|
| **存到哪儿**（写 JSON 文件 / 断点续跑） | `utils/serialize.py` | ✅ checkpointer 取代 |
| **还原成哪个类**（多态类型标签 `__module_class_name` + 子类注册表 + `extra="forbid"`） | `base/base_serialization.py` | ❌ **checkpointer 同样需要**；LangGraph 存 pydantic 也有"字段声明基类、值实为子类"的丢字段问题 |

合并判定的后果：S1 实操时 `SerializationMixin` 直接继承了 `BaseModel`，多态能力静默消失。恢复它之后，`extra="forbid"` **立刻**抓出 `team_graph` 往 `CodingContext` 里塞了一个源模型不存在的 `instruction` 键（此前一直被 pydantic 默认策略静默丢弃）。

**同类要警惕的还有**：R2 的 `action_node.py` 里"字段级定向重试"与"整结构解析"是两件事；R8 的 `tool_registry.py` 里"注册表数据结构"与"函数签名反射"是两件事。**判之前先问：这个文件承担了几件事，哪几件被替代了。**

### ⚠ 第二条纪律：判之前先 grep 调用者（自查又发现 3 处）

第一条纪律只解决了"文件内部"，**下面 3 处错误全是因为只看了文件本身、没查谁在调用它**：

| 处 | 我原判 | 实际情况（grep 出来的） | 改判 |
|---|---|---|---|
| `utils/{graph_repository,di_graph_repository,visual_graph_repo}.py` | `弃`，理由"被 LangGraph state 覆盖" | 职责判错：它是 **SPO 三元组知识图谱**抽象(`insert(subject, predicate, object_)`) + networkx 实现 + 可视化导出，与 LangGraph state（单次运行通道）无关。调用者是 `import_repo` / `rebuild_class_view` / `rebuild_sequence_view` / `extract_readme` —— **这 4 个 Action 我在 §三 判了 `改`**，等于一边说要做、一边把它的存储层删了 | 三件套移出 `弃`，改判 `改`（见 §三 新增行） |
| `utils/mermaid.py` + `mmdc_*` | 整族 `弃`，理由"前端渲染" | `write_prd.py:280` 与 `design_api.py:233` **直接 `await mermaid_to_file(...)`** —— 这两个是我列在 §三 批 1 的主线 SOP Action。而且 `mermaid.py:94-104` 本来就是三引擎**惰性分派器** | `mermaid.py`(164) 改判 `改`（保签名，只接一个引擎）；只 `弃` `mmdc_pyppeteer`/`mmdc_ink`(309) |
| `utils/report.py` | 保留面只写了"九值 BlockType" | Action 按 **12 个 Reporter 类名**调用，缺类名 S6 直接 NameError | 已在 R6 行补齐类名清单 |
| 2b 三只长尾小件（`generate_questions.py` 25、`analyze_requirements.py` 76、`search_enhanced_qa.py` 292） | 随 §三 长尾行判 `改`（S6 批2 落地） | 落地时 grep：前两只**在源项目自身也零角色调用者**（全仓只命中定义文件）；后一只的本体是 `WebBrowseAndSummarize(playwright)` 的包装——浏览器腿在 §S4 已判「推迟」（playwright 未装 / a11y_tree 缺 / 零调用者三条实测） | 前两只不再从源补搬新文件（源自身孤儿）——⚠ 但本仓 `edge_actions.py` 早有同名预埋类，见下一行，处置归批 3；`search_enhanced_qa` 的检索腿已由 research 三腿覆盖、query-rewrite 腿随浏览器腿一并 `推迟`。三只不再占用批2 工时 |
| `edge_actions.py` 六只类（WriteDocstring/WriteDesignReview/WriteReview/ExtractReadMe/AnalyzeRequirements/GenerateQuestions） | （批2 盘点新发现，判定表此前无此行） | 批 3 行原记"源侧消费者含 `strategy/planner.py`"——**2026-09-16 复查明实**：planner 消费的是 `di/ask_review.py`（不属这六只，见下一行），六只里**有源消费者的只有 ExtractReadMe**（`import_repo.py:20`）；其余五只源侧调用方 = 自身 fire CLI + tests（WriteDocstring :193/:218），与本仓 test_p1 ⑤ 的冒烟同构——都是自证。**留五只死分支与本表第二条纪律（源自身零调用不搬，先例 summarizing.py）正面冲突** | 处置归接线台账 #13（B5）：**五只删、ExtractReadMe 保留**转 N2 示例件（补 graph_repo 入库腿，对齐 import_repo 消费语义）；test_p1 ⑤ 段随删随收。registry 死 import 四处由 t17③ 清除（`74cb8e8`）属实，不变 |
| 2d/2f 余件（`di/ask_review.py` 62、`prepare_interview.py` 25、`di/write_plan.py` 88、`execute_nb_code.py` 328、`skill_action.py` 113） | 随批 2d/2f 判 `改` | 批 3 grep 原记"ask_review/prepare_interview 源全仓零调用者"——**2026-09-16 对账改判：ask_review 半句不实**。源消费链真实：`strategy/planner.py:8` import、`:134` 调 `AskReview().run`，挂在 `:96`（update_plan 后计划确认）与 `:104`（task 结果确认）两处闸门——**与本行自认"write_plan 消费方=planner"互相矛盾**（同一个 planner 不能既被判有消费、其调用的件又成孤儿）。prepare_interview 零调用者属实（自身 CLI）。execute_nb_code 的 notebook 形态被 `RunPythonCode` 沙箱替代、skill_action 被 `skills/loader.py` 等价，均属实 | prepare_interview `弃`（登记，随 §四）；**ask_review 不判弃**——评审闸门随 B4 的 plan_and_act interrupt 等价吸收（机制已通，diff 最小），本仓无独立文件；其余三只等价覆盖不再单搬。在途件=rebuild_sequence_view（等类图被真实消费再动） |

**纪律**：每下一个 `弃`/`重` 判定前，先 `grep -rn "该模块的公开符号" E:\MetaGPT\metagpt`，把调用者列出来，再问"这些调用者我判了什么"。**两个判定必须自洽**——被 `改`/`复` 的文件不能依赖一个被 `弃` 的文件。2b 追加一条：**`改` 之前同样要 grep**——长尾件若在源自身零调用，搬过来就是把源的死分支复制成我的死分支。

## 二、`复`：闭眼复制，只改 import 前缀

| 源 | 行数 | 新栈落点 | 备注 |
|---|---|---|---|
| `schema.py` 里 `Message`(232)/`SimpleMessage`/`Resource`/`Task`/`TaskResult`/`Plan`(496)/`MessageQueue`(713) | ~700 | `schema.py` | `Plan` 有 20 个方法是 `WriteTasks` 运行时底座,整段搬 |
| `schema.py` 里 `Document`(138)/`Documents`(194) | 85 | **留在 `schema.py`** | ⚠ 源 `document.py:59` 另有**同名不同物**的 `Document`（带 `path`/`persist` 的文档仓实体）。两者不要混，别把这个搬到 `document.py` |
| `metagpt/document.py` | 235 | `document.py` | 独立移植 `DocumentStatus`/`Document`/`IndexableDocument`/`RepoMetadata`/`Repo`；llama_index 分支判 `改`→LangChain loaders。⚠ 源 `:80`/`:131` 的 `read_text()` 不带编码，与 `to_path()` 的 utf-8 不配对，中文 Windows 必炸——已修，见 S1 门禁 11a/12 |
| `const.py:39-97` 常量段 + `:77-83` 路由 tag | 164 | `const.py` | **源 `MESSAGE_ROUTE_TO_ALL = "<all>"`**,你现项目写成 `"*"`,复制时统一成源值并清掉全部内联字面量 |
| `base/base_serialization.py` | 67 | `base/base_serialization.py` | **零改写**（该文件无任何 metagpt import，只依赖 typing + pydantic）。管三件事：dump 注入 `__module_class_name` 标签、`__init_subclass__` 注册子类表、`extra="forbid"`。**别并入 R4**，见 §一 末警示 |
| `logs.py` 全文 | 153 | `logs.py` | 四个可插拔槽(`log_llm_stream`/`set_human_input_func`/`set_tool_output_logfunc`/`ToolLogItem`)是平台挂载点,**原样复制不要改签名** |
| `utils/common.py` | 1,243 | `utils/common.py` | 可拆子模块但保留同名聚合 re-export。⚠ **两处不"零改写"就出事的地方**：① `get_class_name`(:390) 返回 `module.Class` **全限定名**，而新栈 `RequirementTag`/`Action.name`/`cause_by` 全是**短名**——混用不报错、只会让 watch 订阅静默失配，故 `Message` 的路由归一另走 `schema._tag`/`_tag_set`，**不要调 `any_to_str`**；② 它 import 了 `aiofiles`/`aiohttp`/`chardet`/`requests`/`PIL`/`tenacity`，不装不全则本文件根本 import 不动（S1 实操已补进 `pyproject.toml`） |
| `utils/text.py` 129 / `file.py` 201 / `singleton.py` 22 / `exceptions.py` 61 / `special_tokens.py` 4 / `json_to_markdown.py` 42 / `custom_decoder.py` 297 / `async_helper.py` 37 / `proxy_env.py` 19 | ~810 | 同名 | 零改写 |
| `utils/token_counter.py` | 545 | 同名 | **只搬计数函数**（`count_message_tokens`/`count_output_tokens`/`encode` 降级链）。⚠ 价目表单源定在 `provider/token_costs.py`：当前实测两份 `TOKEN_COSTS` 各 100 条、逐键相同，是真源分裂，调价必漏一处——本文件里那份删除，改为 `from codeharness.provider.token_costs import TOKEN_COSTS` |
| `utils/repair_llm_raw_output.py` | 398 | `provider/repair.py` | **必须含"去围栏 + 修尾逗号"组合策略**——你已踩过只写单独策略的坑 |
| `utils/redis.py` | 63 | `utils/redis.py` | 惰性连接 + 失败吞异常返回 None,这个降级语义照抄 |
| `prompts/` 16 件 | 1,478 | 同名 | **逐字,一个字都不要改**。平台要换的是 prompt 之外的东西 |
| Action 里的 `PROMPT_TEMPLATE` / instruction 常量段 | 大量 | 各 action 文件内 | 同上,逐字。这是源最值钱的资产 |
| `configs/` 字段定义(15 件的 dataclass 字段) | ~300 | `configs/` 同名 | 只搬字段与默认值,机制换 pydantic-settings(见 R7) |
| `tools/libs/editor.py` 1,139 / `git.py` 131 / `shell.py` 53 | 1,323 | `tools/libs/` 同名 | 你已复制过(`editor.py` 1,135 ✅),按 `_shims.py` 垫片模式继续 |
| `utils/tree.py` 136 / `sanitize.py` 183 | 319 | 同名 | `tree.py` 已复制 ✅ |

**`复` 类合计约 8,000–9,000 行**,几乎零风险,是进度最快的一段。

## 三、`改`：cp 当底稿，替换上下层调用

| 源 | 行数 | 保留什么 | 替换什么 |
|---|---|---|---|
| `actions/` 主线 SOP(`write_prd` 325、`write_code` 228、`write_code_review` 315、`design_api` 279、`project_management` 201、`write_test` 70、`run_code` 173、`debug_error` 77、`prepare_documents` 90) | ~1,760 | prompt、字段 schema、输入输出契约 | `self.llm.aask` → gateway;`action_node` 用法 → R2 的 validator;落盘 → `ArtifactStore` |
| `actions/` 长尾(`research` 343、`search_enhanced_qa` 292、`write_docstring` 218、`import_repo` 226、`summarize_code` 123、`extract_readme` 123、`talk_action` 168、`invoice_ocr` 189) | ~1,700 | 同上 | 额外:联网/OCR 部分走你的 tool 注册表,不在 Action 里直接发 HTTP |
| `actions/requirement_analysis/` 12 件 | ~1,400 | 三段式 prompt 与 evaluate 结构 | 同上整批替换。**平台场景里这是"垂直 SOP 模板",不是必须,可推迟到 S6 之后** |
| `roles/` 18 个类文件 | 3,485 | profile / goal / actions 列表 / watch 订阅,逐字 | **不搬类样板**。18 个角色 → 2 引擎 + registry profile。例外:`di/role_zero.py` 491 与 `engineer.py` 513 认真做 |
| `memory/`(`memory.py` 112、`role_zero_memory.py` 201、`brain_memory.py` 345、`longterm_memory.py` 78) | 736 | 列表 + 索引 + token 压缩语义 | `longterm` 后端换 Qdrant hybrid(R9);`brain_memory` 的 Redis key 语义照源 |
| `rag/`(`schema.py` 274、`engines/simple.py` 411、`factories/` 6 件、`retrievers/` 6 件、`rankers/` 3 件) | ~1,400 | 检索编排结构与 rerank 分数融合 | 底座换 LangChain retriever + Qdrant named vectors;`document_store` 四后端(faiss/chroma/milvus/lancedb)**不搬** |
| `strategy/`(`planner.py` 192、`thinking_command.py` 116、`base.py` 109、`task_type.py` 86、`experience_retriever.py` 1,243) | ~1,750 | 计划-执行的数据结构与 prompt | 执行换 LangGraph 子图;`experience_retriever` 是 RoleZero 命脉,认真做 |
| `exp_pool/` 20 件(manager 242、decorator 230、四组子包) | 990 | schema 与序列化格式、`@exp_cache` 语义 | 存储换 Qdrant + Redis 命中计数;`serialize` 相关改 checkpointer |
| `tools/` 其余(`tool_convert.py` 139、`libs/terminal.py` 282、`libs/browser.py` 211、`libs/linter.py` 233、`libs/index_repo.py` 461、6 个 `search_engine_*` 挑 2 个、3 个 `web_browser_engine_*`) | ~1,800 | 工具函数本体 | 联网/浏览器部分收进沙箱与 tool 注册表;搜索引擎**只留 ddg + serper**,其余 4 个是等价实现不必都要。**2026-09-15 实况**：搜索引擎已按"只留 ddg+serper"收成一件 `tools/search_engine.py`(72 行)；`libs/{terminal,linter}.py` 已落地；`libs/browser.py` + 3 个 `web_browser_engine_*` 判**「推迟」**（playwright 未装 + 依赖的 `utils/a11y_tree.py` 本仓没有 + 全仓零调用者，三条实测见施工2 §S4）；`tool_convert.py`、`libs/index_repo.py` 未动 |
| `repo_parser.py` 1,023 | 1,023 | 类图/序列图的输出结构(前端与 RAG 都吃它) | 解析换 tree-sitter;你现项目只有 63 行,是全项目最大的真实缺口之一 |
| **`utils/graph_repository.py` 394 + `di_graph_repository.py` 168 + `visual_graph_repo.py` 162**（自查后从 `弃` 移回） | 724 | SPO 三元组抽象(`insert`/`select`/`delete`/`generate_mermaid_graph`)、`GraphKeyword`(:23)、networkx 实现、JSON 持久化 | 它是 `import_repo` / `rebuild_class_view` / `rebuild_sequence_view` / `extract_readme` 四个 Action 的**存储层**；mermaid 文本导出交前端，其余保留。三个判定必须自洽，不能一边做 Action 一边删它的库 |
| **`utils/mermaid.py`**（自查后从 `弃` 移回） | 164 | `mermaid_to_file(engine, graph, path)` 签名 + `:94-104` 的引擎分派结构 | 被 `write_prd.py:280`（竞品四象限图）与 `design_api.py:233`（时序/类图）直接调用。只接 **playwright** 一个引擎，`mmdc_pyppeteer`/`mmdc_ink` 判弃；是否同时保留前端导出见 §五 决策项 2 |

## 四、`弃`：平台不需要，明确不搬（防范围蔓延）

| 源 | 行数 | 理由 |
|---|---|---|
| `ext/` 138 文件 | ~20,000 | 论文/研究配套,你已排除 |
| `environment/{minecraft,android,werewolf,stanford_town}` | 2,801 | 只服务 `ext/`;android 要 grounding-dino 权重、minecraft 要 mineflayer node 子树 |
| `provider/` 16 家手写客户端 | ~2,300 | R1 已覆盖,LangChain 白拿 |
| `document_store/{faiss,chromadb,milvus,lancedb}` | 337 | 同源能力,Qdrant 一个后端够了 |
| `utils/mmdc_pyppeteer.py` + `utils/mmdc_ink.py` | 309 | 三引擎里的两个冗余实现。**`mermaid.py`(164) 本体不弃**——`write_prd.py:280` / `design_api.py:233` 直接调 `mermaid_to_file`，改判 `改`（保签名与三引擎分派结构，只接 playwright 一个引擎），见 §三 |
| `utils/make_sk_kernel.py` + `skills/` SK 执行 + `management/skill_manager.py` | ~190 | Semantic Kernel 路线你不走 |
| `utils/a11y_tree.py` 312 + `omniparse_client.py` 238 + `parse_html.py` 76 | 626 | 无障碍树/文档解析交 LangChain loaders |
| `learn/` TTS 与文生图 5 件 | ~290 | 外部服务依赖,平台不做多媒体生成 |
| `tools/{azure_tts,iflytek_tts,openai_text_to_image,metagpt_text_to_image,openai_text_to_embedding,moderation,translator}` | ~550 | 同上 |
| `tools/{ut_writer,prompt_writer,metagpt_oas3_api_svc,openapi_v3_hello}` | 459 | 老插件协议遗留 |
| `tools/swe_agent_commands/`(3 py + 6 sh) | ~600 | SWE-bench 评测专用;但 `SweAgent` 角色要用终端工具,可后置 |
| `tools/search_engine_{bing,googleapi,serpapi,meilisearch}` | ~400 | 4 个等价实现,留 2 个 |
| **`tools/tool_data_type.py`**（2026-09-15 由 `复` 改判） | 13 | `Tool.schemas`/`Tool.code` 是 R8 **反射半件**的载体（把函数签名转成 YAML schema）。本仓 `role_zero.py:84` 只读 `t.name`/`t.description`，LangChain `BaseTool.args` 已覆盖其余，复制过来**零读者**。按 §一 末第二条纪律判：先 grep 调用者，无调用者即弃。注册表数据结构那半件另见 `tools/tool_registry.py`（自写 60 行，见 `fc705ef`） |
| `utils/{s3,stream_pipe,highlight,project_repo}` | ~600 | 分别被本地卷+SSE+前端渲染+ArtifactStore 覆盖。**⚠ 原列在此处的 `graph_repository`/`di_graph_repository`/`visual_graph_repo` 已移出**——它们是 4 个 Action 的存储层，判错，见 §三 |
| `utils/parse_docstring.py` / `dependency_file.py` / `save_code.py` / `read_document.py` | ~270 | 按需,平台层用不到 |
| `subscription.py` 100 | 100 | 公众号推送。要接渠道(Slack/企微)时再按渠道抽象重做 |
| **预算强制**：`utils/cost_manager.py` 的 `max_budget`/`check_budget`/`_is_within_budget` 语义 + `team.py:92-100` 的 `invest`/`_check_balance` + `NoMoneyException` | ~60 | **计费域不进 agent 内核**（见 §零 定案）。源靠"每轮问前查余额、超支抛异常"停团队，新栈既不抛也不停：`TeamState` 不再有 `budget_used`，图上是 `START → router`。⚠ 只判弃**强制**那一半，`update_cost`/`get_costs` 的**只读计量**留在 §二 与 N4 trace |
| `llm.py` 20 / `_compat.py` 23 / `startup.py` 10 | 53 | 兼容层与入口,你的入口是 FastAPI |

**合计(自查修正后)**：`弃` 约 10,100 行、`重` 约 6,717 行不复制。相对自查前，从 `弃` 移回 888 行(graph 三件套 724 + `mermaid.py` 164)，从 `重` 移回 116 行(`base_serialization` 67 + `serialize` 映射函数 49)进 `复`。

你的真实待写量因此从"44k 移植"收敛到 **约 10–12k 行**(其中 ~9k 是近乎零改写的 `复`，其余是 `改` 的适配工作)。

> 这些行数是**量级估计**，每做一轮自查就会变动；它的用途是防止范围蔓延和判断阶段工作量，不是预算。真正可核对的是 §一 的 R 表与 §四 的弃表——**动任何一项判定，先按第二条纪律 grep 调用者**。

## 五、`新`：源没有、agent 平台必须有

这几项决定了"平台"两个字,源项目里没有对应物,不要指望从源搬。

| # | 能力 | 为什么源没有 | 落在哪 |
|---|---|---|---|
| N1 | **多租户与账号边界**:user_id 贯穿 session/quota/记忆/产物,检索按 payload 过滤 | 源是单人 CLI | S7 |
| N2 | **扩展点三件套**:自定义 Role / Action / Tool 的注册 API + 声明式配置(profile+actions+watch 三样就能造一个角色) | 源靠继承类改代码 | S6 定义、S7 暴露成 API |
| N3 | **执行策略可插拔**:同一 profile 可选 SOP 流水线或 RoleZero 自主循环 | 源两范式各写一套类 | S3 |
| N4 | **全链路 trace 落库**:每节点耗时/token/输入输出可查,能出 P95 | 源只有打屏日志 | S7/S8 |
| N5 | ~~会话级配额与预算~~ → **拆半作废（见 §零）**：**限流保留**（并入 N1，按 user_id 的并发/请求数限流，超限在 HTTP 入口拒绝）；**每用户每会话预算作废**（不做 `max_budget`、不做超限停止、不做预测式判断） | 源进程内 cost_manager 的记账部分本就无需移植 | S7（仅限流） |
| N6 | **编排可视化 + 时间旅行回放**:从图定义直接画出编排,任意 superstep 重放 | 源没有图对象 | S8 |
| N7 | **SOP 模板化**:把 `SoftwareCompany` 那种硬编码组队变成可存取的模板 | 源写死在 `software_company.py` | S6 |
| N8 | **离线评测门禁**:`rag/benchmark` 从脚本变成 CI 门禁 | 源是脚本 | S9 |

---

## 六、接线台账（2026-09-16 对账开表；"已实现未接线"点的唯一处置账）

> 背景：全仓对账（codegraph 读源 + 双向 grep）查出约 20 处"组件在、通电没有"的点。本表是它们的**唯一处置账**：每行要么有调用点（状态=已接线，附 file:line），要么有处置判定（删除/推迟/判弃入册）。收口标准=状态列无"待"字。B1–B6 逐片回写。

| # | 件 | 现状证据（2026-09-16 实测） | 处置 | 片 | 状态 |
|---|---|---|---|---|---|
| 1 | R2 `_structured()` 零生产调用（`base/action.py:62`） | actions/ 下 12 处直连 `llm.structured(`；`agent.py:88`/`plan_and_act.py:36`/`role_zero.py:155` 三处非 Action 形态保持直连（前两处无 output_schema 类属性，role_zero 已被 exp_cache+自身重试包住——**这是理由不是欠账**） | BaseAction 子类的 9 处切 `_structured`（edge_actions 3 处随 #13 消亡）；门禁钉"缺字段→定向补丁→字段齐"行为 + actions/ 直连归零 grep | B1 | ✅ 已接线（2026-09-16：s6 t20/t21；落地时拆掉 write_prd 对基类 `_ask` 的遮蔽覆写，基类 `_merge` 与 WritePRD._merge 撞名改 `_merge_patch`，新增 `patch_exempt` 豁免"空值即合法答复"字段如 anything_unclear——三处均是"接缝没通电就没人知道线接错了"的实锤） |
| 2 | WriteCodeReview 完整未装配 | 无任何动作表持有；registry Engineer 死 watch `WRITE_CODE/WRITE_CODE_REVIEW`（team_graph SOP 键集无此二 tag） | 装配进 Engineer 动作表 `[WriteCode, WriteCodeReview, SummarizeCode]`（源 engineer.py:128-137"每写完即 CR"由 BY_ORDER 游标表达，不加新 SOP 边）；死 watch 删 | B2a | ✅ 已接线（2026-09-16：team+registry 两表同装、死 watch 三只摘除；s3b t13 钉行为序列与三表互洽——e2e 手写装配早已是此形态，这次落后的是生产侧） |
| 3 | PrepareDocuments 零实例化 | 全仓无调用点；源 PM 显式前置它（`roles/product_manager.py:45-46`） | classic_team PM 前置 `[PrepareDocuments, WritePRD]`；requirements_filename 从此有生产者 | B2a | ✅ 已接线（2026-09-16：PM BY_ORDER，s3b t13 断 requirements.md 落盘） |
| 4 | WriteCode 缺源增量重写分支 | 源 `write_code.py:119-142` REFINED_TEMPLATE + use_inc 上下文置顶语义，本仓恒走新建 prompt | 常量脚本摘取；触发=bug 工单在（本仓无 config.inc，工单即增量态） | B2b | ✅ 已接线（2026-09-16：REFINED/六段上下文逐字入 t11 值比对；inc 等价信号定为"CodingContext 带 code_plan_and_change_doc"；use_inc 旧码置顶含源 main.py 例外；s6 t22 全链钉） |
| 5 | WriteCodePlanAndChange 20 行壳、SOP 边有订阅无产端 | `actions/write_code_plan_and_change.py:14-20`；`team_graph.py:35` 死边 | 加厚为源六段上下文（源 write_code_plan_and_change_an.py:217-240 逐字）+ FIX_BUG→先产 PlanAndChange 再 WriteCode(重写) | B2b | ✅ 已接线（2026-09-16：Agent 新增按因装配 `plans/default_plan`（源 engineer._new_code_actions 的浓缩，构造即校验动作名互洽）；SOP 的 WRITE_CODE_PLAN_AND_CHANGE 边两端活了；顺带合并 BUGFIX_FILENAME/DocName.BUGFIX 同物两值的死孪生） |
| 6 | RunCode Send To 只生成不消费；DebugError 无条件写 SRC/ | `run_code.py:89` 仅 return_code 判路；`debug_error.py:55`（源 `qa_engineer.py:125-155`：Send To 分诊、测试产物 save 进 **tests/**） | 分诊两分支（指 Engineer→具名投递；否则 QA 自修测试写 tests/）+ DebugError 按源语义落目录 | B2c | ✅ 已接线（2026-09-16：parse_recipient 逐字件真当路由判据；Engineer 走具名 send_to（源 mappings:126）、QA 走 <self> 并上 debug_rounds 闸（源 test_round 语义）；三跳回流 instruct 全改**恰好消费方 schema 形态**——旧"回流键名对齐"其实被 extra=forbid 静默炸断，是假回路；s6 t23 三分支+形态兼容钉） |
| 7 | Editor 1135 行零生产接线 | `_shims.register_tool` 是 no-op（`_shims.py:10-14`）；RoleZero 只有整文件 write_file | 按源 `role_zero.py:147-167` 默认装配清单包 BaseTool 进 REGISTRY；run/kill 走 `sandbox.run_proc`+session_root；RZ profile 按 `TOOL_REGISTRY.select()` 白名单 | B3 | 待执行 |
| 8 | git.py 悬空 import | `:67/:129` 运行时 `from metagpt.utils.git_repository`（该件判弃未搬）——一调即 ModuleNotFoundError | 换本仓 subprocess-git 简版，进 REGISTRY tags=["git"] | B3 | 待执行 |
| 9 | editor.similarity_search 悬空 | `editor.py:1124` import 判弃的 index_repo | 删方法（源侧唯一消费者也是它自己） | B3 | 待执行 |
| 10 | schema.Plan 状态机零消费者 | 拓扑排序/级联 reset/游标整套死码；`role_zero.py:136-139` 文本桩、`:226` 吞 Plan.finish_current_task | RoleZero 真用 Plan 驱动命令与 get_plan_status | B4 | 待执行 |
| 11 | rag/knowledge.py 零运行时消费者 | 全仓无调用（repo_parser.py 注释称"在用"系虚指） | 吸收：`_rerank` 接缝移进 `longterm.recall()`，删件；台账记"RAG 上半截由 ltm/exp 承担"（源 MetaVectorStore 线同样未接，自洽） | B4 | 待执行 |
| 12 | DI 人在环评审闸门缺 | 源 planner.py:96/:104 两闸门消费 AskReview（§一 改判行） | plan_and_act 计划产出/任务结果两处 interrupt 等价 | B4 | 待执行 |
| 13 | edge_actions 五孤儿 + ExtractReadMe | 六只里仅 ExtractReadMe 有源消费（import_repo.py:20）；其余五只源侧=fire CLI/tests 自证 | 五只删（含 test_p1 ⑤ 段随收）；ExtractReadMe 保留转 N2 示例件，补 graph_repo 入库腿 | B5 | ✅ 五只已删（提前并入 B1 刀口，test_p1 ⑤ 已收缩）；ExtractReadMe 入库腿待 B5 |
| 14 | RebuildClassView 无装配点 | 件完整（批2c），无调用方 | **推迟入册**：与 rebuild_sequence_view 同判——等 N6 前端把 class_view.mmd 真实渲染再装进 SOP | 推迟 | 已登记 |
| 15 | 网关压缩链缺位 | `CompressType` 字段在、零行为分支 | **推迟入册**：长输入已有 memory 窗护栏（`role_zero._compress`），真机截断实证在输出侧；S9 长上下文体检时再定要不要网关级 | 推迟 | 已登记 |
| 16 | ExpRetriever 静态 few-shot 家族 | 源为手写字母表式示例集（Keyword/TRD 分流，非账本旧口径"四模式"） | **判弃入册**：本仓角色集不同构，经验路径由 exp_pool 向量线承担 | 判弃 | 已登记 |
| 17 | 三处静默漂移 | `timeout` 源 600/本仓 300；`repair_llm_output` 源 False/本仓 True；`aask_code` 源 dict/本仓 str | 全部转**显式偏离**：注释与 gateway 头已改写（B0 完成）；k_times 抄录错已改回 2 | B0 | ✅ 已登记 |

---

## 动手前必须先定的两件事

1. **R2(ActionNode 字段级能力)**:`with_structured_output` 是整结构级,源是字段级。要么接受一次成功率/成本退步,要么把 R2 做成通用 validator 中间件。**我建议后者,并且它是 N3 的前置。**
2. **图表产物走服务端还是前端**（自查后 `mermaid.py` 已判 `改`，此题不再是"要不要放弃"）：源在 `write_prd.py:280`（竞品四象限图）与 `design_api.py:233`（时序/类图）里调 `mermaid_to_file` 服务端出 png/pdf。两条路各有代价：
   - **保服务端**：要引入 playwright + chromium（重，且 `mmdc_*` 三个引擎实现我全判了弃，需自己接一个），产物文件树与源一致
   - **改前端**：Vue3 装 mermaid.js 渲染，产物树只存 `.mmd` 文本 —— 轻，但**产物文件树与源不同**，且"下载图表"要前端生成
   - 也可以**都做**：`.mmd` 落盘为真源，前端渲染 + 提供导出按钮。**我推荐这条**，但要在 S6 之前定，因为 `write_prd`/`design_api` 的落盘代码要按它写。
