## 状态
已接受（决定方向）+ 待批（金额与判据规模）

## 背景
C17/C16/B8 三件的截断与坏产出读数全部来自本机桩（`finish_reason` 是桩恒给的）。要证的最后一层是
**真端点会不会连 usage 都不回**。实测单价：`step-3.5-flash` 一句 28 字问答 = ¥0.27（thinking 全计进输出），
压 `LLM__MAX_TOKEN` 到 512 **只静默截断、不省钱**（¥0.12）。参照：A4 一场 dynamic 会话含前置探针 ≈¥0.60。
额度口径由用户给（现记 15 元）。

## 决策
**不零散烧**：攒到 C6（工具推荐）之后一次覆盖「截断 + 召回 + 成本」三族。
但**预算必须绑判据、不是绑金额**：¥5 ÷ 实测单价 ≈ 20–40 句，而 C6 的判据「工具集变小且命中率不降」
要的是一批真检索问答——**¥5 够烧 C17 那一发，不够跑 C6 的真评测**。
所以拍板时要一起定：要么把 C6 判据缩到小规模（如 20 问）配 ¥5，要么按规模另批。

## 后果
正面：钱花在能同时闭三格的地方；不会出现「烧到一半停住」那种最难看的形态。
负面：在此期间账面长期挂着「真模型下的行为未证」——但它不误事，因为我们改的恰是「拿不到 usage 也要说清」那一侧。
纪律不变：**需要真模型的项一律标未验 + 原因，不许拿 FakeLLM 或桩读数冒充**（PLAN §0.5）。

## 相关链接
`plan/PLAN.md` §2 A4 行末、§4 C17 行末① · 记忆 `reference-local-model-runtime` · ADR-20260922-04

---

## 补记（2026-09-22 16:2x，用户解除预算限制之后）
先跑的是**免费那条**：本机 ollama 7B 真模型整场（会话 `2fff201a`）。结果两半都有读数——
**7B 具格**（真挂审批卡、真 usage 落账 pt2434/ct75），**但截断形状拿不到**：
`LLM__MAX_TOKEN=24` 经 `/v1` 得到 `finish_reason=stop`、ct=118、`truncated_calls=0`，
而原生 `/api/generate` 的 `num_predict=24` 给得出 `done_reason=length`。
⇒ **不是模型不截，是 ollama 的 OpenAI 兼容层不吃 `max_completion_tokens`**。
云端 key 现在**不在手上**（`.env` 里是注释掉的空值、`/tmp` 无 key 文件），所以这一发仍在等：
把 key 用单条命令的 env 给（不落盘、不进 `.env`、日志打码）就能立刻跑 A4 四步。
顺带顶出两格真缺陷，已立 **C18**（待批态被启动自愈抹掉、批准后连接失败直接判 failed）。
