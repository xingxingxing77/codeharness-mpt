# 施工 2 · 能力层（S4–S5）

> S4 给角色手和脚,S5 给记忆与检索。两步都不碰编排,可与 S6 并行推进。

## S4 · 工具与沙箱

### 产出文件
```
codeharness/tools/{__init__,tool_registry,tool_data_type,tool_convert}.py
codeharness/tools/libs/{editor,git,shell,terminal,linter,browser,index_repo,env}.py
codeharness/tools/sandbox.py
codeharness/tools/search_engine.py  search_engine_ddg.py  search_engine_serper.py
codeharness/tools/web_browser_engine.py  +_playwright.py
tests/s4_tools.py
```

### 复制（`复`）
| 源 | 行 | 备注 |
|---|---|---|
| `tools/libs/editor.py` | 1,139 | ⚠ 已复制 1,135 行；**生产路径零 import 者**（未包成 `@tool` 接进注册表，`write_file` 覆盖了 MVP 需求）。门禁 t28 经 `Editor._lint_file` 真实 import 并断言了它，所以"import 不动/一改就炸"这类问题已被拦；但能力要真被角色用上还得等 S6 接线 |
| `tools/libs/shell.py` | 53 | ✅ 已落地，真被 `utils/common.py` / `utils/tree.py` import |
| `tools/libs/git.py` | 131 | ⚠ 已复制但**无真实 import**（唯一提到它的是 `tools/__init__.py` 末尾那条待办注释），待包成 `@tool` 接进注册表 |
| `tools/tool_data_type.py` | 13 | ❌ **2026-09-15 改判 `弃`**：它是 R8 反射半件的载体，本仓零读者，见判定表 §四 |
| `tools/libs/terminal.py` | 282 | ✅ **2026-09-15 已落地**（`15c0970`，200 行）：四处适配（workspace 根 / `terminal_block()` / 本仓注册表 tags / 新增 timeout），修掉源三处致命点（EOF 空转死循环、daemon 漏传导致 `get_stdout_output` 恒空、`close()` 无限等待）。**源 `Bash` 子类不搬**——它 source 的 `swe_agent_commands/` 判 `弃` |
| `tools/libs/linter.py` | 233 | ✅ **2026-09-15 落地 110 行**（`778b707`），替掉 `_shims.py` 里那个返回空的假 `Linter`。**不引 `grep_ast`/`tree_sitter_languages`**（本机未装 + 上游停维护）：源的 Python 链本来只经 flake8 → `compile()`，`basic_lint` 只对非 Python 有增量而源自己把 js/css/sql 指向 `fake_lint`，`tree_context`/`traverse_tree` 在源文件内零调用者。天花板与升级路径（挂 ruff）写在文件头 |
| `tools/libs/env.py` | 158 | ❌ **2026-09-15 判 `弃`**：源的 `get_env`/`set_get_env_entry` 是 app-key 注册表，本平台配置统一走 pydantic-settings 的 `APP__KEY`（R7）。唯一调用点 `utils/file.py:154` 由 `_config_compat.get_env_default` 覆盖——顺手修了它与调用点签名不匹配（同步被 await + 关键字名对不上，一调即 TypeError） |
| `tools/libs/browser.py` | 211 | ⏸ **2026-09-15 判「推迟」**（实测三条，不是"还没排上"）：① `playwright` 未装（`import` 即 ModuleNotFoundError，实测）；② 它顶层 `from metagpt.utils.a11y_tree import ...` 七个函数，本仓没有 `utils/a11y_tree.py`；③ 全仓 `grep -rn "browser\|scrape\|playwright" --include=*.py` 只命中两处**注释**（`tools/__init__.py` 的待办、`prompts/role_zero.py` 文件头的搬运说明），没有一处 import、也没有一处真进 LLM 的 prompt 字符串。照抄会得到一件装不上也无人调的文件——要交互浏览就与 per-session 容器同批做，落地时 `@register_tool(tags=["web"])` 挂进 `TOOL_REGISTRY` |

### 改造（`改`）
- `tools/__init__.py`(23)+ `tool_registry.py`(194)：**`@register_tool` 装饰器签名与"按 profile 选工具"的语义照源**,底座换 LangChain `StructuredTool`(**R8**)。源 `tool_recommend.py` 243 行那套"LLM 推荐工具"保留 prompt,决策路径接进 gateway
  - ✅ **2026-09-15 已落地（`fc705ef`）**，并按 §一 末第一条纪律拆开判：注册表数据结构 + 按名字/tag 选工具 → 自写 60 行（`tools/tool_registry.py`，选择语义照源 `validate_tool_names`：并集、未知 key 告警跳过）；函数签名反射 YAML → LangChain `args_schema` 白拿，判 `重` 不复制。`tool_recommend.py` 的 LLM 推荐工具**仍未做**
- `search_engine*.py` 6 个 → **只搬 `search_engine.py` 基类 + ddg + serper**。另外 4 个是等价实现,平台不需要四份(见判定表 `弃`)
  - ✅ **2026-09-15 落地**：三件收成一件 `tools/search_engine.py`(72 行)。没搬的是 `SearchEngineType` 枚举 + `model_validator` 里 `importlib` 选包的工厂仪式与三个 `@overload` 签名——本仓两个引擎一个调用方，`engine()` 一个 if 就够。ddg 换直取 HTML 端点（`requests`+`bs4` 已在依赖，源的 `duckduckgo_search` 本机未装）。**此前 `settings.search` 整件零读者，`search_internet` 恒返回降级文案（实测）**，接缝接上后 `SEARCH__SERPER_API_KEY` 才第一次真正生效
- `web_browser_engine*.py` 3 个 → 基类 + playwright;selenium 弃 → **2026-09-15 与 `libs/browser.py` 同批判「推迟」**（playwright 未装 + 零调用者，见上表）

### 重构（`重`）/ 新增（`新`）
- **`sandbox.py`**：源没有沙箱层,它的 `run_code.py`(173) 直接 `asyncio` 起子进程。平台要有隔离边界 → 写 `sandbox.run_context(ctx)`,**按会话分配工作目录与执行环境**,接口预留 per-session 容器实现(S7 接配额)。这是 `新` 不是 `改`
  - ✅ **2026-09-15 落地**：出口只有一个 `runtime.session_root()` = `workspace_root/{CURRENT_PROJECT}`（复用 S3 已有的 ContextVar 接缝，不另起一套会话对象）。四处统一收口：文件工具 `_safe()` 的边界、`execute_shell_async` 的 cwd、Terminal 起壳的 cwd、沙箱 `scratch/`；`ArtifactStore` 也改调它（原来同一算法各写一遍）。
  - 落地时才发现的既有洞：**工具层此前写的是 `workspace_root` 根，不是会话目录**——前端文件树读 `session.workspace`，所以 agent `write_file` 的产物在 UI 里根本看不见（端到端实测：改后同一份文件立刻出现在 `/workspace/files` 树里）。
  - 顺带修掉三处（各带断言）：① `session_root()` 只认 `workspace_root` 的直接子目录，脏 `project_name`（可能是 LLM 产出的）退回默认桶；`CreateSessionReq` 在入口 422 掉带路径分隔的名字；`server/api/workspace.py` 的读取口同样从 `startswith` 改 `is_relative_to`（兄弟目录前缀命中，与 t2 同源）；② `sandbox.run_proc` 一个函数替掉三处各写一遍的超时收尸，Windows 上按**进程树**杀（`proc.kill()` 只杀 cmd.exe，实测孙进程攥住管道 → `communicate()` 永久挂死，图节点跟着卡）；③ 超时保留已产出的输出——根因两条：被取消的 `communicate()` 会丢管道数据（自控 pump 解决）+ 子进程 python 对管道块缓冲（`PYTHONUNBUFFERED=1` 解决），只修前者实测仍是空串。
  - `RunCodeContext.additional_python_paths` 字段此前有名无读者，现在真进子进程 `PYTHONPATH`。
  - ⚠ **仍不做的**：`run_context` 不夹 `working_directory`（框架可显式指到会话目录外，per-session 的**强制**边界要等 S7 容器）；`_TERMINALS` 按会话登记后靠 `runner._forget` 收壳，非 runner 路径（CLI/自测）自建的壳不在此列。

### 门禁 `tests/s4_tools.py`
- 注册表完整性:源注册表里每个工具名都能解析到一个 callable
- `linter`/`editor` 用临时目录单测,断言写读往返
- **搜索引擎与浏览器全部 mock,测试零外网请求**
- ✅ **2026-09-15 落地 35 组**（`8db857d` + `fc705ef` + `15c0970` + `778b707` + 本轮），当场抓出并修掉：`tools/_safe` 前缀逃逸、四处超时/收尾不 `drain`、假 `Linter` 垫片的 async/sync 漂移、源行号解析的 POSIX 冒号假设（Windows 上恒退化成第 1 行）、`get_env_default` 与调用点签名不匹配。注册表 6 组覆盖名字/tag 并集去重与未知 key 跳过；`Editor._lint_file` 走真消费者路径。
- 本轮新增 6 组：`t30` 会话目录互不可见 + 脏目录名退回默认桶、`t31` 超时保留已产出的输出（含 `execute_shell_async` 那条此前无断言的超时分支）、`t32` 超时杀整棵进程树（拿 Windows「活进程锁住工作目录」当探针，`rmtree` 删不掉就是没杀干净）、`t33` Terminal 按会话登记且关后可重登记、`t34` `additional_python_paths` 真进 `PYTHONPATH`；`t10/t11/t11b` 重写成"喂 canned HTML/JSON 给真解析函数"——零外网同时不再只测 stub，还钉住了引擎路由（配 key 走 serper 并带 `X-API-KEY`）。
- ⚠ **仍未被断言覆盖**（别拿 35 组当 S4 收口）：`Editor` 的读写命令面（`read`/`edit start:end`/`create_file`，等它接进注册表时一起补）；per-session 的**强制**边界（`run_context` 仍可被显式 `working_directory` 指到会话目录外，要等 S7 容器）；server 层三处改动（`_forget` 收壳 / 入口名字校验 / 文件读取 `is_relative_to`）只在本次会话里手工端到端跑过，**没有 S8 级 API 门禁**——`tests/` 目前无一件起 TestClient。
- ⚠ **2026-09-15 实测：两件 `复` 成品在生产线是零读者的预置资产**——`libs/editor.py`(1,135) 除门禁 t28 外**无任何生产 import**（未包成 `@tool`）；`libs/git.py`(131) 只出现在 `tools/__init__.py` 末尾那条"复制后包 @tool"的待办注释里。按 §一 末第二条纪律，判定要与调用方自洽：**S6 要么把它们经 `@register_tool` 接进 `TOOL_REGISTRY` 并被角色 profile 选到，要么明确改判**，不能停在"已复制 = 已落地"。

---

## S5 · 记忆、检索与经验池（升级最密集的一步）

### 产出文件
```
codeharness/memory/{memory,role_zero_memory,brain_memory,longterm,memory_storage}.py
codeharness/document_store/{qdrant_store,artifact_store,exp_store}.py
codeharness/rag/{schema,interface,knowledge}.py  rag/{engines,retrievers,rankers,factories}/
codeharness/strategy/{planner,thinking_command,task_type,base,experience_retriever}.py
codeharness/exp_pool/  (manager, decorator, schema, serializers/, scorers/, context_builders/)
codeharness/utils/redis.py
tests/s5_memory_rag.py
```

### 拆片与接口定案（2026-09-15）

S5 整步一次做完不现实（`experience_retriever` 单件 1,243 行），按**每片自带门禁**切三片：

| 片 | 内容 | 门禁 |
|---|---|---|
| **S5.1** | `utils/redis.py` + `memory/brain_memory.py` + RoleZero 工作记忆接线 | Redis 有/无两种配置都绿；上一轮工具结果确实进下一轮 payload |
| **S5.2** | **R9** `document_store/qdrant_store.py`：named vectors(dense+sparse) + 单 collection + payload 多租户 + quantization；`longterm`/`exp_store`/`knowledge` 三件改接它 | 同一批 query 的 **hit-rate@5 单向量 vs hybrid** 两份结果存盘（Qdrant 本地模式 `path=`，不依赖 docker） |
| **S5.3** | `exp_pool/` manager+schema+serializers + `@exp_cache` 真接线 + Redis 命中计数 | 序列化 roundtrip 逐字段相等；命中计数改变排序 |

**S5.1 接口定案**：

- `utils/redis.py`（`复` 63 行，只改 import）：`Redis(config=settings.redis)` / `await get(key)` / `await set(key, data, timeout_sec)` / `await close()`。**降级语义照抄**——连接失败 `logger.warning` 后返回 `None`，绝不抛，这是「没起容器也能跑自测」的前提。
- `memory/brain_memory.py`（`复改` 345 → 约 110 行）：留 `history`/`knowledge`/`historical_summary`/`last_history_id`/`is_dirty` + `loads`/`dumps`(整体 JSON 存**单个** redis key，dirty 才写) + `to_redis_key(prefix, user_id, chat_id)` + `add_talk`/`add_answer`/`add_history`(id 单调守卫) + `set_history_summary` + `summarize`/`_summarize`(`DEFAULT_MAX_TOKENS=1500` 分窗滚动) + `split_texts` + `history_text`。
  - 判 `弃` 四条（各带 grep 复核，遵 §一 末第二条纪律）：`_metagpt_summarize`/`_metagpt_is_related`/`_metagpt_rewrite` 三个 `MetaGPTLLM` 分支（源为 AgentStore 无模型兜底，本仓 FakeLLM 已承担该职责）；`get_title`（全仓零调用者，S8 前端另有标题来源）；`is_related`/`rewrite` 两个 RAG 查询改写（本仓 `rag/` 走 LangChain，零调用者，有真实消费者再写）。
- **`memory/summarizing.py`(17 行) 删除**，由 `brain_memory` 取代：它是零调用者的预置资产，且与 BrainMemory 正重复（同一件「滚动摘要」两套写法）——删它不是砍功能。
- ⚠ **本轮实测抓到的真实缺陷（不是推测）**：`roles/role_zero.py:102` 每轮只发 `[SystemMessage, HumanMessage]` **两条**消息，`history` 里的 thought 仅以 `_plan_status` 的 5 行 80 字截断出现，**`_act` 拿到的工具结果从不回喂**；而 `prompts/role_zero.py` 的 `CMD_PROMPT` 反复要求 "review the conversation history"（:11/:90/:91/:114）。结果是 RoleZero 跨轮失忆，且 `CMD_PROMPT.format(current_state="ready")` 是硬编码占位。修法即 S5.1 的接线：`Memory` 做工作记忆（`memory_k` 窗口照源 `roles/di/role_zero.py:97`），溢出交 `BrainMemory` 摘要落 Redis。
  - 顺带：`settings.memory_overflow_size`(settings.py:77) 与 `configs/compress_msg_config.py` 的 `CompressType` 目前**都是零读者**——源项目的压缩在 `provider/base_llm.compress_messages` 里，R1 弃源 provider 时把这件事弄丢了。`memory_overflow_size` 本轮接上；`CompressType` 属 S2 计量链，仍缺读者。

### 复制（`复`）
- `utils/redis.py` 63 行整份:惰性连接、失败吞异常返回 `None`。**这个降级语义照抄**——它保证没起 Redis 时平台仍能跑,是 FakeLLM 自测不依赖容器的前提
- `memory/brain_memory.py` 345:`history`/`knowledge` 两个 Message 列表 + `historical_summary` + **整体 JSON 存单个 redis_key** + 按 `DEFAULT_MAX_TOKENS` 自动压缩。key 语义照源
- `memory/memory.py` 112、`role_zero_memory.py` 201:列表 + 按 `cause_by` 建索引的语义照源
- `rag/schema.py` 274、`strategy/{planner,thinking_command,task_type}`、`exp_pool/` 的 schema 与序列化格式

### 重构（`重`→ 这是本步的重点）
- **R9 · Qdrant 检索层**。核实过源 `qdrant_store.py:94-124`:单 dense 向量 + `k=10`,无 named vectors、无 sparse、无 quantization,`write()` 是空 stub,裸 `except:` ×2,还在用已废弃的 `recreate_collection`。你要写的:
  - **named vectors**:一条 record 同时存 `dense`(bge-m3 1024 维)与 `sparse`(bge-m3 词权重)→ Qdrant 服务端 `fusion` 做 hybrid 召回
  - **payload 多租户**:把 `user_id` / `session_id` / `doc_type` / `project` 放进 payload 做 filter,**用单 collection + payload 隔离取代"每租户一 collection"**(N1 的前置)
  - **scalar/binary quantization** 降内存,并保留原始向量可回退
  - rerank 保留已定稿的 bge-reranker-v2-m3、粗排 k=10 → 精排 top_n=5、**离线自动降级**
- **R9 之外**:`document_store` 的 faiss/chroma/milvus/lancedb 四件**不搬**(判定表 `弃`)
- `rag/` 的 `retrievers/` `factories/`:编排结构照源,底座换 LangChain retriever。`benchmark/base.py` 201 保留(它到 S9 变 CI 门禁)
- `strategy/experience_retriever.py` **1,243 行,是 RoleZero 的命脉**,`改` 要当`重`的量级做;`tot.py` 277 判 `弃`(平台不用 ToT 搜索,`strategy` 的可插拔由 N3 承接)
- `exp_pool`:存储换 Qdrant + **Redis 命中计数排序**(源靠 LLM judge 打分选经验,`perfect_judges` 真实评测推迟到终验之后)

### 门禁 `tests/s5_memory_rag.py`
- BrainMemory:起 Redis 跑往返;**再关 Redis 跑同一份测试**,断言降级不抛异常
- embedding 用 hash-fake,不联网
- **升级度量(本步唯一重要的数字)**:同一批 query,单向量的 hit-rate@5 vs hybrid(dense+sparse)的 hit-rate@5,两份结果都存盘。**没有这张表,S5 就只是"改写了",不是"升级了"**
- `exp_pool` 序列化 roundtrip 逐字段相等

### S5.1 落地状态（2026-09-15 晚，门禁 11 组）

- ✅ `utils/redis.py`：`复` 63 行语义照抄，降级路径实测（死端口 6399 → 返回 `None`/`False` 不抛）。
- ✅ `memory/brain_memory.py`：`复改` 345 → 168 行。四条零调用者分支判 `弃`；`memory/summarizing.py` 与它正重复，已 `git rm`。
- ✅ `roles/role_zero.py` 工作记忆：`Memory` 窗口 + `_act` 结果回喂 + 超窗交 `BrainMemory` 摘要落 Redis（key 走 `CURRENT_PROJECT`）。`settings.memory_overflow_size` 从此有读者。
- ⚠ 本片的实测口径：断言打在 **FakeLLM 收到的 messages 列表**上（要钉的就是"上一轮结果有没有进下一轮 prompt"），Redis 那几组打真连接、收尾统一删 key；`live_redis()` 探不到时 t1/t3/t5/t9 跳过，t2 仍验降级。
- ❌ 未做（别当 S5 收口）：R9 named vectors 与 hit-rate@5 对比表（S5.2）；`exp_pool` 闭环（S5.3）；`brain` 的会话级 resume 只在 `RoleZero._think` 首轮 loads，runner 侧等 S7；`longterm.py`/`exp_store.py`/`knowledge.py` 三件仍是单 dense 向量 + 每租户一 collection，且都还是零调用者。

### S5.2 落地状态（2026-09-15 晚，R9 检索层）

容器就位方式（用户指定复用本机镜像，不拉新 tag）：`docker run -d --name codeharness-qdrant -p 127.0.0.1:6333:6333 -v codeharness_qdrant_data:/qdrant/storage qdrant/qdrant:latest` → server **1.18.2**（client 1.19.0）。⚠ `docker-compose.yml` 里 pin 的 `qdrant/qdrant:v1.19.0` 本机没有，联网拉取未试；`latest` 实测比 client 低一个 minor，四条 R9 能力都可用（下面逐条实测）。Redis 用的是宿主机已在跑的 6379，**没起 compose 的 redis**（端口会撞）。

- ✅ `document_store/qdrant_store.py`（判 `重`，源 176 行不复制）四条形态全部实测在真集合上看得见：
  1. named vectors：`dense`(COSINE) + `sparse`(`Modifier.IDF`)，一条 record 两个向量；
  2. 单 collection + payload 多租户：`user_id`(`is_tenant=True`)/`session_id`/`project`/`doc_type` 建 keyword 索引，取代旧的「每项目一 collection」；collection 名取 `QDRANT__COLLECTION_PREFIX`（**该环境变量此前因为 `QdrantConfig` 里没这个字段，一直被 `extra="ignore"` 静默丢掉**，本轮补上并删掉零读者的 `collection_name="m3"`）；
  3. 服务端 `Fusion.RRF` 融合两路 prefetch（不是应用层拼结果）；
  4. INT8 标量量化，原始向量留在盘上可回退。
  - client 1.19 的三个 API 坑（写下来省得再撞）：`QuantizationConfig` 的枚举类型名对不上、直接构造会 `Cannot instantiate typing.Union`，**量化配置传 dict 形态** `{"scalar": {"type": "int8", "always_ram": False}}`；`VectorParams` **不支持** per-vector quantization，量化只有 collection 级；prefetch 里两路都要显式 `using="dense"/"sparse"`，否则 400 `Not existing vector name`。
- ✅ `sparse_from_text`：`crc32(词) % 2^20` 当下标、`1+log(tf)` 当值。**不用 `str.hash()`**——它按进程加盐，用它做下标等于每次重启索引作废（门禁 t14 起子进程换 `PYTHONHASHSEED` 钉住这条）。
- ✅ `memory/longterm.py`、`document_store/exp_store.py`、`rag/knowledge.py` 三件全部改接到这一层：不再是「单 dense 向量 + 每租户一 collection」，`knowledge` 也不再经 `langchain_qdrant.QdrantVectorStore`（那层把 payload 结构钉死，多租户 filter 与 sparse 塞不进去）。`_rerank` 的静默降级补了 warning（docs P1：精排长期缺席也不会被发现）。
- ✅ 接线：`RoleZero._compress` 溢出批次现在两路走——逐字引用进 Qdrant(`ltm.overflow`)、背景理解进摘要(`brain`)，分工照源；`self.ltm` 从此有构造点（`default_team` 里按 `settings.enable_rag` 建，**这个开关此前也是零读者**）。embedding/Qdrant 不可用只降级 + warning（t17）。
- 📊 **升级度量（S5 唯一重要的数字，实测）**：`storage/benchmark/s5_hitrate.json` —— 24 条语料、4 条 query，`hit@1 2/4 → 4/4`、`mean rank 1.75 → 1.0`，`hit@5` 两边都 4/4（语料太小，@5 已饱和，这点如实记在表的 note 里）。dense 用的是 hash-fake bag-of-chars，**真 bge-m3 上线后要拿同一份 query 集重测**，那张才是 S9 的基线。
- ⚠ **仍未闭合**：`EMBEDDING__*` 未在 `.env` 配置（默认指向离线 `localhost:9998`），所以 `LongTermMemory`/`KnowledgeBase`/`ExpStore` 的**真实语义向量路径从未跑过**，只跑过 hash-fake；`exp_pool` 闭环是 S5.3；`document_store/artifact_store.py` 与这一层无关（文件产物，不走向量）。
