# 对照 3 · 工具调用 · 本项目 vs 源 MetaGPT

> 口径与状态词汇见 `对照1-上下文管理.md` 文件头。源快照 `E:\MetaGPT\metagpt`（version=1.0.0），本项目 `E:\Codeharness`。
> 源 `tools/tool_registry.py` 的机制在账本里判 `重`（R8）：保留「注册表 + 按 profile 选工具」的语义，底座换 LangChain `@tool`/`StructuredTool`。本文件核对：**换完底座后，源的每一项工具能力还在不在、有没有真被角色用到**。
>
> **复核时间 2026-09-19（批次2 落地后）**：上一轮（B2）加的 `search_enhanced_qa.py`/`import_repo.py` 两件零装配 Action，**批次2 处置**：① `SearchEnhancedQA` **删除**（与已接线的 `tools/search_internet` 工具功能重叠、且零装配——两份代码一份通电是最坏形态）；② `ImportRepo` 接进 `POST /{sid}/workspace/import_repo` 端点，从死件变有唯一调用者（顺带修了 `_call` 里把上一循环的 `item` 当每 node 判断的残留变量 bug）；③ ~~`write_analysis_code.py`/`generate_skill.md` 的 metagpt 包名改净~~——**批次2 直改 prompt 逐字资产撞上 s6 t1，批次3 已回退，正解是加法拼接校准常量（见 §结论-2）**；**2026-09-21 C9 复核：那条"正解"同样没有落点——三件隐患资产在生产路径零消费者，本项已改判 ⛔，见 §结论-2 末段**。门禁 `s11` 重写为经端点调达 + 越界 400 两条。实现度：联网/导入接线面提升，但 prompt 包名 bug 回到"登记待做"，整片约 75%。

## 结论速览

**实现度：约 75%——工具「面」是全的（注册表、选工具、schema 进 prompt、解析执行、沙箱隔离、搜索、代码执行、Git、Linter 全落地且有门禁），缺口集中在两处：LLM 推荐工具整件未做，以及源 DI 线的 notebook/kernel 执行面退化。** 本轮 B2 补的两个 Action（`SearchEnhancedQA`/`ImportRepo`）**没有改变这张账**：它们以独立 Action 形式存在，但既没进 RoleZero 的命令分发、也没有别的调用者（§四-A），其中一个还与早已接线的搜索工具重复。

四条最要紧的缺口：

1. **`tool_recommend.py` 未实现**：源有两级召回（`ToolRecommender.recall` BM25/TypeMatch + LLM `rank_tools`，`tools/tool_recommend.py:77-170`，RoleZero 构造时 `BM25ToolRecommender(tools=self.tools, force=True)` `roles/di/role_zero.py:110`）。本仓只有静态 tag 选择（`roles/registry.py:85`），**全仓 grep `bm25|recommender` 零实现**。后果现在不显（注册面 18 只，`tests/s4_tools.py:43-51`），但工具数翻倍时 18→N 只全量进 prompt，token 直接顶上去。
2. ~~**prompt 里的包名还是 `metagpt`（运行时隐患仍在，登记为待做）**~~ → **2026-09-21 C9 逐条取证后改判：今天不构成运行时隐患，而是「零消费者死资产」，且本行原先开的正解被门禁堵死**。原文说 `prompts/di/write_analysis_code.py:33,:101` 与 `generate_skill.md:12`、`metagpt_sample.py:19-20` 让模型 `from metagpt.tools.libs.*` / `from metagpt.llm`——本仓无 metagpt 包，「照此生成的代码必 `ModuleNotFoundError`」，但**触发路径不存在**：`grep -rn "from codeharness.prompts" codeharness/ server/` 的通电面只有 7 件（`role_zero`、`product_manager`、`di/architect`、`di/engineer2`、`di/swe_agent`、`di/team_leader`、`task_type`），三件隐患资产里的常量（`INTERPRETER_SYSTEM_MSG`/`STRUCTUAL_PROMPT`/`REFLECTION_*`/`CHECK_DATA_PROMPT`/`DATA_INFO`/`METAGPT_SAMPLE`）在 `codeharness/`+`server/` **零引用**；`utils/common.py:647 import_class` 自身零调用者，没有动态装载 prompt 的旁路；真在跑的 DI 线用的是 `actions/data_analysis.py:7` 自写的 `ANALYSIS_CODE_PROMPT`（装配于 `roles/registry.py:210-215`、`strategy/plan_and_act.py:78-79`），文本里没有 metagpt。⚠ **批次2 曾直接改这两个文件成 codeharness，撞上 s6 t1「prompt 顶层常量与源逐字相等」硬门禁（批次2 未跑 s6 漏检），批次3 已回退为逐字**。本行原先开的正解「**新增一个校准常量、在 WriteAnalysisCode 消费处拼接覆盖**」**同样不可执行**，两条实测理由：① 拼接只能打在消费处，而消费处不存在，写了就是新增死代码；② 想往 `prompts/` 里加常量也不行——s6 t1 除逐字外还断言顶层常量**键集相等**，本轮实测加一个常量当场红 `AssertionError: di/write_analysis_code.py 常量集不等，缺 set() 多 {'C9_PROBE_CALIBRATION'}`（`log/c9_probe_additive.out`；基线 `log/c9_s6_before.out` = 23 组 exit 0、比中 54 项，证明 t1 真在跑、不是"供体不在所以跳过"）。**重开条件（谁接谁负责）**：一旦有人把这三件常量接进生产路径，必须同批在消费处加法拼接校准常量、覆盖那句 metagpt 指引，逐字资产字节不动。`s4_tools` t38 维持"代码扫 `from metagpt`、prompts 豁免"（豁免的是逐字资产，非纵容 bug）。~~此隐患列 §六 待做~~ → **§六-2 那条同步作废（见该条），PLAN §4 C9 记 ⛔**。
3. **`tools/libs/editor.py:80` 的死装饰器**：`@register_tool(include_functions=[...])` 形似源结构，实际是 no-op——`tool_registry.register_tool(tags=None, **kwargs)`（`:115-127`）把 `include_functions` 整包吞进 `**kwargs` 丢弃，且 `Editor` 不是 LangChain `BaseTool`，走的是 `_warn` 分支。真正接线的是 `libs/editor_tools.py` 的 11 个 `@tool`。
4. **notebook/kernel 执行面退化**：源 DI 线用 `ExecuteNbCode` 起真 Jupyter kernel（`actions/di/execute_nb_code.py:69`，被 `data_analyst.py:47`、`data_interpreter.py:42` 使用），有跨单元变量态；本仓是 `RunPythonCode`→`sandbox.run_python_code`（`actions/data_analysis.py:5`、`tools/sandbox.py:70`）**一次性子进程**，无 kernel、无跨轮变量保留。数据探索类任务的多步语义因此不同。本轮 B4 明确推迟，未做。

## 一、源项目怎么做

| 位置 | 机制 |
|---|---|
| `metagpt/tools/tool_registry.py:27-69` | `ToolRegistry` 双层字典 `tools` / `tools_by_tags`；`register_tool` 登记时反射生成 schema；单例 `:91` |
| `metagpt/tools/tool_registry.py:131-163` | `validate_tool_names`：一个 key 可为工具名 / `Class:m1,m2` 方法级裁剪 / tag / 文件路径，取并集，未知告警跳过 |
| `metagpt/tools/tool_registry.py:166-194` | 按文件路径**运行时动态注册** |
| `metagpt/tools/tool_convert.py:9-40` | schema 由 docstring 反射：`convert_code_to_tool_schema`(inspect) + `_ast`(AST) 产 `{type, description, methods}` |
| `metagpt/tools/libs/__init__.py:7-17` | eager import 全部工具模块触发装饰器登记；`Editor:84`/`Browser:32`/`git:15` 各带 tags |
| `metagpt/tools/tool_recommend.py:77-170,195` | 召回 + LLM 排序两级；`BM25ToolRecommender` |
| `metagpt/roles/di/role_zero.py:219-230` | 工具清单以 JSON 塞进 system prompt 的 `{available_commands}`。⚠ **全仓无 function-calling 的 `tools=` 用法**（grep 零命中），即源是纯文本命令协议，不是原生 tool-calling |
| `metagpt/utils/role_zero_utils.py:98-143` | `parse_commands`：JSON + repair + LLM 二次修复 + exclusive 命令截断（调用点 `role_zero.py:284`） |
| `metagpt/roles/di/role_zero.py:121-168` | `tool_execution_map` 手工映射 `Plan.*` / `Browser.*`(10 法) / `Editor.*`(14 法) / `SearchEnhancedQA` |
| `metagpt/roles/di/role_zero.py:385-413,420-447` | `_run_commands` 协程/同步双分支、**失败即 break**；特殊命令 `_run_special_command` |
| `metagpt/actions/run_code.py:83-128` | 代码执行两套：subprocess 与 `mode=text` 的 in-process `exec` |
| `metagpt/actions/di/execute_nb_code.py:69-107` | 真 Jupyter kernel（`make_sk_kernel`），跨单元状态保留 |
| `metagpt/roles/di/data_analyst.py:33-41` | 各角色按 tag 声明工具面，含 `Editor:write,read,similarity_search` 方法级裁剪语法 |
| `metagpt/skills/` | Semantic Kernel 目录形态（`WriterSkill`、`SummarizeSkill`） |

## 二、本项目怎么做

| 位置 | 性质 |
|---|---|
| `codeharness/tools/tool_registry.py:15-45` | **改造**：`BaseTool` + `by_tag`，`select` 的并集/未知 key 告警跳过语义照源；schema 反射判 `重`（LangChain 白拿） |
| `codeharness/tools/tool_registry.py`（实件 **71 行**）| ⚠ **过期引文已订正（2026-09-22 现证）**：本行原写 `:77-98,101-108` 的 `normalize_tool_args`（参数别名兜底）与 `args_spec`（喂 prompt）——**两件全仓零命中、且文件只有 71 行**，是从未存在的引用。真实情况：本仓没有「工具级参数 spec」这一层，参数形状只被 `ZeroThought` 的 structured 契约约束（`{command_name, args}`），单个工具的 args 内容不校验 |
| `codeharness/tools/__init__.py:10,26,36,50` + `libs/{editor_tools,git,terminal}.py` | 登记面 18 只；副作用 import 在 `tools/__init__.py:64-66` |
| `codeharness/roles/role_zero.py:289-297`（原引 `:207-213` 已漂，2026-09-22 现证）| **改造**：只有 description 进 `{available_commands}`（**不给 args spec**，见上一行订正），模板 `prompts/role_zero.py:36`；C6 起这一跳之前先过一层工具召回（`:289` 的 `select_for_prompt`）|
| `codeharness/roles/role_zero.py:30-33,230-235` | **新栈原生替换**：`ZeroThought` structured 输出取代 `parse_commands`，失败回退 `repair` |
| `codeharness/roles/role_zero.py:265-279` | `_act` 直调 `BaseTool.ainvoke` + 180s 超时 + self-heal；**失败继续而非 break** |
| `codeharness/roles/role_zero.py:71-87` | `PLAN_COMMANDS` / `_run_plan_command`（对齐台账 #10 的 Plan 状态机） |
| `codeharness/roles/registry.py:85` | profile 选工具：SweAgent 用 `select("terminal","file","edit","git")`；其余 RoleZero 用全量 `REGISTRY`（`:39,46,53`） |
| `codeharness/tools/_boundary.py:11-16` + `tools/sandbox.py:14,32,70,86` | **`新`（源无沙箱层）**：`is_relative_to` 边界 + `kill_tree` 进程树回收 + `run_proc` 统一超时 + `run_context` |
| `codeharness/runtime.py:23-36 session_root()` | **`新`**：文件工具、终端 cwd、沙箱 scratch、产物仓四处同一出口（按会话隔离） |
| `codeharness/tools/libs/{terminal,editor_tools}.py:173-186,18-30` | 按会话登记实例，回收在 `server/runner.py:268,274` |
| `codeharness/tools/search_engine.py:24,38,68` | **改造收敛**：源 7 个搜索引擎 → serper + ddg 一件；生产接线点是 `tools.search_internet`（进 RoleZero REGISTRY） |
| ~~`codeharness/actions/search_enhanced_qa.py`~~ **已删**（`70f0f8e` 批次2） | 与已接线的 `tools/search_internet` 同一能力两份代码，且本 action 从未进分发 → 保留工具、删 action。联网腿现在只有 `tools.search_internet` 一处（进 RoleZero REGISTRY） |
| `codeharness/actions/import_repo.py` | `ImportRepo` action：扫目录 → `DiGraphRepository` SPO + `.mmd`。**2026-09-21 复核：已不是死件**——端点 `server/api/workspace.py:61`、前端唯一入口 `frontend/src/components/DetailsPanel.vue:28,184`（`2791bc7` 批次29）、免审白名单 `tools/_approval.py:40`，门禁 `tests/s11_search_import.py` 5 组全打端点路径（含路径/跨会话/规模护栏）。**仍未做的**：不进 RoleZero 命令分发（只有 HTTP 入口），见 §四-A |
| `codeharness/ext_api/tools.py:12-24` | **`新`（N3 扩展点）**：追加同一 REGISTRY 引用 |
| `codeharness/skills/loader.py:13-29` | **改造**：SK 目录 → 轻量 skill 加载 |

## 三、逐能力对照

| 源能力 | 源位置 | 本项目位置 | 状态 | 依据 / 缺口 |
|---|---|---|---|---|
| 注册表 + 按 name/tag 选工具 | `tool_registry.py:27-69,131-163` | `tool_registry.py:15-45` | 已复刻 | 选择语义照搬；`t1/t15-t19` 钉名字与并集 |
| 反射生成 YAML schema | `tool_convert.py:9-40` | LangChain `args_schema` | 现代化替换 | 无用例（判 `重`，成立） |
| 方法级裁剪 `Class:m1,m2` | `tool_registry.py:131-163` | 无（整只工具为粒度） | 部分实现 | 源靠它把 Editor 裁到 3 法；本仓 11 个 `@tool` 天然细粒度，等价但形态不同 |
| 按路径动态注册 | `tool_registry.py:166-194` | `ext_api/tools.py:12` | 部分实现 | 只接受传入 `BaseTool` 实例，不能扫目录 |
| 工具清单进 prompt | `role_zero.py:219-230` | `role_zero.py:207-213` | 现代化替换 | 多给 args spec；仍是文本协议（与源一致，非缺陷） |
| LLM 调用解析 | `role_zero_utils.py:98-143` | `role_zero.py:30,230` structured + repair | 现代化替换 | 少一层 LLM 二次修复调用，换 `normalize_tool_args` |
| 执行分发与失败语义 | `role_zero.py:385-413` | `role_zero.py:265-279` | 现代化替换 | 源失败即 break，本仓继续跑下一命令 |
| Editor 编辑面 | `Editor.*` 14 法 | `editor_tools.py` 11 只 + read/write | 已复刻 | `similarity_search` 判弃 |
| 终端（保态 shell） | `libs/terminal.py:16,184` | `libs/terminal.py:37,191` | 已复刻 | 本仓多「按会话登记 + 关后可重登记」（`t33`） |
| 低层 shell | `libs/shell.py` | `utils/common.py:895`、`utils/tree.py:35` | 已复刻 | 内部件，不接工具面（合理） |
| Git PR/Issue | `libs/git.py:15,95` | `libs/git.py:26,39` | 现代化替换 | PyGithub → `gh` CLI（无 token 依赖） |
| Linter | `libs/linter.py:28` | `libs/linter.py:33` ← `editor.py:206` | 已复刻 | 自动 lint 钩子通（`t25-t28`）；tree-sitter 链判弃 |
| 代码执行（script） | `actions/run_code.py:92` | `sandbox.run_context:86` + `actions/run_code.py:13` | 已复刻 | — |
| `mode=text` in-process exec | `run_code.py:83-87` | 无 | 有意不做 | 沙箱纪律：只走子进程 |
| notebook / kernel 执行 | `execute_nb_code.py:69` | `actions/data_analysis.py:5` RunPythonCode | **部分实现** | 无 kernel 态、无跨轮变量（§结论-4）；本轮 B4 推迟 |
| 沙箱 + 会话工作目录隔离 | 源无（全局 cwd） | `_boundary.py` + `sandbox.py` + `session_root()` | **新栈增强（`新`）** | `t30/t32/t36` 实证八入口拒越界、超时杀进程树 |
| 搜索引擎 | `search_engine*.py` ×7 | `tools/search_engine.py` | 现代化替换 | 收敛为 serper+ddg，`t10-t11b` 零外网 canned 断言 |
| 联网问答 Action（源 SearchEnhancedQA） | `role_zero.py:121-168` tool_execution_map | 本仓同名 action **已删**（`70f0f8e`）；能力只由 `tools/search_internet` 承担 | **已收敛（判重后删）** | 曾「新 action 不在 RoleZero 命令分发 + 与工具重复」（§四-A）→ 按「保留工具、删 action」闭环 |
| 仓库导入（源 import_repo / extract_readme） | `actions/`（N2 示例件） | `actions/import_repo.py` + 端点 `server/api/workspace.py:61` + 前端 `DetailsPanel.vue:28,184`；`ExtractReadMe` 在 `ext_api/actions.py` | **已接线（有唯一调用者）** | `2791bc7`（批次29 接界面入口）；门禁 `tests/s11_search_import.py` 5 组走端点（200 / 路径越界 / `save_name` 越界 / 跨会话 / 规模护栏）。角色内命令分发仍无（只 HTTP 入口） |
| 浏览器交互 | `libs/browser.py:32` + 3 个 web_browser_engine | 无 | 有意不做 | 三条实测理由登记在 `docs/施工2-能力层-S4-S5.md` §S4（playwright 未装 + 缺 `utils/a11y_tree` + 全仓零 import） |
| TTS / 文生图 / moderation / translator / ut_writer / deployer / CR / index_repo | `tools/*`、`libs/*` | 无 | 有意不做 | 判 `弃`，理由在 `判定-复制与重构清单.md` §四 |
| LLM 推荐工具（召回+排序） | `tool_recommend.py:54-228` | `tools/tool_recall.py`（词法 ∪ bge-m3 dense 的 RRF 融合 + 可选精排）+ 接线 `role_zero.py:289`；**默认休眠**（`min_tools=30`，名册 18 只）| 🟡 机制与两腿都落地、**判据差一格**（真裁小 13/14，miss=`跑一下 pytest`→终端）| §结论-1 + PLAN §4 C6 行末 |
| skills 机制 | `metagpt/skills/`（SK） | `skills/loader.py:13-29` | 现代化替换 | 仅 `tests/test_p1.py:76` 一处 FakeLLM 冒烟 |

## 四、「看着像有、其实没接线」

`tools/libs/` 逐个文件核了消费者（Grep import 复核，非推断）：

| 文件 | 真实消费者 | 结论 |
|---|---|---|
| `editor.py` | `editor_tools.py:14`、`tests/s4_tools.py:28` | 有消费者；**但自身 `:80` 的 `@register_tool` 是 no-op**（§结论-3） |
| `editor_tools.py` | `tools/__init__.py:65`、`server/runner.py:274` | 已接线 |
| `terminal.py` | `tools/__init__.py:64`、`server/runner.py:268` | 已接线 |
| `git.py` | `tools/__init__.py:66` | 已接线（经 gh） |
| `linter.py` | `editor.py:206` ← `_shims.py:8` | 间接接线（自动 lint） |
| `shell.py` | `utils/common.py:895`、`utils/tree.py:35` | 内部件，非工具 |
| `_shims.py` | 仅 `editor.py:16` | 单点依赖 |
| `data_preprocess.py` | **生产代码零消费者**；只有 `tests/s6_sop.py:78` 与 prompt 文本（`prompts/di/write_analysis_code.py:101`） | **裸着**。⚠ 2026-09-21 C9：引用它的那段 prompt 文本本身也在零消费者资产里（§结论-2），两头都是死件 → 「包名是错的」今天打不到运行期，重开条件同 §结论-2 |

**A.【当轮记录，2026-09-21 已处置】** —— 本段是 B2 那一轮的发现，处置结果：**`SearchEnhancedQA` 已删（`70f0f8e`）、`ImportRepo` 已接端点 + 前端入口（`2791bc7`）**，两条"要么接上要么删"的账都已闭。以下正文保留作当轮证据（含当时 `grep` 的真实零引用读数），不要再据以判断现状。

【本轮新增】B2 的两个 Action 是「定义齐、无分发」：`grep -rn "SearchEnhancedQA|ImportRepo"` 在生产（`codeharness/`+`server/`）零引用。RoleZero 的联网能力走的是**已接线的 `tools/search_internet` 工具**（进 REGISTRY，`_act` 直调 `BaseTool.ainvoke`），本轮新增的 `actions/search_enhanced_qa.py SearchEnhancedQA` **不在任何命令分发表里**——`role_zero.py` 里那条 `SearchEnhancedQA` 只是 DI prompt 文本里的命令名（`prompts/di/*.py`），不是对本 action 类的引用。`ImportRepo` 同样无人装配（对照 5 §五 的「装配缺席」在工具面复现）。结果：`SearchEnhancedQA` 与 `search_internet` 是**同一能力的两份代码**，且新的那份没通电；`ImportRepo` 是孤立件。门禁 `s11_search_import.py` 直接手工 `action._call(...)` 测这两件本身能跑，**绕过了「谁在真实会话里会调到它们」这个致命问题**——绿不代表接上了。

另两处：

- `prompts/role_zero.py:5` 的文件头声称「QUICK_THINK 系列(:148-235) 原样保留」，实测该文件内**没有任何 `QUICK_THINK*` 常量**（只有 `ROLE_INSTRUCTION`/`SYSTEM_PROMPT`/`CMD_PROMPT`/`THOUGHT_GUIDANCE`/`DETECT_LANGUAGE_PROMPT`/`TASK_TYPE_DESC`/`REPORT_TO_HUMAN_PROMPT`/`JSON_REPAIR_PROMPT`）；源 `_quick_think` 快答通路未移植。**文档与代码漂移**，这行注释会误导下一个动 prompt 的人。
- `sandbox.py:27` 自带 `ponytail:` 注记：POSIX 分支未 `setpgid`，孙进程可能漏杀（Windows 分支已按进程树杀，`t32` 实证）。

## 五、门禁覆盖

`s4_tools.py`（40 组，真断言密集；2026-09-21 实测 `grep -c "^def t"` = 40）：注册表面 t1/t15–t19；边界 t2/t3/t36（八个入口拒越界）/t30；搜索 t10–t11b（canned HTML，零外网）；沙箱 t12–t14/t31–t34；Terminal t20–t24/t33；Linter + Editor 钩子 t25–t28；ext 对齐 t35；防回归 t38（全仓无 `from metagpt`）。

跨门禁：`s6_sop.py:716-760` t19 断「新注册的工具对 RoleZero 命令面即时可见 + 同名拒绝」；`s9_langfuse.py:212` t4 用真实翻车样例断 `normalize_tool_args` 的别名搬移与 1:1 兜底。`s11_search_import.py` 现为 5 组**端点级**用例（`2791bc7` 起改打 `POST /{sid}/workspace/import_repo`，不再手工 `action._call`）：t1 端到端 200、t2 路径越界、t3 `save_name` 穿越、t4 跨会话、t5 规模护栏。

零/弱断言：**`tool_recommend` 无任何测试（因为无实现）**；browser / deployer / skills-as-tool 无门禁；`SkillAction` 只有 `test_p1.py:76` 一处冒烟；`editor.py` 的死 `@register_tool` 未被任何断言捕获（t1 只比名字集合，不比装饰器是否生效）；**prompt 字符串里指向不存在的 `metagpt` 包**，`t38` 管不到（2026-09-21 C9：这句成立但**不是盲区**——那三件资产零消费者，运行期没有模型会收到它，详见 §结论-2 改判与重开条件）；**~~B2 两个 Action「生产零调用者」这件事没有门禁~~（2026-09-21 已闭：`ImportRepo` 的 `s11` 五组全经 `POST /{sid}/workspace/import_repo` 真实路径，「有没有人调」从此由门禁覆盖；`SearchEnhancedQA` 那半件随文件删除一并消解）**。

## 六、建议的收口顺序

1. ~~**B2 两 Action 要么接上要么删**~~ —— **已闭合（2026-09-21 复核）**：`SearchEnhancedQA` 走「删」那条腿（`70f0f8e`，保留已接线的 `search_internet` 工具）；`ImportRepo` 走「接上」那条腿（端点 `server/api/workspace.py:61` + 前端唯一入口 `DetailsPanel.vue:28,184`，`2791bc7`；`s11` 5 组全经端点，不再手工 `_call` 绕过「谁调它」）。第三态已不存在，别再按本条排队。
2. ~~改 `prompts/di/write_analysis_code.py:33,101` 的包名（并把「prompt 字符串里不得出现 `metagpt.`」并进 `t38`，一道 grep 就够：`grep -rn "metagpt\." codeharness/prompts`）~~ —— **作废（2026-09-21 C9 取证）**，三条理由：① 直改逐字资产 = 撞 s6 t1，批次2 已踩过、批次3 已回退，本仓不许；② 那条 grep 实测命中 **3 处**（`di/write_analysis_code.py:33,:101` + `generate_skill.md:12`），**全在 t38 明文豁免的逐字资产里** → 真并进 t38 会让 s4 当场红，且与 s6 t1 互斥（一个要求逐字相等、一个要求不得出现）；③ 通电的那条 DI 线用的是 `actions/data_analysis.py:7` 自写的 prompt，里面没有 metagpt，**没有可改的活路径**。本条改判为 §结论-2 末段的**重开条件**：谁把这三件资产接进生产，谁同批在消费处加法拼接校准常量。
3. 删 `editor.py:80` 的死装饰器，或把 `register_tool` 改成收到非 `BaseTool` 时**抛错**而不是 `_warn`——静默 no-op 正是这类残留能活到今天的原因。
4. 删 `prompts/role_zero.py:5` 里 QUICK_THINK 那句，或补上 `_quick_think` 通路（源 `role_zero.py` 的快答能省一次结构化调用，成本上有价值，但不紧急）。
5. ~~`tool_recommend`：真要扩工具面再做，做成 `REGISTRY.select` 之前的一层召回，别改注册表本身。判据也现成——`s4_tools.py:43-51` 的名册一旦超过 ~30 只就动工。~~ → **2026-09-22 已按这条形状做完**（`codeharness/tools/tool_recall.py`，注册表一字未动，接线在 `role_zero.py:289`）。两条现场读数要留给下一个接手人：① 那条「~30 只」线今天没过——名册 18 只、全量描述块实测 1455 字符，裁它省的不到 5% prompt，而召回漏一次要多烧一整发（¥0.12–0.27）⇒ `min_tools` 默认 30，**今天生产 prompt 逐字不变**；② 词法腿在中文任务上**有损**（14 条标注 query 覆盖 13/14，其中 1 格是靠「薄命中回全量」兜回来的，裁了还中 = 12/14；换成二元组切分仍 13/14，只是平均给到 12.4 只≈不裁）⇒ 欠的那半是**跨语言语义腿**（bge-m3 已在仓，`s5 t25` 已证语义腿要真服务在线）。**→ 2026-09-22 19:5x 语义腿已补**（`1a09f83`：粗筛改成词法 ∪ dense 的 RRF 融合、向量按名册签名缓存、离线自动退词法并留话），真裁小从 12/14 抬到 **13/14**；仍差的一格是 `'跑一下 pytest 看结果'`（`terminal_command` 描述写「执行命令」而任务只有「跑」与 `pytest`，bge-m3 把 `find_file` 排到前面）——**没靠改描述把这格抹平**，那叫教测试答题。判据 `tests/s4_tools.py` t39–t45（48 组），反向验证 3 轮记录在 PLAN §4 C6 行末。
6. notebook/kernel：如果 DI 线要继续做数据探索，`RunPythonCode` 需要一个 per-session 常驻 kernel（Terminal 已经有按会话保态的先例，照它做）。本轮 B4 已明确推迟。

## 复核方式

```bash
cd /e/Codeharness
grep -rn "SearchEnhancedQA\|ImportRepo" --include=*.py codeharness server   # 2026-09-21 复测：只剩 `actions/import_repo.py` 定义 + `server/api/workspace.py:61` 端点 + `tools/_approval.py:40` 白名单；`SearchEnhancedQA` 只出现在 prompt 文本里（类已删）
grep -rn "search_internet" --include=*.py codeharness/tools codeharness/roles  # 已接线的搜索工具在另一处
grep -rn "metagpt\." codeharness/prompts                       # §结论-2：包名 bug 仍在
grep -rn "bm25\|recommend" --include=*.py codeharness | grep -v __pycache__   # §结论-1
sed -n 78,95p codeharness/tools/tool_registry.py               # §结论-3 装饰器吞 kwargs
grep -n "QUICK_THINK" codeharness/prompts/role_zero.py         # §四 第二条（应为空）
PYTHONPATH=. PYTHONIOENCODING=utf-8 python tests/s4_tools.py
PYTHONPATH=. PYTHONIOENCODING=utf-8 python tests/s11_search_import.py   # §五：绿，但只测 action 本体不测接线
# 源侧锚点
sed -n '131,163p' /e/MetaGPT/metagpt/tools/tool_registry.py
sed -n '121,168p;219,231p;385,413p' /e/MetaGPT/metagpt/roles/di/role_zero.py
```
