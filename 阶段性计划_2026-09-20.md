# Codeharness 阶段性治理计划（审查发现 + 修复执行 · 唯一总文档）

- **文档性质**：本仓库代码质量治理的**唯一总文档**。前半是审查发现（现象/定位/切入点），后半接着是同一条目的修复指引（根因/改动/验收），一条目一闭环，可直接交接 Agent 逐条执行。
- **制定时间**：2026-09-20（审查快照基线 `2791bc7`，分支 `codeharness`；项目由其他 Agent 持续开发中，**行号以执行时现场代码为准**）
- **修复状态（2026-09-21 05:12 收口 C1）**：**22 项中 20 项已处理并各带实测读数**，剩 **F1、B13** 两项登记不修
  （F1 并入「图片域名白名单」一起做，B13 维持现状且 ceiling 注释在位）。其中 **F4 只补 docstring、不改行为**，
  F1/B13 属「核对并登记」。分四轮：阶段一 S1–S5（`f013a1c` `2ae84b7` `0e0a53a` `a9e58c5` `750a4ba`+`e1cdde7`）、
  阶段二 B1/B9/B2（`193b021` `6e06bcc` `fc9a569`）、阶段三 B3–B12 九条（`f8e92d0`…`faa5c5a`）、
  阶段四 F2/F3/F4 + F2 同类面（`0916763` `a90f875` `9d11f8b` `abcd75f`）。
  四道阶段门读数：`plan/governance-gate-stage1/2/3/4.md`。全量自测终态 = **29 条 28 绿 + 1 既有基线红**
  （唯一红 `tests/s3b_runtime.py::t14`，非本计划引入，交「阶段开发驱动器」那条线）。
- **全程纪律**：审查阶段只读完成，未影响开发；修复阶段按第 0 章纪律执行

---

# 第 0 章 执行总则（修复 Agent 先读）

## 0.1 开工前必做（快照已漂移，勿直接信行号）
1. `git status` + `git log -n 3` 确认当前基线；其他 Agent 正在开发，**本文行号可能已漂移**。每条开工前先按"文件 + 函数名"重新定位，再核对现象仍存在。
2. 跑基线测试拿"绿名单"：`pytest tests/ -q`（失败项记录备案，区分"本来就红"与"你改红的"）。

## 0.2 执行纪律
- 遵守仓库 `AGENTS.md`（ponytail）：最小 diff、根因修共享函数、不留没人要的抽象；每个非平凡修复留下**一个可跑的检查**（一条 pytest 或一组 curl），禁大框架。
- **一条一 commit**，message 带条目编号（如 `fix(S2): import_repo save_name 路径穿越`），出问题可单条回滚。
- 改动 `server/` 或 `codeharness/` 内核后，跑 `pytest tests/ -q` 再进下一条。
- 前后端联动的（S1/F2/F3）放同一 commit。
- 每完成一条，在本文对应条目末尾追加 `（已于 <日期> 修复，commit <hash>）`。

## 0.3 本文内的两处勘误/待验证（以此为准）
- **F4 降级**：`react` 范式 = 经典五角色队形（`codeharness/team.py` 的 `react_assembly` 只换 `react_mode`、不换编排），`/graph` 对 react 会话画 classic 表**行为正确**，不修代码，仅补注释（阶段四）。
- **B1 验证优先**：先跑一个 20 行 LangGraph 小实验确认"条件边路由函数写 state 是否持久化"，按结论分支：写入丢失→修；会持久化→不是 bug，只补注释。实验结论写进 commit message。

## 0.4 审查范围与边界（防追问）
- **已逐文件精读**：`server/`（14 py）、`platforms/`（7 py）、`codeharness/` 核心链路（runtime / team / roles(agent,role_zero) / environment(team_graph,checkpoint) / provider(gateway) / tools(sandbox, _approval, _boundary, libs: shell,terminal,editor_tools,git) / utils(common,redis) / actions(run_code,import_repo) / configs / schema / report）、`frontend/src` 主链路（api/client、stores(sessions,auth)、MarkdownText、render、mermaid、ComposerCard、ConversationRoot、VMenu、trajectory 等）、`docker-compose.yml`
- **粗查/模式扫描**：prompts/、logs.py、observability.py、exp_pool/、sop/、memory/longterm.py、其余 actions 与前端组件——未发现需登记的问题≠没有问题
- **排除**：`codeharness/vendor/`、`vendor/`、`workspace/`（Agent 运行产物）、`frontend/node_modules|dist`、`tests/`（未逐行）、`main.py`（PyCharm 模板死文件，登记备查）
- **严重度口径**：高 = 安全突破或数据损坏/越界执行；中 = 确定的功能缺陷或稳定性风险；低 = 已知债、体验或资源问题

---

# 第 1 章 总览：批次规划

| 阶段 | 内容 | 理由 |
|---|---|---|
| **阶段一 安全** | S1–S5 | 可利用性最高，独立可验，单独成批便于审计 |
| **阶段二 编排内核** | B1（先验证实验）→ B9 → B2 | 三条都动 `team_graph.py`/`runner.py` 的图语义，集中处理；B1 与 B9 都改 route 行为，**B1 必须先完成** |
| **阶段三 平台与工具** | B3→B4→B5→B6→B7→B8→B10→B11→B12 | 相互独立；shell 系（B4/B5/B6）连着做 |
| **阶段四 前端与收尾** | F2、F3、F4 注释、F1/B13 登记 | 低危与文档项 |

**依赖提醒**：
- S4 改变 approval_id 输入构成 → Redis 存量待批项（TTL 30 天）全部失配，表现为"老卡点了没反应、重弹一张"——可接受，commit message 写明。
- S1 有前端联动：Grep 前端 `'/workspace/` 全部引用点，确认都走 `workspaceUrl()` 或同样补 token。
- B2 的前置：先核 interrupt 后 `_run` 任务是否已从 `tasks` 清出，结果写进 commit message。

**统计**：22 项 = 安全 5 + 缺陷 11 + 前端/登记 6；高 8、中 9、低 5。

---

# 阶段一：安全类（S1–S5）

## S1. `/workspace` 静态挂载完全无鉴权（含 checkpoints.db 外泄）

- **严重度/置信度**：高 / 确定
- **定位**：`server/app.py` `create_app()` 内 `app.mount("/workspace", StaticFiles(...))`；断点库位置 `codeharness/environment/checkpoint.py` 的 `default_checkpoint_path()`（落在 `workspace_root/storage/`）
- **现象与影响**：挂载不经 `current_user`。WORKSPACE_ROOT 下有全部会话产物目录 + `storage/checkpoints.db`（完整黑板消息与角色记忆）。auth 开启时无需 token 即可下载任意会话文件与断点库，N1 可见性隔离在此失效；auth 关闭时同机任意进程可读。
- **排查切入点**：`GET /workspace/<project>/src/main.py`、`GET /workspace/storage/checkpoints.db`。
- **根因**：装配期裸挂 StaticFiles，无中间层校验；checkpoint 落盘位置与静态暴露面重叠。
- **修复方案对比**：
  - **A. HTTP 中间件守门（推荐）**：`/workspace/storage/` 前缀一律 403（断点库从暴露面彻底摘掉，不动落盘位置、零迁移）；auth 开启时其余 `/workspace/*` 要求 `access_token` query（复用 `server/auth.py` 的 `tokens.resolve`，与 SSE 同一条 ceiling 口径）。改动 ~20 行 + 前端 `workspaceUrl()` 拼 token。
  - B. 路由化（FileResponse + Depends）：最"正统"，但 `<img>` 带不了 header，前端全部静态引用要改 fetch+blobURL，改动面大 10 倍，且 img 场景还是躲不掉 query token。
  - C. 只挪 checkpoints.db + 中间件 auth：是 A 的子集，会话产物文件本身跨用户可见的问题仍在。
- **改动点（方案 A）**：
  1. `server/app.py`：中间件放 `create_app()` 内、mount 之前；`/workspace/storage/` → 403；auth 开时校验 query token。前缀匹配别写宽（不得影响 `/api/*`）。
  2. `frontend/src/stores/sessions.ts` `workspaceUrl()`：有 token 时追加 `?access_token=`（无条件带，少一个分支）。
  3. Grep 前端 `'/workspace/` 全部引用点，逐一确认带 token。
- **验收**：auth 开：无 token → 401；带 token → 200；`storage/checkpoints.db` → **永远 403（auth 关也是）**。auth 关：文件照常 200，storage 仍 403。前端图片预览（image 分支走 `/workspace` 静态 URL）在 auth 开时不破图。
- **风险**：token 进 uvicorn 访问日志（与 B13 已知债同源，注释登记）；若未来 nginx 对 `/workspace` 加独立规则，中间件会失效——compose 形态与单机 uvicorn 当前无此层。

（已于 2026-09-20 修复，commit `2ae84b7`。方案 A 中间件已落 `server/app.py`，注册在 CORS 之前使跨域头仍包住 401/403；判据按 `resolve()` 后的落点比较而非字符串前缀。验收实测（`tests/s12_workspace_guard.py` 3/3）：auth 开 无票/坏票→401、query 真票→200、Bearer 头等价→200；`storage/` 六种写法（原样、`./`、`..`、反斜杠、`STORAGE` 大写、`%73torage`）全 403 且不回 SQLite 文件头，**带真票访问 storage 仍 403**；auth 关 合法产物 200 且回真 PNG 字节、storage 照旧 403；`/api/health`、`/api/sessions` 不受牵连。前端 `workspaceUrl()` 已拼 token，全仓 `/workspace/` 引用只有它一处构造点（ChatNode/ToolCard/DetailsPanel 四处消费都走它）。门禁 s7 13/13、s8 8/8、`npm run build` grep error 为零。）

## S2. `import_repo` 端点的 `save_name` 路径穿越（越界读；写侧经核实不越界）

- **严重度/置信度**：高 / 确定（一条 curl 可验，建议本阶段第一条修）
- **定位**：`server/api/workspace.py` `import_repo` 端点（入口未校验）；`codeharness/actions/import_repo.py` `_call()` 内 `pathname=Path(self._artifact_store.root) / f"{save_name}.json"`
- **现象与影响**：`save_name` 取自请求 body 原样拼接。`../` 序列使 `.json` 落到会话目录外；`load_from` 对已存在目标会先读取解析（越界读，**已实测成立**：`save_name=../别的会话/repo` 会把那个会话的图节点载进来并写进本会话产物）。~~保存即越界写~~ —— 修复时探针证伪：`repo.save(path=ArtifactStore.root)` 显式传目录，`pathname = Path(path)/self.name` 把写落点钉回会话根，写侧不越界。同函数 `.mmd` 那条有 `ArtifactStore._checked` 防护，`.json` 这条没有——两条写路径防护不一致。
- **排查切入点**：`POST /api/sessions/{sid}/workspace/import_repo`，body `{"repo_path": "<workspace 内目录>", "save_name": "../../server/data/x"}`。
- **根因**：HTTP 入口缺校验；Action 内直接拼接。
- **改动点**：
  1. 端点 `repo_path` 校验之后加：`save_name = str(body.get("save_name", "repo")).strip()`；`if not save_name or Path(save_name).name != save_name: raise HTTPException(400, ...)`——口径对齐 `CreateSessionReq._single_dir_name`（`server/api/sessions.py`），不发明第二种写法。
  2. `ImportRepo._call` 开头加同一句防御（ext_api 示例件未来可能有第二个调用者；这是信任边界的输入校验）。
- **验收**：`save_name="../../server/data/x"` → 400 且服务端无新文件；`save_name="repo"` 正常。留下的检查：`tests/` 补一条 TestClient 端点测试。
- **风险**：无（带 `.json` 后缀的旧调用照常工作）。

（已于 2026-09-20 修复，commit `f013a1c`。端点 5 个越界名逐个 400、越界落点文件前后字节快照一致、合法名 `" repo "` 仍 200 落 `repo.json`；检查留在 `tests/s11_search_import.py::t3`。门禁 s7_platform 13/13、s8_frontend_contract 8/8 全绿。严重度按「越界读 + 跨会话图合并」计，原「越界写」不成立。）

## S3. 沙箱 `run_context` 的 `working_directory` 越界（LLM 可控）

- **严重度/置信度**：高 / 确定
- **定位**：`codeharness/tools/sandbox.py` `run_context()`（`workdir = Path(ctx.working_directory).resolve()` + `mkdir(parents=True)`）；上游 `codeharness/actions/run_code.py` `RunCodeContext(**(msg.instruct_content or {}))`（内容来自 LLM 产出的结构化输出）
- **现象与影响**：`working_directory` 无 session_root 包含性校验：`..`/绝对路径均可，目录不存在还会**越界新建**；`command` 为任意 argv。文件工具的会话边界（`_boundary.safe_session_path`）在执行面没有对应物。
- **排查切入点**：经典线 QA 环节——让 WriteTest 产出带越界 `working_directory` 的 RunCodeContext。
- **根因**：执行面缺边界判据；RunCodeContext 全字段信任 LLM 输出。
- **改动点**：`run_context()` 内 workdir 解析后加会话边界校验，越界退回会话根——**与 `runtime.py` `session_root()` 的越界退回默认桶同一语义**：
  ```python
  base = session_root()
  workdir = Path(ctx.working_directory).resolve() if ctx.working_directory else base
  if not workdir.is_relative_to(base.resolve()):
      workdir = base          # 界内执行，而不是拒绝整场
  workdir.mkdir(parents=True, exist_ok=True)
  ```
  顺带确认 `run_python_code`（落 scratch）不受影响。
- **验收**：单测构造 `working_directory` 为会话外路径 → 断言 cwd 落在会话根；合法子目录行为不变。
- **风险**：若某链路依赖"工作区外跑测试"（不应存在——会话边界是本仓设计承诺），从越界执行变为落到会话根，属预期收紧。

（已于 2026-09-20 修复，commit `0e0a53a`。修法用现成的共享判据而非在 sandbox 里重写一遍包含性规则：`safe_session_path(ctx.working_directory or "") or session_root()`。修复前探针（把旧两行原样跑出）：`working_directory` 指到会话根外 → 子进程 cwd 真的落在那儿、`is_relative_to(session_root)` 为 False、`mkdir` 在会话外长出了目录；同一入参修复后落点回到 `ws/<session>`。验收实测：越界四种写法（绝对/workspace_root 外/解释器目录/`../` 相对）断言 cwd 全落会话根且会话外不长目录，界内 `"tests"` 落 `会话根/tests`（顺带纠正了旧码「相对路径按进程 CWD=仓库根解析」的第二个越界面）；合法子目录行为不变（t13 原样绿）。检查留在 `tests/s4_tools.py::t13b`，s4 40 组全过、s7 13/13、s8 8/8。
  ⚠️ 本条收尾时看到的同族问题，**未在本条处理**：同一个 `RunCodeContext` 的 `additional_python_paths` 也只 `Path(p).resolve()` 就塞进子进程 `PYTHONPATH`——指到会话外的目录能 shadow 标准库/第三方包，是执行面第二处没边界的字段。）

## S4. Action 级审批的判定输入不含真实参数——批准粒度失真

- **严重度/置信度**：高 / 确定
- **定位**：`codeharness/roles/agent.py` `_approval_key()`（args 仅 `{cause_by, send_from, content[:500]}`）；`codeharness/tools/_approval.py` `approval_id()` / `preview()`
- **现象与影响**：同一触发消息下，不同命令/不同 working_directory 的 RunCode 算出**同一个 approval_id**：人工批一次，后续内容完全不同的动作重放即命中"已批"。审批卡 preview 只展示消息内容，人类看不到将执行的命令。工具面（kind="tool"）args 是真实参数，两条面粒度不对齐。
- **排查切入点**：readonly 档会话跑 QA（RunCode 属 `_ACTION_FULL` 必审批），连发两次内容不同的失败测试，观察第二张卡是否出现。
- **根因**：`_approval_key` 不取触发消息的 `instruct_content`；preview 不认识 instruct 键。
- **改动点**：
  1. `_approval_key` 的 args 加入 `instruct_content` 摘要（gate 与 act 两处都从同一个 trig 经 `_approval_key` 计算，单出口纪律不破）：
     ```python
     inst = getattr(trig, "instruct_content", None) or {}
     args = {"cause_by": ..., "send_from": ..., "content": (...)[:500],
             "instruct": json.dumps(inst, ensure_ascii=False, sort_keys=True, default=str)[:1000]}
     ```
  2. `preview()`：优先解析 `instruct` 反序列化后的 `command`/`working_directory`/`filename` 展示给人，吃不到再退回现有键顺序。
- **验收**：两次不同 command 的 RunCode → **两张**审批卡，各自真实命令可见；同参数重放不重复弹。
- **风险**：① 存量待批项失配（见第 1 章依赖提醒）；② digest 截断 1000 字符，CodingContext 类元数据远小于此，可控。

（已于 2026-09-21 修复，commit `a9e58c5`。`instruct` 放**整包 dict** 而不是本文片段写的 `json.dumps(...)[:1000]`——摘要只进 sha1，不心疼长度，而截断会把「差异在 cut 之后」的两笔又并成同一个 id，正是要修的洞。修复前探针（旧 args 形状原样复算）：`test_a` 与 `test_b`、cwd 还从 `repo/tests` 换成 `/etc` → **同一个 id** `73a12491d2e77d00`，preview 只有 `RunCode: 跑一下`；修复后两个不同 id，preview = `RunCode: python -m pytest test_a  (cwd=repo/tests)`（argv 列表 join 成一行，命令命中时顺带标 cwd）。检查留在 `tests/s8_frontend_contract.py::t8 ⑥b`：两笔不同 command 两个 id、卡上命令与 cwd 可见、批 A 不放行 B（`_act` 自己重算 id 必须与 gate 一致）；同参数重放不重复弹由原 ⑤/⑥ 钉住。门禁 s8 8/8、s7 13/13、s4 40 组全过。连带成本见 commit message：Redis 存量待批项 id 全失配，老卡点没反应、重弹一张。）

## S5. docker-compose 将无鉴权 Redis/Qdrant 直接暴露宿主机端口

- **严重度/置信度**：高 / 确定
- **定位**：`docker-compose.yml`（`ports: ["6379:6379"]`、`["6333:6333"]`）；`codeharness/configs/settings.py` `RedisConfig`（password 默认 None）
- **现象与影响**：Redis 无密码无 bind 限制；Qdrant 无 api_key。Redis 里是会话态哈希、事件流、审批台账（可伪造回执）、trace、配额；Qdrant 里是长期记忆向量与 payload。同网段任意主机可读写。
- **排查切入点**：`redis-cli -h <宿主IP>` 直接 `KEYS ch:*`。
- **改动点**：
  1. redis 服务：`command` 加 `--requirepass ${REDIS_PASSWORD:?need REDIS_PASSWORD}`；backend `environment` 加 `REDIS__PASSWORD=${REDIS_PASSWORD}`（Settings 的 env 嵌套分隔已支持，字段已存在）。
  2. qdrant 服务：`command: ["./qdrant", "--api-key", "${QDRANT__API_KEY:?need QDRANT__API_KEY}"]`；backend 加 `QDRANT__API_KEY=${QDRANT__API_KEY}`（接线处确认 `QdrantConfig.api_key` 真传给 client）。
  3. `.env.example`（无则建）写明两变量；**真值不入库**。
- **验收**：compose 起后：无密码 redis-cli → NOAUTH；无 key `curl :6333/collections` → 401；backend 正常起。
- **风险**：本机 Windows 日常开发用 `F:\Redis` 的 Windows 服务（无密码、非 compose 形态），本改动不影响；本地直连路径（密码为空）不受影响。

（**已于 2026-09-21 修复：代码与 compose 在 `750a4ba`，验收读数与门禁检查在 `e1cdde7`**。本文「字段已存在、env 嵌套已支持」的前提逐条核下来断了两处，而且断在共享出口上——只改 compose 的后果不是更安全，是平台起不来：① `RedisConfig.to_url()` 静默丢掉 username/password，而 `platforms/` 五件 + `server/runner` 跨 worker 控制通道 + `utils/redis` 全走这一个出口，现已在 URL 里带上凭据并 `quote(safe="")`（密码里能有 `@ : /`），无凭据时输出与改前逐字节相同；② `QdrantConfig.api_key` 全仓零读者，`AsyncQdrantClient` 构造处没传，已补 `api_key=settings.qdrant.api_key or None`。③ qdrant 侧改用它官方那套嵌套 env `QDRANT__SERVICE__API_KEY`，**没照本文片段写 `command: ["./qdrant","--api-key",…]`**：那会替掉镜像 entrypoint 的初始化逻辑，而该 CLI 形态本机无镜像可验，不敢按片段写。④ compose 的 `environment` 必须写成块式——flow 序列里的 `${}` 花括号是 YAML 保留字符，实测 `docker compose config` 报 "did not find expected ',' or ']'"(exit 15)，第一版就是这么踩的。
  已取到的实测读数（`tests/s13_redis_qdrant_auth.py` t1–t4 + `docker compose config`）：一次性 `redis-server --requirepass`（本机 F:\Redis 的进程，未碰 6379 那个共享服务）→ 匿名 ping 报 `AuthenticationError: Authentication required.`、`redis-cli keys *` 报 `ERR Protocol error: unauthenticated multibulk length`、带 `to_url()` 的同步与异步客户端读写通；抓头 HTTP 服务证明 qdrant 客户端确实把密钥打在 **`api-key`** 请求头（不是 Authorization），空值时不发头；缺 `REDIS_PASSWORD` 时 compose 解析期即报 "required variable REDIS_PASSWORD is missing a value: need REDIS_PASSWORD"。门禁 s7_platform 13/13（redis 双配置，证明无密码路径未破）+ s8 8/8。
  **两条读数已回补（2026-09-21 01:05，`tests/s13_redis_qdrant_auth.py` t5/t6 绿）**，用的不是容器：
  qdrant 官方 Windows 二进制 **v1.19.0**（与 compose pin 的 `qdrant/qdrant:v1.19.0` 同版本，服务端自报
  `{"version":"1.19.0","commit":"74f3e85b…"}`）一次性起在随机端口、storage 落 tmp，密钥就走镜像同一套
  `QDRANT__SERVICE__API_KEY`——服务端读数与容器形态无差。实测：无 key `GET /collections` →
  `401 Must provide an API key or an Authorization bearer token`；错 key → `401 Invalid API key or JWT`；
  匿名 `PUT /collections/probe` → 401；带 `api-key` 头 → 200。本仓 `QdrantStore.ensure()` 在这台真服务上
  真建成集合（dense+sparse+scalar int8+payload 索引这套请求体被接受，`points_count=0`），同地址不带 key
  的 `AsyncQdrantClient` 报 401。redis 侧一次性实例：匿名 ping → `AuthenticationError: Authentication required.`。
  backend 按 `docker compose config` 渲染出的 `QDRANT__API_KEY`/`REDIS__PASSWORD` 原文起（只把服务名
  `redis`/`qdrant:6333` 换成回环，宿主解析不到那两个名字）：`/api/health` 200，`app.state.store` 实现类
  = `RedisSessionStore` 并经该连接真读写一遍。
  ⚠ 顺手记下本文这条验收自己不合格：**「backend 正常起」证明不了凭据通了**——`server/app.py` 的 lifespan
  在 `use_redis` 开着但 ping 不通时**静默退回进程内 `SessionStore`**，服务照起、health 照 200。所以 t6 把
  判据钉在实现类上：带密码 = `RedisSessionStore`，只撤密码（= 修复前 `to_url()` 丢凭据的形状）= `SessionStore`
  （实测 `ping=False` 且 health 仍 200，正是那种「配错了但看起来没事」的形态）。
  **仍无读数的只剩全栈形态本身**：`docker compose up`（镜像构建、entrypoint、服务名网络解析）——本机引擎
  未运行（`//./pipe/dockerDesktopLinuxEngine` 不存在，装在 `E:\Docker`，WSL 默认发行版 `docker-desktop`），
  起引擎与拉镜像属越界，要用户点头。它不影响本条判据：compose 的插值已由 `docker compose config` 读到原文，
  凭据消费侧已由真服务 + 真进程读到。）

---

# 阶段二：编排内核（B1 → B9 → B2，顺序不可换）

## B1. 团队路由在条件边函数里写 state，`debug_rounds` 刹车可能从未生效

- **严重度/置信度**：高 / **高度可疑（先验证再动手）**
- **定位**：`codeharness/environment/team_graph.py` `make_route.<locals>.route()`（自环分支与广播分支两处 `state["debug_rounds"] = ... + 1`）
- **现象与影响**：LangGraph 条件边路由函数不是状态提交口（节点返回值才是），route 内赋值按该库语义不持久化——注释自述的"QA↔沙箱活循环"3 轮闸可能从未生效，实际只靠 `recursion_limit=60` 兜底；触发 recursion limit 时以 `GraphRecursionError` 把整个会话打成 failed。
- **排查切入点**：构造 QA 连续失败 ≥3 次的会话，观察是否第 3 次按预期 END，还是循环到 recursion limit。
- **验证实验（先做这个，20 行）**：`tests/` 写临时实验（验完转正为回归测试）：节点 A 返回 `{}`，条件边 route 内 `state["n"] = state.get("n",0)+1`，节点 B `return {"seen": state.get("n")}`，两轮 `ainvoke` 断言 seen。记录 langgraph 版本到 docstring。
  - `seen` 不累计 → B1 成立，按下述改；
  - `seen` 累计 → 不是 bug，`team_graph.py` 补注释，**且 B9 不得动 route 的计数逻辑**。
- **修复改动（若成立）**：写入口唯一化到 router 节点函数（节点返回值才是提交口）：
  ```python
  async def router(state: TeamState):
      last = state["messages"][-1] if state["messages"] else None
      if last and ((MESSAGE_ROUTE_TO_SELF in last.send_to and last.cause_by in
                    (RequirementTag.DEBUG_ERROR, RequirementTag.RUN_CODE))
                   or last.cause_by == RequirementTag.DEBUG_ERROR):
          return {"debug_rounds": state.get("debug_rounds", 0) + 1}
      return {}
  ```
  `route()` 删两处自增，保留 `>= 3 → END` 的**只读**判断（自环与广播都计数，语义与注释不变）；Send 载荷不动。
- **验收**：实验测试转正 + 既有 debug_rounds 用例全绿；手写 QA 三连败观察第 3 次 END。
- **风险**：把写入挪到节点侧后，即便未来 langgraph 改条件边语义，修复依然正确。
- **（已于 2026-09-21 01:55 修复，commit `193b021`）** 验证实验走的是**「不持久化 → 修」**这一支：
  langgraph 1.2.11 上同 thread 续跑 3 轮，条件边内写的 `seen` 序列 = `[0, 0, 0]`（对照组把写入挪到节点
  返回值 = `[1, 2, 3]`，证明不是载体本身坏）。真图行为级读数：修复前 QA `<self>` 自环在
  `recursion_limit=12` 下抛 `GraphRecursionError`（刹车恒不生效，只靠 recursion_limit 兜底），
  修复后 `debug_rounds == 3` 且 QA 恰被激活 2 次（第 3 次进 router 即 END）。实验已转正为
  `tests/s16_route_state.py`（t1 语义 + t2/t3 真图两分支刹车）。
  ⚠ 落地时发现的边界，不改本条口径但登记备查：`debug_rounds` **不区分成功与失败**——
  `RunCode` 成功时也产出 `cause_by=RunCode` + `send_to=<self>`（`run_code.py:103-108`），
  故一次会话内累计到 3 条 RUN_CODE/DEBUG_ERROR 消息即 END（计数器只在 `team.py:42/61` 的 init 里重置，
  即「每轮 run 3 次」上限而非「连续失败 3 次」）。修复前这道闸从未生效过，所以该口径从未被检验。

## B9. Action 拒绝/异常的自愈消息没有订阅者——直接散会

- **严重度/置信度**：高 / 确定
- **定位**：`codeharness/roles/agent.py` `_act()`（拒绝分支 `verdict != "allowed"`、异常分支 `except Exception`）；路由侧 `codeharness/environment/team_graph.py`（SOP 表 + `<all>` 不广播设计）
- **现象与影响**：两类分支产出的 Message `cause_by=action.name`（不在 SOP 订阅表）、`send_to` 默认 `<all>`（按设计不广播）→ 无订阅者 → route 返回 END → 会话以 "run completed" 提前收口。注释承诺的"回喂自愈下一轮"实际不存在；**人类在审批里拒绝一个动作 = 整场静默结束**。
- **排查切入点**：真模型经典线，让 WriteTasks 产出越界 filename 触发拒写；或在审批卡点"拒绝"。
- **根因**：自愈消息用了"广播/订阅"寻址，但两个寻址面都不命中。
- **改动点**：两处消息改 `send_to={MESSAGE_ROUTE_TO_SELF}`（`sent_from` 已是角色名，`route()` 的 `<self>` 分支会 Send 回同角色再激活一轮；BY_ORDER 下 `_act` 的 memory 回退路径恰好读到错误文本，真自愈成立）。**必须在 B1 之后改**（`<self>` 分支计数刚动过）。
- **验收**：mock Action 抛错，断言角色节点被 Send 第二次（graph 级测试）；真模型观察角色被再次唤醒而非"run completed"。
- **风险与边界**：① 自环继续 `loops+1`，`max_loops` 封顶，不死循环；② **REACT 模式（QA）下错误文本进不了 `_think` 决策 prompt**（inbox 被 watch 过滤），自愈可见度有限——更深的设计缝隙，**本批不扩 scope**，在 `_act` 注释登记一句。
- **（已于 2026-09-21 02:12 修复，commit `6e06bcc` + 登记注释 `9f6421f`）** 两处自愈消息改
  `send_to={MESSAGE_ROUTE_TO_SELF}`，真图读数（载体 `log/b9_probe.py`，真 Agent + 必抛错 Action）：
  修复前角色只激活 **1 次**即散会（末条 `send_to={'<all>'}`、`debug_rounds=0`）。
  ⚠ **本条风险①「`max_loops` 封顶」经实测证否**——`Agent.as_node` 每次激活都用
  `{"loops": 0, "action_cursor": -1}` 重建内层状态（`agent.py:236`），内层闸只管单次激活；
  只改 `send_to` 的读数是「Boom 执行 **12 次** → `GraphRecursionError`」（生产 limit=60 即约 30 次
  模型调用后把会话打成 failed，比原 bug 更贵）。故 `<self>` 计数不再挑 cause_by 白名单
  （`team_graph` 的 router 写入点与 route 只读刹车同步收敛），最终读数：**3 次、`debug_rounds=3`、正常收尾**。
  检查转正为 `tests/s16_route_state.py::t4/t5`（抛错回喂 / 审批拒绝回喂，两条都断言
  「角色被再激活 + 3 轮内收尾」）。风险②按原口径登记进 `_act` 注释，机制实测：
  `_observe` 后 inbox 剩 0 条 → REACT 的 `_think` 确实读不到错误文本。

## B2. `runner.answer_human` 覆盖同 sid 运行任务引用

- **严重度/置信度**：高 / 确定
- **定位**：`server/runner.py` `answer_human()`（无条件 `self.tasks[sid] = create_task(_resume(...))`）；对照 `_run`/`_resume` 的 finally `tasks.pop`
- **现象与影响**：会话 running（未 interrupt）时 API 层不拦 human-input；`_run` 与 `_resume` 共用 `tasks[sid]` 槽位，先结束方 pop 掉另一方引用：`is_running()` 误报 False、`stop()` 取消错对象、跨 worker 停止失配。
- **排查切入点**：会话 running 期间 `POST /api/sessions/{sid}/human-input`。
- **前置核对（1 分钟）**：确认 interrupt 后 `_run` 任务已从 `tasks` 清出。若 interrupt 后仍有活任务残留，本条改成"按状态分流"而非简单守卫——**先核再改**，结果写进 commit message。
- **改动点**：
  ```python
  def answer_human(self, sid, content):
      if not self.store.get(sid):
          return False
      if self.is_running(sid):
          return False    # 有活任务（_run 或进行中的 _resume），不抢槽
      self.tasks[sid] = asyncio.create_task(self._resume(sid, content))
      return True
  ```
  `server/api/sessions.py` human-input 端点把 `ok=False` 翻 409；前端 `answerHuman` 对 false 弹 toast。
- **验收**：running 时 POST human-input → 409 且 tasks 未被覆盖；awaiting_human 正常 resume；连点两次只起一个任务。
- **（已于 2026-09-21 02:38 修复，commit `fc9a569`）** 前置核对的实测结论（载体
  `tests/s17_human_input_guard.py::t1`，真 `SessionRunner._run` + 假图产 `on_interrupt`）：
  interrupt 收尾后 `is_running=False`、`sid` 已不在 `runner.tasks` → **守卫用活任务视角就够，
  不必按状态分流**。但同一次读数里取到一条本条不修的现形：`awaiting_human`（`runner.py:434`）
  随后被 `_run` 收尾那句无条件 `status=finished`（`:356`，`_resume` 同一句在 `:231`）覆写掉，
  所以 `store.status` 在「停在断点」时不可信，而 `sessions.py:150/210` 的
  「running/awaiting_human 必有人在跑」前提正建在这个字段上——登记待判。
  修复落在共享出口 `answer_human`（两个调用者 `sessions.py:251` / `approvals.py:46` 一次修好）：
  有活任务返回 False；human-input 把 False 翻 409，审批端点维持 `{"ok": …}`（回执已落账，
  409 会把生效的回执显示成失败，那是 §F3 的地盘）。
  ⚠ 顺带一条前端事实：`stores/sessions.ts:403` 把整个响应体当布尔用，旧口径下 200 + `{"ok": false}`
  是恒真 → 问答卡照样被清掉，所以计划里「前端对 false 弹 toast」这半边由 409 + `QuestionCard`
  现有 catch 兑现，前端零改动。
  读数：修复前 running 时 `answer_human` 返回 True 且 `tasks[sid]` 引用被换掉；修复后
  running→False 且不换槽、空闲→200 且真起 resume、连点两次只起一个任务（s17 3/3）。

---

# 阶段三：平台与工具（B3 → B4 → B5 → B6 → B7 → B8 → B10 → B11 → B12）

## B3. `RedisSessionStore.update(persist=True)` 实际是全量重写，注释与实现相反

- **严重度/置信度**：中 / 确定
- **定位**：`platforms/session_store.py` `update()`（注释称"只写传进来的字段"，实现 `_dump(s)` 全量 dump 后 HSET 整哈希）
- **现象与影响**：读-改-写整哈希，两会话字段并发更新互相覆盖，与文件头"字段级更新天然并发安全"的宣称矛盾（进程内 JSON 版同是全量重写，但至少单进程串行）。cost 快照（`set_cost` 高频）与状态更新并发时可能互退。
- **排查切入点**：Redis 模式，运行中会话并发 `PATCH /permission` 与 LLM 流上的 `_sync_cost`。
- **改动点**：persist 分支只 HSET 传入 fields。抽 `_dump_fields(fields) -> dict`（`_dump` 内部也调它，两处不重复）；`cost`/`roles`/`llm_override` 必须走 `_JSON_FIELDS` 的 json.dumps 分支；内存对象照旧全量重建（返回语义不变）。改完文件头的宣称变成真话。
- **验收**：两线程对同 sid 各 update 不同字段，互读不丢。留下的检查：一条 threading 并发测试。
  （已于 2026-09-21 02:57 修复，commit `f8e92d0`：抽出 `_dump_fields()` 供 `_dump`/`update` 共用，persist 分支只 HSET 传入字段；
  检查 `tests/s18_session_store_fields.py`（一次性真 redis）t1/t2 全绿，对照模式 `S18_OLD_UPDATE=1` 把 update 换回全量重写
  → 实测 `status` 被 cost 那条线程的旧快照覆回 `created`，即丢失更新的现形；门禁 s7_platform 13/13、s8_frontend_contract 8/8。）

## B4. `shell_execute` 是 async 壳包阻塞 `subprocess.run`

- **严重度/置信度**：中 / 确定
- **定位**：`codeharness/tools/libs/shell.py` `shell_execute()`（`subprocess.run(..., timeout=600)` 在事件循环线程）；调用方 `codeharness/utils/tree.py`（tree 命令）、`codeharness/utils/common.py` `get_mime_type`
- **现象与影响**：阻塞调用跑在事件循环线程，一次最长冻结整个服务 600 秒（所有 SSE、所有会话）。Windows 无 `file`/`tree` 命令：`common.py` 侧有 try 兜底，`tree.py` 侧未见 `FileNotFoundError` 兜底。
- **改动点**：`subprocess.run(...)` 包 `asyncio.to_thread(...)`（签名与返回值不变）。**不做**与 `sandbox.run_proc` 的合并重构（返回值形态不同，合并动所有调用方，收益不配）。`tree.py` 补 try 降级文案，与 `tools/libs/git.py` `_gh` 同款口径。
- **验收**：`shell_execute("sleep 5")` 期间 health 请求不卡；Windows 走 tree 路径返回降级文案不抛。
- **风险**：timeout 由 subprocess.run 内部实现，语义不变；ponytail 注释登记线程池占用。
  （已于 2026-09-21 03:10 修复，commit `1cffcc8`：to_thread + `_execute_tree` 补 FileNotFoundError 降级文案。
  检查 `tests/s19_shell_family.py::t1/t2`：t1 拿「修复前形状」取对照读数 ticks=0，改后 2s 内跳 34 次，
  并钉 timeout 仍抛 `TimeoutExpired`；t2 本机不补丁就走降级分支——Windows 自带 tree 是 .com，shell=False 解析不到，
  修复前实测直接 `FileNotFoundError: [WinError 2]` 出环。）

## B5. `get_mime_type` 单引号注入面

- **严重度/置信度**：中 / 确定（POSIX 可达；Windows 因 `file` 不存在先触发 FileNotFoundError）
- **定位**：`codeharness/utils/common.py` `get_mime_type()`——`shell_execute(f"file --mime-type '{str(filename)}'")`，shell=True + f-string 拼单引号
- **现象与影响**：filename 含 `'` 即突破引号注入任意 shell 命令；filename 来自文档/仓库路径，可被 LLM 产出影响；叠加 B4 的阻塞执行。
- **改动点**：改 list 形态 `await shell_execute(["file", "--mime-type", str(filename)])`（list 分支 shell=False，注入面闭合）。
- **验收**：文件名含 `' && echo pwn` 只被当作一个 argv；功能输出不变。
  （已于 2026-09-21 03:10 修复，commit `e055c9b`。检查 `tests/s19_shell_family.py::t3`：对照组把修复前那套
  模板打 shell 内建 `echo`，payload 真被执行成 stdout 独立一行；改后 argv 恰为 `["file","--mime-type",<payload>]`。
  ⚠ 本机 PATH 上有 Git 带的 GNU `file`，它把文件名原样打回 stdout——判「有没有执行」不能只 grep stdout 找标记，
  本轮实测就这么假红过一次。**总文档「Windows 因 file 不存在先触发 FileNotFoundError」这半句在本机不成立**：
  file.exe 存在且可达，注入面在 Windows 上同样可达，只是 `'` 不是 cmd 的引号、闭合方式不同。）

## B6. `repo_parser.rebuild_class_views` 三处问题

- **严重度/置信度**：中 / 确定
- **定位**：`codeharness/repo_parser.py` `rebuild_class_views()`（`f"pyreverse {str(path)} -o dot"` + shell=True + check=True + 阻塞 subprocess.run）
- **现象与影响**：① path 带空格/元字符即注入；② `check=True` 使紧随的 `if result.returncode != 0: raise ValueError` 成死代码（非零先抛 CalledProcessError）；③ async 函数内阻塞执行（同 B4）。
- **改动点**：argv list 化；删 `check=True`，保留显式判断（错误信息更可读）：`subprocess.run(argv, cwd=...)` 后 `if result.returncode != 0: raise ValueError(f"pyreverse 失败: {result.stderr[:500]}")`；`asyncio.to_thread` 包裹。
- **验收**：成功路径照常产出 dot；失败路径抛 ValueError 且含 stderr；含空格路径不炸。
  （已于 2026-09-21 03:10 修复，commit `7c27707`。检查 `tests/s19_shell_family.py::t4`：真 pyreverse 在
  `<tmp>/pkg with space/sub pkg` 上跑出 Board/Game；替换 subprocess 的缝注入 rc=3 → ValueError 含
  `pyreverse: bogus interpreter`；对照组用修复前的 `f"pyreverse {path} -o dot"` + shell=True + check=True
  打同一个含空格路径 → 实测 rc=1、classes.dot 不产出、抛的是 CalledProcessError，一次同时证伪
  「拆词无害」与「那句 returncode 判断有用」两条。⚠ 行为差一条：pyreverse 不在场时从旧的 rc=127/
  CalledProcessError 变成 `FileNotFoundError`（list + shell=False 没有 shell 可报错），调用方
  `actions/rebuild_class_view.py` 不捕这个类型——已登记 handoff 悬案，本条不扩 scope。）

## B7. `import_repo` 扫描无界 + 目录边界只到 workspace_root（跨会话可见）

- **严重度/置信度**：中 / 确定
- **定位**：`server/api/workspace.py` `import_repo`（越界校验 `is_relative_to(ws_root)`）；`codeharness/actions/import_repo.py`（`rglob("*")` 全量进内存 set）
- **现象与影响**：`repo_path` 可以是 workspace 下**其他会话**的目录：auth 开时用户 A 可扫用户 B 的目录结构并生成关系图（N1 可见性隔离的洞）。rglob 无深度/数量上限，含 node_modules 的巨型目录使节点集合无界增长。
- **方案对比**：
  - **A. auth 开时收紧到本会话 workspace（推荐）**：端点把 repo_path 边界改判 `Path(s.workspace)`；auth 关维持 ws_root。与 `api/sessions.py` 既有 auth 分支风格一致；auth 关时导入公共模板目录的合法用法不破。
  - B. 一律收紧到本会话：简单，但 auth 关时导入公共目录会 400。
- **连带（无论选哪案）**：rglob 加规模护栏——计数到上限（如 20000 节点）截断，返回里塞 `"truncated": true`，ponytail 注释登记 ceiling，上限提为模块常量。
- **验收**：auth 开跨会话目录 → 400；巨型目录返回截断标记且不卡；正常目录行为不变。
  （已于 2026-09-21 03:20 修复，commit `ac08d5f`，按方案 A。检查 `tests/s11_search_import.py::t4/t5`：
  t4 带对照断言——同一个别人会话目录在旧口径 `is_relative_to(ws_root)` 下**是放行的**，所以那条 400
  只能来自新边界；本会话目录 200、auth 关同一请求回到 200（旧口径不破）。t5 把上限压到 5 →
  `truncated=true`、node_count=6；恢复上限 → `truncated=false` 且节点更多。两条都跑在新增的
  `_isolated()`（会话表指 tmp + use_redis=False）里，否则自建的会话会真写进本机共享 redis。）

## B8. `LLMGateway.ainvoke` 流式分支无超时

- **严重度/置信度**：中 / 确定
- **定位**：`codeharness/provider/gateway.py` `ainvoke()` 的 `stream=True` 分支（`async for chunk in model.astream(msgs)` 未套 wait_for；非流式走 `_acall` 有 deadline）
- **现象与影响**：LLM 连接挂死时该分支永久等待；外层 RoleZero 的 180s 工具超时管不到模型调用本身。单会话冻结，只能靠人工 stop。
- **改动点**：astream 收集包成内部协程套 `asyncio.wait_for(coro, deadline)`，超时抛 `asyncio.TimeoutError`（`_retryable` 已认它）。**不做**流式半包重试（重放会让前端重复上屏，注释已有同句纪律）。
- **验收**：mock 慢流（每块 sleep 10s），`cfg.stream=True` → 按 deadline 超时失败而非挂死；正常流不受影响。
  （已于 2026-09-21 03:26 修复，commit `c85f009`：收集过程包成内部协程走 `wait_for(_collect(), deadline)`，
  deadline 与非流式同一个；`timeout=0` 仍不走 wait_for；**不做半包重试**。检查
  `tests/s2_gateway.py::t15`（零联网桩流）：① 对照=同一条 hang 流在测试侧 0.5s wait_for 下也收不完；
  ② 修复后 deadline=1s 实测 1.0s 抛 `asyncio.TimeoutError`；③ deadline 内正常流拼接与单点记账不变。）

## B10. 工作区文件读取无大小上限

- **严重度/置信度**：中 / 确定
- **定位**：`server/api/workspace.py` `file()`——`target.read_text(...)` 全量进内存整体返回
- **现象与影响**：无上限无分页；大文件一次 GET 即撑爆 worker 内存并长时间占用。
- **改动点**：`read_text` 前 `target.stat().st_size > 5 * 1024 * 1024` 则 413（带大小提示）。前端 `fileContent` 调用侧对 413 展示"文件过大"即可，**不追加下载功能**（YAGNI）。
- **验收**：>5MB → 413；≤5MB 照常。
  （已于 2026-09-21 03:29 修复，commit `5075adb`：`MAX_PREVIEW_BYTES=5MiB`，stat 超限即 413、**不读**；
  不追加下载功能。检查 `tests/s12_workspace_guard.py::t4`：上限内 200 带 content、恰好等于上限仍 200
  （判据 >，不是 >=）；超限 413——临时替换 `pathlib.Path.read_text` 的探针记录数为 0，证明 413 之前
  一次读都没发生；5MiB+ 的 .png 仍 200（图片分支只回元数据、不读字节，不受上限牵连）。）

## B11. `TraceStore` 的 ZSET 只加不裁

- **严重度/置信度**：低 / 确定
- **定位**：`platforms/trace.py` `record()`（每 LLM 调用追加，无 TTL 无裁剪）
- **现象与影响**：Redis 模式长跑内存单调增长；`/trace` 端点随 span 数变慢。
- **改动点**：zadd 后 `zremrangebyrank(key, 0, -5001)`；`_KEEP = 5000` 提为模块常量（与事件流 `MAX_EVENTS_PER_SESSION` 同量级口径）。
- **验收**：循环 record 6000 条后 `ZCARD == 5000` 且最新条在。
  （已于 2026-09-21 03:31 修复，commit `4849e32`：`MAX_SPANS=5000`，record 里 zadd + `ZREMRANGEBYRANK (0,-(MAX+1))`
  同一条 pipeline。检查扩进 `tests/s7_platform.py::t7`（真 redis db15）：记 6000 条 → ZCARD==5000、
  末条 i=005999、首条 i=001000（最老 1000 条确实淘汰）。span 的 i **必须补零**：score 是 time.time()，
  紧循环里同刻的多笔按 member 字典序排，不补零时 "999" > "1000"，断言会随本机时钟粒度翻脸。）

## B12. 账号体系：注册竞态丢用户 + 登录无限流

- **严重度/置信度**：中 / 确定
- **定位**：`server/auth.py` `register()`（读-改-写整文件无锁）；`login_route`（无 quota）
- **现象与影响**：并发 register 两用户名，后写覆盖先写丢账号；login 可无限尝试（auth_enabled 定位是"生产多租户显式开启"）。
- **改动点**：① 模块级 `threading.Lock` 包住 register 的读-改-写整段（verify 只读不加）；② `_Tokens` 旁加进程内失败计数 `{username: [次数, 窗口起始]}`，5 次/60s 拒绝并提示；注释登记 ceiling（多 worker 不共享，沿用 auth.py 既有坦白格式）。
- **验收**：两线程并发 register 不同用户名 → users.json 两者都在；6 次错密 → 第 6 次起被拒。
  （已于 2026-09-21 03:36 修复，commit `faa5c5a`：`_USERS_LOCK` 只包 load→判重→save（pbkdf2 留锁外）；
  `_login_fails{username:[次数,窗口起始]}` + 5 次/60s → 429 带剩余秒数、成功登录清零、过 4096 项清过期窗口。
  检查另起 `tests/s20_auth_race_ratelimit.py`（不并进 s10_auth.py：那份在工作树里是别人未提交的改动）：
  t1 用「替换 `_save_users` 加 150ms 延时」撑开窗口 → 修复后 race_a/race_b 都在；**对照组用修复前本体
  （无锁、hash 在 load 之后算）跑同一延时 → 实测只剩 ['race_b']**，race_a 无声消失。
  t2：前 5 次 401、第 6 次 429、被限期间真密码也拒、窗口过期恢复、成功登录清零后再计一轮仍生效。
  ⚠ 顺带取到一条没修的：`_save_users` 的临时文件名是写死的 `users.tmp`，两笔并发 save 在 Windows 上
  `replace` 直接撞 WinError 32（对照组的另一种现形）。register 现在是 users.json 唯一写入方且在锁内，
  所以不破；**若别处再开一个 users.json 写口就会复现**——已登记交接悬案。）

---

# 阶段四：前端与收尾登记

## F2. `client.ts` 的 fetch 无超时/无重试

- **严重度/置信度**：低 / 确定
- **定位**：`frontend/src/api/client.ts` `req()`——无 AbortController/deadline，服务端挂起时前端永久 pending，`busy` 占住按钮。
- **改动点**：fetch 选项加 `signal: AbortSignal.timeout(15000)`。
- **验收**：DevTools 降速后写操作 15s 出错误提示，按钮不卡死。
- **已于 2026-09-21 04:26 修复**（主仓 `0916763`）：`req()` 的 fetch 选项加
  `signal: AbortSignal.timeout(REQUEST_TIMEOUT_MS)`（15s），并把 `AbortSignal.timeout` 抛的
  `TimeoutError` 翻成「`POST <url>` 超时（15s 无响应，后端可能卡住）」——调用方（ApprovalCard /
  QuestionCard / LoginPage…）都是直接把 `e.message` 塞进 toast，不翻就是英文 DOMException 文案。
  **取值依据**：所有端点都在请求内同步返回（`runner.start` 只登记任务、`stop` 只发 cancel），
  最重的 `import_repo` 实测 12287 文件 0.71s（`log/probe_import_timing.py`）→ 15s 不会误伤长操作。
  **验收读数**：真 Chrome + CDP 降速 1 B/s（`log/cdp_stage4.mjs`），点「允许一次」→ 2ms 卡片乐观出队
  → **15009ms** 快照回滚 + 红色 toast → 17624ms toast 收走，末态按钮 `disabled=false`；
  截图 `log/stage4_V1_throttle.png`。门禁 `tests/s8_frontend_contract.py::t9`（10/10）。
  ⚠ 环境坑：内置浏览器面板 `visibilityState=hidden` 时 Chrome 会节流渲染器定时器，同一格实测成
  34.6s 才到点——**计时类前端判据不能在隐藏面板上量**。

## F3. `respondApproval` 失败回滚的竞态

- **严重度/置信度**：低 / 确定
- **定位**：`frontend/src/stores/sessions.ts` `respondApproval()`——catch 恢复旧数组快照，若回滚前 SSE `resolved` 已出队（另一标签页批了），恢复会把已决议卡片复活。
- **改动点**：catch 分支改 `void this.loadApprovals(this.currentId)` 重新对齐服务端真值。
- **验收**：断网点拒绝 → 卡回队列；双标签页同刻批一张卡 → 不复活。
- **已于 2026-09-21 04:26 修复**（主仓 `a90f875`）：catch 里**两句都留**、顺序固定——
  先 `this.approvals = keep` 再 `void this.loadApprovals(this.currentId)`。
  ⚠ **本条原改法（只换成 `loadApprovals`）单独照做会让验收第一条失效**：`loadApprovals` 的失败是
  静默吞掉的（它设计上不该把首屏拖挂），断网时重取同样失败 → 卡片既不回来也不报错。所以快照回滚
  必须留在前面，重取只是把「别处已决议」这条纠正掉；顺序反过来则错误提示要再等一个 timeout 才弹。
  **三格实测**（真 Chrome + CDP `log/cdp_stage4.mjs`，故障注入 `log/probe_stage4_ui.py`）：
  ① 降速 1 B/s（断网格）：15009ms 卡回队列 + toast，末态刚点的那条在座、按钮可点；
  ② 服务端已决议而响应被吞：15003ms 回滚 → **15013ms 重取落地** → 屏上换成下一条，已决议那条不复现；
  ③ 对照（把重取那句注掉）：32s 后屏上仍是刚决议的那条且按钮可点——复活形态成立。
  门禁 `tests/s8_frontend_contract.py::t10`（10/10）。

## F4. `/graph` 端点补注释（非代码改动）

- **严重度/置信度**：低 / 确定
- **定位**：`server/api/sessions.py` `session_graph()`
- **改动点**：docstring 补一句——react 范式 = 经典队形（只换执行策略），此处画 classic 表是**刻意**行为；sop/dynamic 分支 `_make_llm()` 不带会话 override 是合理的（纯画图不调用模型），防后人误改。
- **已完成（2026-09-21 04:31，主仓 `9d11f8b`，仅 docstring）**。两条口径都先取到代码依据再写：
  ① `codeharness/team.py:74-81` `react_assembly(llm)` = `classic_team(llm)` 后逐个
  `r.react_mode = "REACT"`，路由表不变 → `runner.py:160-162` 的 react 线与 classic 同队形，
  `session_graph` 走 classic 兜底是**对的**；② `_make_llm()`（`team.py:84-93`）只
  `LLMGateway(cfg=…, cost_manager=…)`，`gateway._build` 到 `ChatOpenAI(**kwargs)` 为止
  （`api_key=SecretStr(cfg.api_key or "EMPTY")`），构造期不发请求 → 画图不传会话 override 合理。
  门禁：`s7_platform` 13/13、`s8_frontend_contract` 10/10（t4 正是打在这条端点的三态上）。

## F1 / B13. 登记不修

- **F1**（Markdown 外链图片可作跟踪像素；`MarkdownText.vue` v-html + `render.ts` `html:false` + mermaid `securityLevel:'strict'`，基本面可控）：本文追加"暂不处理，后续做图片域名白名单时一并考虑"。
  - **已按此登记（2026-09-21 04:33，不改代码）**。三条前提本轮实地核过，判据成立：
    `MarkdownText.vue:2` 确实 `v-html="html"`；`utils/render.ts:5` 是
    `new MarkdownIt({ html: false, linkify: true, breaks: false })`——**`html:false` 只挡原始 HTML，
    挡不住 markdown 图片语法**，所以 `![x](https://<host>/t.png?sid=…)` 仍会被浏览器真取，
    构成出站跟踪像素 + 访客 IP 外泄；`utils/mermaid.ts:16` `securityLevel:'strict'` 到位。
    全仓没有图片域名白名单（`render.ts` 无任何 link/img 规则改写）。
    **为什么不单独修**：白名单要连 `<a>` 的 rel/target、`/workspace` 直读口（见 B13 同族）一起定口径，
    单给 `<img>` 打补丁会留下第二条旁路。留到做图片域名白名单时一并处理。
- **B13**（SSE token 走 URL query，进访问日志）：维持现状，`server/auth.py` 注释已有 ceiling 记录。
  - **已核对在位（2026-09-21 04:33，不改代码）**：`server/auth.py:147-148` `current_user` docstring
    原文——「ceiling：EventSource 发不了 header，`/events` 允许 `access_token` 查询参数等价
    （token 进 URL 有进日志的风险，仅 SSE 消费；换 cookie 是升级路径）」。query 侧共两个消费点：
    `stores/sessions.ts:193`（SSE `/events`）与 `:445`（S1 加的 `/workspace` 图片直读，`<img>` 同理发不了
    header）。升级路径仍是 cookie；本机 uvicorn 形态没有独立访问日志层，风险面在加 nginx 之前不扩大。

---

# 第 2 章 回归清单（每阶段跑）

1. `pytest tests/ -q` 全绿（或 diff 与基线红名单一致）。
2. 阶段一追加手工冒烟（curl 清单见各条"验收"）。
3. 阶段二完成后追加一次真模型最小会话：classic 范式 1 轮 + dynamic 范式 1 轮 + 一次审批拒绝，确认编排行为无回归。
   - **C1 复跑（2026-09-21 05:10，本机 ollama 不花钱）**：dynamic `finished`（prompt 37013，与阶段二
     32898 同量级）与 reject `finished`（0 token，与阶段二读数同形：拒卡后 PM 仍走到 WritePRD、
     `error=''`）两条绿；
     classic 头两次异常——**192.3s `failed` `GraphRecursionError: Recursion limit of 60`（prompt 30334、
     事件 5000）**与 348.8s `finished`（prompt 64686、15 产物）。
     对照跑在阶段二基线 `fc9a569`（detached worktree，同载体同 prompt）= **72.1s / prompt 6446 / 13 产物**，
     与阶段二当时读数（68.1s / 6448）逐字同形 → 差距不在代码，在**运行现场**：
     载体 `log/gate2_real_min.py` 把项目名写死成 `gate2_classic`，HEAD 复跑继承了阶段二留下的旧产物目录，
     `PrepareDocuments`/`SummarizeCode` 面对的是「已经有 13 个文件的项目」→ prompt 与决策全变。
     把旧目录挪开再跑 HEAD = **68.1s / prompt 6444 / completion 2485 / 13 产物 / `error=''`** →
     **清单 3 在 HEAD 判绿**（三次读数见 `log/gate4_real_*.txt`、`log/gate4_control_classic_fc9a569.txt`）。
     ⚠ 记一条坑：这条载体**不幂等**，复跑前必须换项目名或清 `workspace/gate2_*`（已进队列铁律 16）。
4. 全部完成后更新本文档顶部状态行：`修复状态：22 项中 N 项已修，剩 <编号> 登记`。

# 第 3 章 不做的事（防 scope 蔓延）

- 不重写 `shell.py`/`sandbox.py` 的进程模型（不合并 run_proc/shell_execute）；
- 不给 REACT 模式补错误上下文链路（B9 边界登记即可）；
- 不动 checkpointer 落盘位置（S1 用中间件挡 storage/ 即可）；
- 不做 `/workspace` 路由化大改造（S1 方案 A 已够）；
- 不引入 Redis Lua、不升级 langgraph、不加新依赖。
