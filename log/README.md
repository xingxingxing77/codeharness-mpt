# Codeharness 开发日志目录（`log/`）

约定照 `E:\java-agent\mall-dev\logs\` 的口径搬过来，逐条适配本仓。每次阶段开发都要遵守。

> ⚠ 别和 `logs/` 搞混：`logs/` 是 loguru 的**运行时输出**（`20260920.txt` 那种），而且
> `logs/sitecustomize.py` 是门禁跑法的依赖项——跑任何 `tests/s*.py` 都必须带
> `PYTHONPATH=/e/Codeharness:/e/Codeharness/logs`，少了这一段会撞上本机 WMI 永久卡死。
> **`logs/` 一个字都不要动、不要改名、不要合并进来。** 本目录只放人写的开发记录。

## 五条硬约定

1. **文件名**：`YYYY-MM-DD-<主题>.md`，一个阶段性任务一份。时间用 24 小时制带时区（本机 +08:00）。
   时刻一律用 `date` 取真实值；推断出来的写「约」。**（2026-09-21 C9 轮的实测教训：上一轮 handoff/队列
   里的时刻没按 `date` 取、超前真钟 15–25 分钟，直接把下一棒的窗口预算算错一半——「只剩 45 分钟」其实是
   113 分钟。交接里的每个时刻都要现取，别沿用记忆或推算。）**
2. **每条记录五段**：**现象 → 排查过程（含实际命令与输出证据）→ 根因 → 解决方案 → 解决后效果**，
   末尾标相关 commit 号与门禁读数。
3. 必须区分三种结论：**已修并实测** / **已修未实测**（写清为什么验不了）/ **不是代码问题**
   （环境或工具造成的假异常）。不允许把「看起来好了」写成「已解决」。
   本仓的具体形态：`FakeLLM` 门禁绿 ≠ 真模型行为对（README §陷阱 #2 已经应验三次），
   每条「效果」都要写明断言打在**回放**上还是打在**构造参数**上。
4. **自己引入的问题照实记**：笔误、凭印象写不存在的 API、误归因、把逻辑推断当成实测读数——
   都要写，不粉饰。这一节是本目录对下一轮会话最有用的部分。
5. 与 `log/error/` 的分工：本目录记一次开发的**全过程**（含已解决的）；
   `log/error/` 只记**卡住没解决**的，见该目录 README。

## 索引

| 文件 | 覆盖范围 |
|---|---|
| `2026-09-21-C1回报通路成员产出回队长.md` | C1-②b（委派链路的回程）。**为什么必须指名回报**：源 MGXEnv 靠成员把话发 `<all>`、队长自己看见，而本仓「`<all>` 不当广播」是已入库决定（`对照6 §结论-3` + `s3b t6`）→ 不指名就是结构性丢失（21:4x 的 t2 读数 `activated:0` 那条就是证据）。动手前先清掉 21:4x 记下的两处拦路形状：`as_node` 只要 `task != "continue"` 就 `self.plan = None`（队长收到回报会把自己的计划抹掉，也就无法 `finish_current_task`），以及出口只读 `history[-1]["results"]`（模型「先汇报再收尾」时汇报蒸发）。判别用消息标记 `instruct_schema="TeamReport"` 而不是 `cause_by`（委派载荷与正常产出同为 `RunCommand`，源同款）。刹车另起 `team_rounds`（12 档 = 6 个来回，够源 TL 四步 SOP）而不是复用 `debug_rounds`——后者只认 `<self>`/`DEBUG_ERROR`；计数写在 `router` 节点、判定在 `route`（条件边写 state 会被丢，`s16 t1`）。**两条判据都不是空转**：t6 反向验证（临时倒回"只读最后一轮"→ 当场红在 content='收工'）、t7 阳性对照（同剧本把 `recursion_limit` 压到 12 必须真抛 `GraphRecursionError`）。**施工里查出新账两条**：C13 同一超步多条产出只有最后一条能被投递（探针复现：15 条并发 Send → Alice 被激活 15 次、14 条回报卡死，`llm.calls=229`）、C14 `TeamState.round` 是死键（C2 的 `docs` 同族，没顺手删是因为要动 3 处 init + 8 处测试构造）。读数：s22 5→**7 组**、按 §0.3 全量重跑 **18 条 exit 0 且组数与改动前一致**。**未验**：全程 FakeLLM，真队长一轮攒几条委派、会不会真触发 C13，只有 A4 那批知道 |
| `2026-09-21-C1委派载体publish_team_message.md` | C1 第二片：队长的委派真能把成员唤醒。**先推翻自己上一片写下的前提**：原判「让 `ActionNode.assignee` 成真 Send 目标」是错的——源里 `assignee` 同样零路由消费者（`grep -rn assignee E:\MetaGPT\metagpt` 只命中 schema/write_plan/experience_retriever），真载体是源 `team_leader.py:81-91 publish_team_message`，而本仓 prompt 早在 `prompts/di/team_leader.py:12` 就叫模型用它、命令面却零实现（调到就是「未知命令」），`team_info` 还恒为空串（模型连队友叫什么都不知道）。落地三处：命令 → `self._outbox` → `as_node` 收口发一条聚合消息（`cause_by=RunCommand`，**必须排返回值最后一条**，因为 `route()` 只读 `messages[-1]`）→ `CONTEXT_WIRING[RUN_COMMAND]` 拆成一人一条 → `route()` **只对装配器产出的载荷**按 `send_to ∩ targets` 定向（原始消息不收窄，经典线 `run_code.py:104` 那条行为不变；只收窄不新增，所以 `UnknownRecipient` 绕不过去）。档位上把「模型写错名字」与「代码写错路由表」分开：前者 `[已拒绝]` 回喂自愈（B9 同路）、后者照 21:0x 当场抛。**为什么不直接 `return Command(goto=[Send(...)])`**：那会绕开 `route()`，把 21:0x 那道闸、`stats` 每轮激活数、插话通道全丢掉。顺带删掉零读者的死类属性 `RoleZero.SPECIAL`（留着就是一份会分叉的假表）。门禁新增 `tests/s22_delegate_route.py` 5/5（真图 stats `[{UserRequirement,1},{RunCommand,1},{RunCommand,0}]`、Bob `llm.calls==0`、一次点名两名不互串、含阳性对照），按 §0.3 面全量重跑 17 条 exit 0。**两条实测出来的既有形状**（②b 要用，本片没动）：`as_node` 的对外 content 只读**最后一轮** `results`，所以中途 `reply_to_human` 会蒸发在 thought 里；`task != "continue"` 会把角色自己的 `plan` 清掉，回报若按新任务进队长=队长的计划自我抹除。**留账**：全程 FakeLLM 真模型未验（本轮零花费）、成员→队长回报通路不存在（散会即终点）、`/api/sessions/{sid}/graph` 画不出委派边（它渲染 SOP 表，委派是消息级） |
| `2026-09-21-C1委派地基未知收件人不再静默.md` | C1 的先决地基片（用户 19:5x 定的四条档位之一：未知收件人**当场抛**）。**四类静默**各有后果：SOP 表指向不存在的节点 → LangGraph 只打 `Ignoring unknown node name PM` 然后**整场零 LLM 调用**（会话"正常结束"零产物）；具名 `send_to` 指错人 → 委派悄悄蒸发；`<self>` 回喂目标不在册 → 落 END 等于 B9 白做；插话指错人 → 用户追问凭空消失。根因是 `route()` 把"目标存不存在"当**过滤器**用（本意只是别给 LangGraph 发未知节点），顺手把"指错人"也吞了。**两条红我先怀疑自己再查证据**：`s3b::t4` 钉的就是旧静默行为（改判据不改代码）；`s7::t13` 变 failed 我去读了投递形状，发现该用例故意 `enqueue("…","Ghost")` 证明跨 worker 消费——我把 C1-d 一刀切到插话路径，等于**为一根错名字赔上整场已烧的用量**。两类代价不对称，摆给你定了档：委派抛、插话丢 + 留痕。顺带修 `build_team` 的 `sop or SOP`（显式空表被 falsy 吞成默认表，写隔离用例时正好撞上）。门禁：s16 新增 t6 五格（①②③必抛 + ④合法指名照常送达的阳性对照 + ⑤`<all>` 既不抛也不广播）、s3b 新增 t3b（丢 + `stats` 有 `chat-dropped` + 队列消费干净）并改判 t4 → s3b **17 组**、s16 **6 组**、s7 13/13、s8 14/14、s21 3/3。**照实记一条没做到**：我给选项时写的是「往事件流发 error」，实现是 loguru WARNING + `stats` 记账，没进事件流——route 手上没有 bus，为此要加第四个 ContextVar 或给 ChatQueue 加 drop 回调；真要界面可见，接缝该在 ChatQueue（投递时就看得见目标）。**C1 未完的两件**：`ActionNode.assignee` 今天只活在计划展示文本里（`role_zero.py:174`，全仓无一处当路由输入）→ 要先造委派载体；再是 planner 现场产 profile + 工具名校验 + 新角色写进 `session.roles`（动 Session 字段先看 `_JSON_FIELDS`） |
| `2026-09-21-C11时间旅行回放三件套.md` | 阶段 C 的 C11（用户 18:15 拍板「三件套全上」）。**先取证再设计，取证否掉了一件活**：`AsyncSqliteSaver` 每个 superstep 本来就落一份 checkpoint（两节点小图 4 场 ainvoke = 12 份、`step` 单调、新→旧、换个 compiled graph 实例读同 thread 仍是 12 份 ⇒ 端点可走 `_ensure_graph` 重建路），所以「按 superstep 采快照」**零新代码**。真数据层只读打开仓库现成的 `checkpoints.db`：35 thread / **3513 份 / 32.4 MB**，最单条 711 份、单份最大 62 KB ⇒ 形状必须摘要/详情分离。一条被签名拦下来的错：我第一版把 `checkpoint_id` 塞进 config 当 `before` 用——那是**闭区间**（含它自己，翻页会重复吐边界条），`aget_state_history` 的 `before=` 关键字才是开区间（B2 同族形状）。另量了延迟会不会撞 F2 那条 15s deadline：711 份的 thread 上 limit=3/80/200 = 0.068/0.141/0.306s，首请求 7.4s 是一次性建图预热。落地：`runner._ckpt_page()`（页内不携带 values）+ 两条只读路由（`MAX_STATE_BYTES=4MiB` 超限 413、重建失败列表带 `reason`、详情 409）+ 右栏第 7 个页签「回放」（点行展开当时黑板、`加载更早` 胶囊、**换会话清游标的 watch**）。客户端参数名刻意写 `checkpoint_id` 而不是 `cid`，因为 s8 t3 按「模板段名 ≡ FastAPI 占位符名」比形状。门禁：新增 `s21_checkpoint_replay` 3 组（含 413 正向+恢复对照、**只读性那组专门防端点路径里藏写**、`finally` 显式 `close_all()` 免得被 aiosqlite 线程吊住）、`s8` 加 t14 两侧同判 → 14/14、`s7` 13/13 双配置、build 零 error、tsc 只剩一条既有红。**留账四条**：413 无活体读数（真历史最大 83 KB 压不到上限）、换会话那格只读到行数归零没读到 dim 文案、`metadata.writes` 实测为空（只 `parents/source/step`）界面靠 `tasks` 兜底而没去挖为什么、超步与事件流未对齐（点超步跳不到对话流，那是第四件新面） **段 6 是用户纠正后的参照系复核**（口径：`E:\deepseek-harness` 是唯一前端参照系，参照系没有的界面也要按它的风格设计）：我第一版四段 CSS 照本仓 token 手拼（自造 999px 胶囊 + 选中底用错 token），三版演进全留着。终版逐项对参照系源值——表格 `ui-trajectory/src/client/TrajectoryTable.module.css` 的 `.table th/td`（30px 行、`0 8px`、行线 border-l1、表头 sticky + sidebar-fill）、选中用 `interactive-bg-active`（hover 才是 -hover）、表格内加载开关是**整行按钮** `.historyLoadButton`（29px 无框）而**不是**对话流那颗 `.older`（14px 圆角）、展开块复用右栏代码块（pad16/r12/13-22）；参照系 UI 层没有 superstep/checkpoint 词，界面只说「步」。复核后活体 getComputedStyle：td 30px/8px/rgba(0,0,0,.04)/12-18、th sticky/500/rgba(0,0,0,.1)/rgb(249,250,251)、选中 rgba(38,49,72,.1)、.cpMore 29px/radius 0、.stateBox 13-22/r12/pad16。s8 t14 因此加了 6 条钉源值的样式判据（另记：写判据又踩两次锚点——`split(".earlier")` 打中自己的注释、`split("</template>")` 被嵌套 `</template>` 截断）。新留账一条：本仓 `.trace` 表（trace 页签在用）与参照系表格不同族，没顺手改 |
| `2026-09-21-C12跨币种分桶.md` | 阶段 C 的 C12 跨币种半（用户 18:15 拍板四条之一）。**根因是数据结构里根本没有币种这个维度**：`Costs` 只有一个无单位 float，`TOKEN_COSTS` 同表混人民币行与美元行，`+=` 之后报出来的数既不是美元也不是人民币，而展示层只能猜一个符号（界面一律 `¥`、用量页标签写着「人民币价目」、日志 `:41` 硬写 `$`，两边正好相反）。**一条比账面大的发现**：混币种不止 `qwen3.8-flash` 一行——行内注释自带 ￥ 的有 6 行，厂商官方就是人民币报价的还有 deepseek 直连三行 / doubao 六行 / yi 三行 / glm-4-flash+plus，落地 `CNY_MODELS` = **21 行 / 102 行**（这是本轮唯一需要用户拍板的点，选「连厂商行一起标」）；防呆：`deepseek/deepseek-chat` 这种带斜杠的是 OpenRouter 美元价，**不进名单**。修法：`Costs`/`CostManager` 拆 `cost_usd`+`cost_cny`（**没有合计字段**，多一个「合计」就是混加回潮）+ `currency_of()`（未登记模型回 `""`，**不冒充 USD**）+ `records` 逐笔带 `cc` + `runner.cost_snapshot()` 成快照唯一出口 + span 改两桶增量 + `_seeded_ledger` **不回读**老记录里的 `total_cost`；前端收成 `utils/money.ts` 一处符号（四个消费处都走它，否则 `$`/`¥` 又漂回一处），布局不动。门禁 t8 五格含**对照组**（`cny_models` 清空后 step-3.5-flash 必须落 USD 桶 → 证明按名单走而不是写死两个名字），s8_runner_meter 8/8、全套 s1..s8 + build 绿、tsc 只剩一条既有红。**留账**：真模型混币种的一场会话还没跑过（要在 A4 那批取）、`doubao-*` 单价量级可疑但没有原件不敢改（分桶只解决混加不修单价，写进文件头）、缓存命中档 ¥0.14 今天不可达、老记录的 `total_cost` 键留着变哑数据没做迁移 |
| `2026-09-21-F项compose容器形态读数.md` | 阶段 F 第 6 项，**零代码改动、纯取证**。补 S5 收官时唯一没读到的那半：「在 compose/镜像这个交付形态里，`${REDIS_PASSWORD:?…}` 那两行声明是否真落到容器 argv 并真挡住匿名客户端」——`docker compose config` 只证明插值算对，不证明 redis 收下了密码。范围由用户点定（只验 redis：不拉新镜像、不碰本机 6379、不起 backend 构建），Docker Desktop 按用户一句「帮我启动」由我拉起。读数四步全真：`docker inspect .Args` 里 `--requirepass` 在位 → 匿名 `ping` 回 **NOAUTH** → 带密码 `PONG` → `set/get` 通；同时 `netstat` 证明共享 6379（PID 11952 的 F:\Redis 服务）全程没停没改（宿主端口用**临时 override** 挪到 16379，不动仓库那份 compose）。收摊核对：`down -v` 掉 fixe5 的两个卷与网络、override 删掉、16379 零 LISTENING、`docker ps -a`/`volume ls` 计数 0。**两条"不是代码问题"**：① 「引擎可用」这种读数只在取的那一刻成立（16:5x 读到在跑、17:5x 就不在了，我为此在 PLAN 撤回过一句话）；② `grep -c ":16379"` 会数到 TIME_WAIT，判端口关没关要 `grep LISTENING`。仍欠的两条写清：qdrant 缺 v1.19.0 tag（语义侧早已用同版本官方二进制验过 401/200，欠的只是"compose 里那台容器起得来"）、backend/frontend 镜像构建未验 |
| `2026-09-21-E项413与truncated走到界面.md` | 阶段 F 第 5 项（治理 §3 第 2 条：阶段三只落了服务端）。**根因是信号在最后一米断了**：`client.ts::req()` 抛的错误只有 message 一个通道，**状态码在出口被压扁** → 413 只能退化成通用报错且预览面板什么都不留；`truncated` 则全仓零消费者，撞上限的导入与完整导入长得一模一样（绿色成功条），图不完整没人知道。修法：`err.status = rsp.status`（不让调用方去猜文案——那是把契约写在文案上）+ DetailsPanel 按 `=== 413` 分流（预览里落后端原文 + warn 语气，**阈值不在前端重抄**）+ 导入按 `truncated` 换语气与文案。判据 s8 新增 **t13 两侧同判**（后端仍回信号→Error 挂 status→前端按码分流+面板留话→没抄第二份数字，少一边就红），S8 13/13；顺手把 s8 收官打印从硬编码 `12/12` 改成 `len(checks)`。**两层读数**：HTTP 侧真隔离实例 8721+db15（413 detail 原文 / 小文件对照组 200 / 20001 文件目录 `truncated:true` 1.9s / 3 文件目录不误报），界面侧真浏览器读 DOM（`.pvCode` 里的 413 文案、`item warn` vs `item success` 三种语气）——面板 0×0 视口只挡可信 click 与几何判据，文本判据不受影响，用 `evaluate_script` 派发。**登记不下手的一条**：小目录导入回「4 节点/20003 边」，边数是从上一笔 `repo.json` 合并带来的（`save_name` 默认同一个 `"repo"`）→ 「这次导入了什么」与「图里现在有什么」在这两个数上是混的，属 B7/导入语义设计问题。收尾：vite 与 8721 两个进程按 PID 身份认过再杀、端口复测零监听、2 万个探针文件与临时 vite 配置已删；**db15 留了 5 条会话记录没 FLUSHDB**（另一条会话可能也在用 db15） |
| `2026-09-21-D项users_json写盘互踩.md` | 阶段 F 第 4 项。**登记时只说了 rename 撞 WinError 32，实测有三种失效形态，最糟的不是异常而是静默数据损坏**：`write_text` 内部是 open('w')→write→close，两笔同名 scratch 各自 truncate、各自从 0 覆写 → 短的那笔盖不掉长内容的尾巴 → **users.json 变半新半旧的坏 JSON，`_load_users` 从此必炸**。第二条也是当天就能发生而不是"以后再开写口才复现"：`verify()` 读表不持锁，与注册写盘同时撞 → 读者 `PermissionError`。修法：写侧一次性 scratch 名 + rename 四档退避（最后一档不吞异常，scratch 留在盘上=可查的失败而不是静默成功）；读侧 170ms 内重试且**不返回空表**（空表="所有人都不存在"，比 500 更坏）。判据 s20 新增 t1b 四格（①确定性的手工交错前提格 ②4 写者+2 读者生产并发 ③拦 `token_hex` 数名字唯一性 ④**修复前本体对照组**，实测 WinError 32×18），连跑 3 次不抖；s7 13/13 双配置、s8 12/12、s10_auth 6/6（那份文件在别人在途里，只跑不改）。**记一条我自己的错判**：先用 2 线程 + barrier 当对照组，实测两笔都成功 → 那条判据是空的；换 4 线程×8 轮才稳定复现。反证类判据得先量出对照组的稳定失效率 |
| `2026-09-21-C项pyreverse缺位不再裸抛.md` | 阶段 F 第 3 项（B6 自己留下的行为差）。**根因**：B6 从 `shell=True` 换 argv list 之后，"可执行文件不在场"改由 `Popen` 本地抛 `FileNotFoundError`，那句 `if result.returncode != 0` 永远看不到它；调用方 `actions/rebuild_class_view.py` 零 `except`，图内会撞上 `agent.py:201` 的泛捕获 → **把"没装 pylint"当成动作失败重跑三轮自愈**。**修法在共享出口**（`repo_parser.py` 只咬 `FileNotFoundError` → 归成本函数既有的 `ValueError` 口径；不在调用方补 except：那文件在别人在途的 29 件里，且 ext_api 还有直连调用者）。**判据 s19 新增 t5 三格**：①前提格用真 `shell=False` 打不存在的命令必须先证明抛的是 FileNotFoundError（否则整条判据演空戏）；②注入缝 → ValueError 含「pyreverse 不在 PATH」；③不放宽格：同缝注入 `PermissionError` 必须原样出环。读数：s19 5/5、s4 41 组、s7 13/13 双配置。**顺带修一处账面**：s19 收官打印把编号硬写成「t1–t4」而组数用 `len(fns)`，加格后自相矛盾。未验：没真把 pylint 摘出 PATH 跑一次完整动作（本机 pyreverse 在场，这条分支在本机从未走到） |
| `2026-09-21-B项additional_python_paths补边界.md` | 阶段 F 第 2 项（S3 收尾时自登记的同族洞，两轮没人动）。`additional_python_paths` 与 `working_directory` 是执行面上挨着的两个 LLM 字段，原先只 `Path(p).resolve()` 就进子进程 `PYTHONPATH` → **外部目录能 shadow 标准库**。**修法不能照抄 S3**：`PYTHONPATH` 没有"退回会话根"这种合理替代（凭空多一条 import 路径比不加更糟），故越界**丢弃**、全越界退回继承环境；落点选共享出口 `_env_with_paths()`（生产侧 `actions/run_code.py` 在别人在途的 29 件里）。取证：先 `grep` 证明这字段除 LLM 外**零生产者**（收紧零误伤）。读数：s4 **41 组** exit 0（新增 t34b 三格，含"越界夹在合法值里"那一格；t34 当阳性对照），**反向验证 CAUGHT**（把判据换回修复前那行当场红）；s7 13/13 双配置、s8 12/12。另把 `docs/施工2:41` 那句只写"进得去"没写"不进得去"的口径补准——这正是这条洞能在文档里活下来的原因 |
| `2026-09-21-A项断点态不再被收尾抹掉.md` | 阶段 F 第 1 项（治理 §3 余账开账）。**现象**：会话停在 interrupt 后 `store.status=finished`，前端靠 SSE 事件本地补一刀才没露馅，刷新即变「已完成」而待批卡还在。**根因**：`_run`/`_resume` 两条 teardown 各写一遍无条件 finished，`_forget` docstring 自己声明的「awaiting_human 必须留着 graph」被它自己的 `terminal=True` 违反；连带三处建在这个字段上的前提失效（`start_session` 放行重开冲掉断点、`stop()` 转发给没有任务的 worker 把会话钉死 `stopping`、`heal_running` 永远命中不到）。**修法**：共用出口 `_settle()` 判状态 + `stop()` 对断点态就地落 stopped + `/start` 两条 409 分开 + 契约加「awaiting_human 是驻留态」段。**判据**：s17 从 3 组扩到 4 组，t1 那句「如实记录不假设」升级成断言，t4 带**修复前原样对照组**（monkeypatch 回那三行必须读出 finished，否则第一条没有区分力）。读数：s17 4/4、s7 13/13 双配置、s8 12/12、s3b 16 组。**自己踩到**：`str(SessionStatus.x)` 在本机是 `'SessionStatus.awaiting_human'` 不是 `'awaiting_human'`——判据要字符串得用 `.value`。未验边界：断言打在假图产 `on_interrupt` 上（非真 langgraph interrupt 端到端）、多 worker 收紧只有单进程读数、活体留证缺口 |
| `2026-09-21-B1断线横幅与轮内error行.md` | B1 三格里两格 ✅、一格证伪后改档。**横幅**：源值逐条取自参照系 `ConnectionBanner.module.css`，第一版我从 `evtSource.readyState` 派生 → **实测后端活着时横幅就亮且永不消失**（`EventSource` 实例非 plain object，Vue 不 proxy，computed 停在首帧），改三态 `stream: idle/open/down` 后真断线 2.5s 现身、重启后 26.3s 自清（fixed/4px 12px/12·18/red-600 全对上，截图 `b1_banner.png`）。**error 红点行**：走块管线（`type='Error'`+`closed=true`，另开数组=第二个游标），历史回放与活流两条路各取一次（`wrap>prose>prose>errRow`、`tail=1` 证明 closed 必要），样式与文案照 `.turnErrorRow`/`message.turnError`。新增 `s8` **t12**（'Error' 不是 BlockType，t1 有盲区）+ 反向验证五例全 CAUGHT。**max-tokens 提示归错档**：源值出处找到（`turn-max-tokens` 由后端 `turn/end reason.kind` 驱动），本机桩服务实测 `finish_reason='length'` 在网关**两条路都进了 `response_metadata` 而全仓零消费者** → 截断今天静默，属档二，登记 PLAN **B8**；顺带推翻 `.env:13` 那句「会被截断（LengthFinishReasonError）」——content 非空时不抛。环境四条：面板不可见=0 宽布局（几何判据必须走 `shot.mjs`）、跨时间读数用「后台杀进程 + 前台长 eval」、反向验证锚点必须唯一（`b.closed = true` 有两处，撞出过假空转） |
| `2026-09-21-A2markdown层级实测与代码块补样式.md` | A2 六项判据全部取到计算值，并**推翻「CSS 已落位」这半句**：正文五项（h1 24/34、h2 22/32、h3 20/30、p `16px 0`、inline code 14px=0.875×16 + r6 + `0 5px`）确实在位；**code block banner `9px 14px` 全仓无落点**（`md-code-*` 四个类只有 `render.ts:35` 的 markup、零 CSS，实测 `pre padding=0`、字号继承 16px）→ 按参照系 `CodeBlock.module.css` 逐条补进 `MarkdownText.vue`，只用已有 token（不为 banner 造 `--dsw-font-xs-13`），复制按钮照抄 `font:inherit` 实测比标签高一号故改同档。样本自己造（判据点名的会话事件流在 db=0、隔离实例读 db15），踩空一次：`idea` 带中文 → 服务端没回 id（curl 载荷一律 ASCII 的老坑）。留一条设计问题：浅色档两个 alias 同为 `neutral-bluish-50` → banner 与代码无层次 |
| `2026-09-21-A3复制回执可信点击.md` | A3 从「只差留证」到 ✅（零代码改动，全部工作量在取证路径）。**核心事实：`doCopy` 只在 `clipboard.writeText` resolve 后才置 `copied`** → 没回执=写没成，不是漏渲染。两条浏览器路径实测都拿不到：内置面板 `clipboard-write=denied`；无头 Chrome `perm:"granted"` 但 `writeText` 仍 `NotAllowedError`（headless 无剪贴板后端）。解法：真窗口 Chrome + CDP `Browser.grantPermissions` + `Input.dispatchMouseEvent` 派发浏览器级可信输入（工装 `log/b14_a3_chrome.mjs`）。读数：`WRITE_OK` → 悬停 `[role=treeitem]` 出卡 → 点击 → `copied="复制成功"`、`cardClass="card copyable feedback"`，截图里卡**没被抽瘪**（`minHeight=copyHeight` 成立）。两次踩空：`.root` 是多组件共用的 scoped 类名（7 个候选全不是锚点）、第一版 `querySelector('.card .copied')` 打到了落地页 `.card.hero` |
| `2026-09-21-B2反向分页与首屏窗口.md` | B2 界面半边收口（用户 13:40 批「自起隔离实例」后）。**四条改判/新洞**：① `limit` 语义从「无 `before` 取头部」改判成**始终取窗口尾部**——首屏要的是「最新一屏」，取头部则胶囊永远没有下一页（`t11` 两头同判）；② 响应补 `has_more`（路由多取一条判显隐，bus 返回契约不动）；③ 反向翻页的整页游标全比 `lastCursor` 小 → 直接喂 `applyEvent` **被整页丢掉**，而被页界切开的块若 `tokens.push` 会把早半截排到晚半截后面 → `mergeEarlierPage` 在临时状态里合成再拼回正片；④ **`captureAnchor` 的「左边缘 +8px」打点永远落在正文列 32px 左内边距里**（四次探测全空 → 恒回 null → `restoreAnchor` 空转），这条零调用者的接缝一接上就红；改水平中心后锚点视口 y `-1242→-1242`。端到端读数：8721 + `REDIS__DB=15` 隔离实例、合成 60 块/895 条（**块长故意不整除 400**，定长 16 会让页界全落在块边界上、一次也走不到切块路径），首屏 27 行→两次点击 60 行、序号 `0..59` 升序无重复、到底 `has_more=false` 胶囊自隐。门禁：s8 11/11×3、s7 13/13 双配置、build 零 error×3、`tsc` 只剩一条与本次无关的旧错。一条环境噪声（Langfuse 未起的 export 报错）；**另有一条我自己取错的「发现」已当场撤销**：写过的「`think-14` 全仓无定义 → Think 行是豆腐块」不成立，别名在 `DsIcon.vue:45`、键在 `glyphs.ts`，探针实测是真 `svg+path`——失误在只 grep 了两张错表 + 把 16px 小图标看成 tofu（段 6 全文）。自纠四条见段 7（含「`python -B env …` 把 env 当脚本名 → exit 2 零 ✅，退出码非零 ≠ 测试红」） |
| `2026-09-21-C12取证过期引文与混币种根因.md` | C12 未起（剩 45→25 分钟要吃 s1..s6+s7），先做第 0.4 步锚点复核：**四条断言三条成立、两处更正**——① `tests/s7_platform.py:237` 措辞「原料就位」**不是臆造而是过期引文**（`git log -S` 定出生死：`187b3fc` 引入、`4849e32` B11 03:31 改写，旧文案仍印在 9 份 `log/*.out` 里），真锚点换成 `:241`/`:262`；**我自己第一版把它判成「编造的引文」并写进了 PLAN，全仓检索后当场回改**；② P95 半账面写大了（`GET /api/sessions/{sid}/trace` @ `sessions.py:288-294` 与前端 trace 展示都在位、span 带 `t0`/`ft`/`ts`）→ **只缺聚合口径**，且 `t0`/`ft` 可为 null 要先剔样本。**跨币种根因升级**：不是「测试缺覆盖」，是 `TOKEN_COSTS` 同表混两种币种（`:14` 人民币/千 vs 其余美元/千）相加进无单位 float `cost.py:26`+`:40`，符号还两处不一致（日志 `$`、界面 `¥`）→ 两条腿（A 分桶 / B 定汇率）登记 PLAN §9 待用户拍，不代拍、不编汇率。段 7 另记 D 档余账第一批：三条点名前提（`对照4:94` 装饰器零引用、`对照2:97` UploadKB 未通电、N6 两处）复核**全部仍成立、零文档改动**，并撤销一条我对 docs 的错判（docs 对、PLAN 错）。免门禁档，工作树两次都 34 件；三条检索坑见段 6 |
| `2026-09-21-C11时间旅行账面已过期.md` | C11 取证：PLAN 那格的证据**已过期**——「等 S7 快照采集」那两处 D1 在 06:37 就改准了（`施工4:110`/`README:222`），改准的新文字里 `make_checkpointer:80` 又是漂锚点（真值 `:82`，本轮两处改回）；回放面实测仍零（`get_state_history` 全仓零命中、无端点、前端只有事件 Timeline），采集侧有底（`s3b` t7/t8 真落断点）。**判据两支都不是自动化会话该拍的板** → 记 🟡 并把「做 vs 砍」连同成本写进 PLAN §9（原内容是「（空）」），顺带把 C1 与两条授权凑成一张待答清单 |
| `2026-09-21-C10鉴权不跨worker写进契约.md` | C10 按判据走第二支：「登录态不跨 worker」写进 `docs/前端/接口契约.md` §5.1（四条**进程内状态**表：token 表 / 登录失败计数 5×N / `users.json` 只有线程锁故跨进程丢账号 / 重启失效），并写明今天为什么打不到（compose 钉 `--workers 1` + auth 默认关）与提 workers 前的必做升级；**运行期行为一字未改，不是「修好了」**。**顺带实测查出一条反安全语义的错文档**：`auth.py:3` 与契约都写 `PLATFORM__AUTH`，但 settings 用 `env_nested_delimiter="__"` 且字段名是 `auth_enabled` → 两进程对照读数 `PLATFORM__AUTH=1` 打不开、`PLATFORM__AUTH_ENABLED=1` 才生效；照旧文档写会以为开了鉴权、实际全站不校验。读数：s8 11/11、s7 13/13 双配置×2（三份日志 ⚠=0 即 redis 分支未跳过）。同族错名已全仓反查并同批扫完：frontend 三处注释改真名（s8 11/11 + `npm run build` 零 error），只剩**别人在途的** `tests/s10_auth.py:3` 按铁律未动 |
| `2026-09-21-C9-prompt包名取证改判.md` | C9 由「只处理 3 处真隐患 prompt 文本 + 加法拼接校准常量」**改判 ⛔**，两条实测：① s6 t1 除逐字相等外还断言顶层常量**键集相等**，往受保护模块加一个新常量当场红（反向探针 exit 1，基线 23 组/t1 比中 54 项未跳过）；② 那三件资产 `codeharness/`+`server/` **零引用**、无反射旁路，真跑的 DI 线用自写 prompt → 隐患文本今天**没有触发路径**，写校准常量=新增死代码；重开条件与四处账面回写见文。另记一条：**上一棒 handoff 的时刻没按 `date` 取（超前真钟 15–25 分钟），导致「只剩 45 分钟」的窗口预算是错的** |
| `2026-09-21-C8精排默认值改未配置.md` | `RerankerConfig.base_url` 缺省从 `localhost:9998/v1` 改成空串（`.env`/compose 都没这服务，判据只挡「URL 空」→ 默认状态=每次走失败路径 + 一条 warning）；`s5 t32` 两层判据（配置类**默认值** + 哨兵客户端证明走跳过分支），**两层都反向验证会红**，层② 顺带实证哨兵异常被 `except Exception` 吞掉=「先抛再吞」的直接证据。**顺手发现**：`s5 t31` 的「A 写 B 查不到」是**空转断言**（替身 `search` 无条件返回 `[]`，连 A 自己都查不到）→ C4 不能拿它当已收口；另记一条自纠：C2 漏改 `docs/README.md` #4，**改门禁语义后要按新语义关键词全仓反查旧说法** |
| `2026-09-21-C2删除TeamState假通道.md` | C2 走「删干净」：`TeamState.docs` 字段 + 三处 init + `tests/` 7 处构造删掉，msgpack 白名单 `Document`/`Documents` **同批摘**（依据是实验：摘后整套 s3b 含真落断点 → **0 条 unregistered 告警**）；新增 `s3b t15` 两层正向守卫并反向验证会红；**关键实测：LangGraph 对未知状态键静默丢弃**（删字段后 `s16` 仍 5/5 exit 0）→ 残留写者炸不出来只能正向钉；**翻案**：`s3b main()` 中途抛错时 `close_all()` 不跑 → aiosqlite 线程吊住解释器 6.5 分钟不退出，上一轮那条「管道跑 26 分钟不返回」的悬案机制是它而非 grep 缓冲 |
| `2026-09-21-B2收口与SSE流尾游标竞态.md` | 接管僵尸锁（上一棒卡在等用户答复）后重新取证 B2：s8 **11/11** + s7 **13/13 双配置连跑 3 次**；**修掉一条生产真洞**——`RedisEventBus.subscribe()` 的流尾游标等后台 reader 首次调度才取，`/events` 走法 subscribe→history→续推，落在两者之间 XADD 的事件**永久丢失**；`log/s7_rep1.out`（03:21）证明该红早于本改动，隔离跑 t12 三遍全过；**「60s watchdog 卡死」其实是 t12 内层 `q.get()` 的 3s 超时冒到外层** → 看门禁红先读 traceback 里内层那一句 |
| `2026-09-21-阶段D文档对账.md` | PLAN §1 基线回写（HEAD 漂 24 笔、s7 13/13 + s8 10/10 真读数）；阶段 D 四项 D3/D4/D2/D1 收口全过程；新环境事实：**本机 ollama 推理已可用**、8718 未运行、后端没起时改用**只读枚举 `ch:index` status** 判「无人在跑」；长跑门禁别管道 grep（缓冲看不见进度） |
| `2026-09-20-计划审查与自动化建档.md` | `plan/PLAN.md` 逐条对账的取证结果（6 条事实断言已失效或口径反了）；两条心跳驱动器的互斥设计与 30 分钟错开；`log/` 目录的建立 |
