# 2026-09-21 · 阶段 D 文档对账（D3 / D4 / D2 / D1）+ §1 基线回写

会话标记 `stage-dev-D-20260921-0612`（阶段开发驱动器 06:11 槽位，本轮起就是该窗口第 1 轮）。
真实时间：06:11:14 开工（`date -Iseconds`）→ 06:39 写完本段（时刻一律 `date` 现取）。

---

## 段 0 · 开工前置：HEAD 漂移 24 笔，先回写 PLAN §1 再动 D3

**现象**：队列 `baseline_head=5579e99`，而 `git rev-parse --short HEAD` = **`abcd75f`**，
`git rev-list --count 5579e99..HEAD` = **24**。并存的治理驱动器（`plan/governance-queue.md`）
状态头已是 `mode: complete` / `cursor: done` / `lock: none`，四阶段 22 项全部收口（含 C1 收官，
其作业 `93e80729` 自记 `enabled=false`）。

**排查过程**：
1. 锁判定：`lock` 字段（两份队列都读）→ 我自己这条 `none`（链条断），治理那条 `none` 且 `mode: complete`
   → **本轮不存在「被治理锁降级」**，与上一份 handoff 的预判（"每轮大概率降级"）相反，因为它不知道治理会收官。
2. 门禁能否跑：`curl -s http://127.0.0.1:8718/api/health` → **`health_http=000`（后端没起）**，
   所以 §0.4 那条「跑门禁前 curl /api/sessions 确认无 running」这条判据**拿不到读数**。
   改用只读探测共享 Redis（`heal_running()` 真正会动的地方）：
   ```
   PYTHONPATH=/e/Codeharness:/e/Codeharness/logs ... python -B -c "from platforms.session_store import RedisSessionStore, INDEX; ..."
   → ping= True
   → sessions_in_index= 567
   → status_counts= {'stopped': 71, 'created': 465, 'failed': 14, 'finished': 17}   # 零 running / 零 awaiting_human
   ```
   `server/app.py:41-49` 只在 `use_redis` 且 `ping()` 通时才 `heal_running()`；
   `platforms/session_store.py:124-130` 只改 running/awaiting_human → **既然两条都没有，跑门禁是 no-op**，安全。
3. 跑门禁（姿势三件套齐）：
   - `tests/s7_platform.py` → `s7_platform: 13/13 全绿（双配置）`，退出码 0
   - `tests/s8_frontend_contract.py` → `s8_frontend_contract: 10/10 全绿`（t9/t10 = 治理 F2/F3 新增，
     故 §1 旧记的 `8/8` 过期）
4. `ls tests/s*.py | wc -l` = **26**（旧基线 19）→ 连带把 **D2 的目标数从 19 改成 26**。
5. 顺手取环境读数：`curl 11434/api/tags` 可达；`/api/generate`（`qcwind/qwen2.5-7B-instruct-Q4_K_M`，
   `num_predict=8`）→ `"response":"OK"`、`done_reason=stop`、`total_duration=4.25s`、`load_duration=4.13s`、
   `eval_duration=0.029s/2 token`。

**根因**：不是代码 bug，是**并发驱动器的正常推进**让计划文档过期（§1 的 HEAD/门禁读数/脚本数三处全过期）。
另外两条环境层面的事实：① 共享后端没起 → 依赖 8718 的判据本轮一律拿不到读数；
② **上一份 handoff 的「本机 ollama CUDA 坏、真模型一律不可验」是过期结论**（`response:"OK"` 直接证否）。

**解决方案**：按 §0.6 重写 PLAN §1（HEAD、24 笔来源、s7/s8 真读数、Redis 只读探测姿势、26 件脚本、
ollama 已可用与它的边界、8718 未运行 → A2/A3/B 探针项标越界），并把 §0.5 那句「CUDA 当下是坏的」改成
「已可用，但 7B 能否产出合法 tool call 未验」。

**解决后效果**：**已修并实测**（读数见上）。§1 与磁盘一致；下一轮不必再花一轮重新取证。
新增一条环境铁律：**跑门禁前的"无人在跑"探测，后端没起时不能靠 `/api/sessions`，要直接只读枚举
`ch:index` 的 status**——本轮就是这么过的。

---

## 段 1 · D3：撤掉「开工先删 .gitignore 的 docs/ 行」这条会诱导重复劳动的指令

**现象**：`docs/README.md` 有两处仍写 docs/ 未入库：`:81` 的 `# 0) 先修 .gitignore：删掉 docs/ 那一行`、
`:288` 陷阱 #1 整段「仍未删…开工第一件事仍是删这行并提交 docs/」。**自动化会话照它做会去删一个不存在的配置，
并把 14 份文档重复提交一次。**

**排查过程**：
```
git ls-files docs | wc -l        → 14
git check-ignore -v docs/README.md → 无输出，退出码 1（没被忽略）
grep -n docs .gitignore          → 零命中（末行其实是 log问题/）
git log --oneline -1 33244d8     → 33244d8 docs: 全套施工账本纳入版本控制
```
同文件 `:109` 早就写对了（「`docs/` 从 `.gitignore` 移除并入库（`33244d8`）」）——**文内自相矛盾**。
再按「修根因」全仓 grep 同族前提（`先修 .gitignore|删这行|docs/ 那一行|零版本控制`）：
命中只有这两处（改后只剩新写的正确段），**没有第三处需要一起改**。

**根因**：`33244d8`（2026-09-15）修掉了配置，但没回写写下这条指令的两处文字；台账型文档里
「当日警示」和「当前状态」长期混写，就会留下这种会驱动错误动作的条目。

**解决方案**（commit `052ac88`）：`:81` 那行注释直接删（环境准备里不需要它）；陷阱 #1 整段改判为
「已闭环 + 真实残留风险」，并把风险落点换成同族且仍然成立的那条：**未跟踪的 `plan/`
（`.git/info/exclude:7`）与 `log/` 一次 `git clean -fdx` 就连锅端**。

**解决后效果**：**已修并实测**（判据三条全是本轮命令输出）。后续会话读到 README 不会再重复提交 docs/。

---

## 段 2 · D4：三份旧计划标 superseded（**无可提交文件，留痕方式要说清**）

**现象**：PLAN D4 说三份旧计划顶部都没标取代关系。`ls` 实测三份都在且只有三份：
`.zcode/plans/plan-sess_abb6c635-….md`、`.trae/documents/N9数据体检修复计划.md`、
`.trae/documents/引入Langfuse全链路可观测.md`，头部 4 行内都没有 superseded 行。

**排查过程 + 解决**：各在 H1 下方插一行 `> **本计划已由 plan/PLAN.md 取代（2026-09-21，阶段 D / D4）**…`，
**一字不删正文**。改完逐项复测：
```
wc -c 改前 → 7955 / 7422 / 7460（合计 22837）
wc -c 改后 → 8166 / 7559 / 7597（合计 23322，净增 485 字节 = 三行标记本身）
grep -lc "本计划已由 `plan/PLAN.md` 取代" → 三份都命中
file → 三份都仍是 UTF-8 text（没有本机 GBK 写回的坑）
```
**根因/口径**：这三份都在**未跟踪目录** `.zcode/`、`.trae/` 里，而队列铁律 3（用户 2026-09-20 定）
明写这类目录不进任何提交名单 → **D4 没有主仓 commit**，留痕只有上面的实测读数 + 本段日志 + plan 小仓提交。
这一条要写给下一轮看，别以为 D4 漏提交了。

**解决后效果**：**已修并实测**（尺寸/命中/编码三项对得上，正文零损失）。

---

## 段 3 · D2：门禁脚本清单对账——「16 个」现在是 26 件，编号还撞了车

**现象**：README 的 dated 段一路记 5→8→9→11→12→13→14→15→16 件「不花钱脚本全 exit 0」，
最后一个数是 16（`:276`/`:282`）；磁盘实测 `ls tests/s*.py | wc -l` = **26**。
读者（尤其自动化会话）会拿 16 当当前基线。

**排查过程**：
1. `grep -n "个不花钱|脚本全|全 exit 0|门禁基线" docs/README.md` → 命中 15 处（含 dated 段）。
2. `ls tests/` 逐件点名，发现**编号已不唯一**：`s12_rebuild_views`+`s12_workspace_guard`、
   `s13_compress`+`s13_redis_qdrant_auth`、`s20_auth_race_ratelimit`+`s20_p0_complete`
   ——「第 N 件脚本」这种数法从此不成立。
3. 判「探活式」不能靠猜：`grep -ln "未起|不在本机|无 Redis|无 Qdrant|未装|拿不到" tests/s*.py` 先出 11 件候选，
   再逐件读上下文剔掉**只是被测语义**的（`s4_tools` 的「禁行命令替换跳过」、`s2_gateway`/`s8_runner_meter`
   的「拿不到 usage 必须留 warning」是断言文字不是跳过）→ 终判 **8 件**：
   `s5_memory_rag`(t3/t5/t9/t12)、`s6_sop`(t1/t4 需供体 `E:/MetaGPT`)、`s7_platform`(redis 段整段跳)、
   `s9_benchmark`(t1 需数据集)、`s9_langfuse`(t2/t3)、`s10_auth`(t5)、`s13_redis_qdrant_auth`(t2/t5/t6)、
   `s18_session_store_fields`(**缺服务时整件全跳**，它自己打印「退出码 0 不代表验过」)。
4. 本轮自跑读数：`s7_platform` 13/13、`s8_frontend_contract` 10/10、`s14_tot` 6/6。
   `s3b_runtime` 06:13 起跑、06:39 仍未返回（约 26 分钟，它的长腿真起沙箱跑 pytest）→
   治理阶段门记的既有红 `s3b_runtime::t14` **本轮没有自测读数**，文档里明写「不要当成已复核」。
   **我自己的操作失误让它更没用**：命令写成 `... | grep -E "✅|❌|t14"`，管道缓冲 → 中途**看不到任何进度**，
   既不能判断它是慢还是卡死，也拿不到半截证据。正确姿势是 `> log/s3b.out 2>&1` 落盘后再看（已写进 A5 判据）。

**根因**：README 是**流水账**，每段只记当天的数；缺一条「以下是当日快照」的声明与一处当前数，
于是每一轮都要重新数一遍（今晚建档那轮就重数过一次）。

**解决方案**（commit `b0645c5`，纯新增 24 行）：①「## 现状基线」小节头加一行醒目声明
（以下 N 个全绿都是当日快照，当前数看文末段）；② 文末新增
`### 2026-09-21 · 门禁脚本清单对账（D2）`：磁盘读数、编号撞车、本轮实测三件 + 未跑清单、
8 件探活式逐件点名。

**解决后效果**：**已修并实测**（`git diff -U0` 只有两个新增 hunk，未动任何历史段）。
**未做**：没跑全 26 件——治理驱动器 05:14 的全量读数（29 条 28 绿 + 1 既有红）在文档里按**指针**引用，
没冒充本轮实测。

---

## 段 4 · D1：stale 账目逐条回写，实测下来比 PLAN 记的 5 处多

**现象**：PLAN §5 D1 列了 5 处「文档说没接线、代码早就有」。逐条对代码复核后确认成立，
但**同一失效前提在每个文件里还被复制了额外几处**（PLAN 没列），按「修根因不修症状」一并处理。

**排查过程与逐条证据**（每条都是本轮命令输出，不是记忆）：

| # | 文档处 | 旧断言 | 实测反驳 |
|---|---|---|---|
| ① | `对照3-工具调用.md` 二/三节表格两行 + §四-A + §五三处 + §六第 1 条 + 复核命令 | `ImportRepo` 零引用未接线；`SearchEnhancedQA` 仍是现役文件 | `grep -rn "ImportRepo\|import_repo" codeharness server` → 端点 `server/api/workspace.py:61,62` + 白名单 `tools/_approval.py:40`；前端 `DetailsPanel.vue:28,184`（`2791bc7`）；`ls codeharness/actions/search_enhanced_qa.py` → **No such file**（删除于 `70f0f8e`）；`tests/s11_search_import.py` 现 **5 组全打端点**（`grep -c "^def t"`=5） |
| ② | `施工2-能力层-S4-S5.md` 复制表 editor/git 两行 + 「仍不做的」run_context + 「仍未被断言覆盖」+ 2026-09-15 零读者条 | 生产路径零 import 者 / 待包成 @tool / 无 S8 级门禁 / `tests/` 无一件起 TestClient | `codeharness/tools/__init__.py:65,66` 副作用登记（Editor 11 命令 + git 两件），`libs/git.py:26,39` 各有 `@register_tool(tags=["git"])`，`roles/registry.py:98` SweAgent 真 `select(...,"edit","git")`；`sandbox.py:95` = `safe_session_path(...) or session_root()`（S3 `0e0a53a`）+ 门禁 `s4_tools.py:221 t13b`；`grep -ln TestClient tests/*.py` → **8 件 s\***；`s12_workspace_guard.py:59,78,95,104` 四组守 `/workspace` |
| ③ | `对照4-AgentLoop运行时.md` 三节 ToT 行 + §六「零断言」+ s14 那条 + §七第 3 条 | 「部分实现（空壳）」「判定表仍未补」「3 组浅断言」 | `git log -- codeharness/strategy/tot.py` → `3f975f7` 批次1 移植 BFS/DFS；`strategy/base.py:26 update_value`；`tests/s14_tot.py` 6 个用例名含 `t2_evaluate_writes_value`/`t3_select_keeps_highest`/`t4_bfs_solve_picks_high_path`，**本轮跑 6/6 全绿**；判定表 `:120` 已补 |
| ④⑤ | `施工5-前端重构-F1-F7.md` 批次22「刻意不画的三样」+ 档一清单 | 模型目录没接、`llm_override` 只读展示 | `server/api/models.py:12,18` 存在；`codeharness/team.py:84-90 _make_llm(..., override)` 读 `override["model"]`；`frontend/src/api/client.ts:71` + `components/composer/ComposerCard.vue:131 modelPick`（`store.modelsOk && models.length` 才画箭头）；ContextMeter 仍零命中 → **三样改成两样**，`.add`/`ContextMeter` 的理由原样保留 |
| ⑥ | `docs/README.md` S8 未闭合项 + `施工4:109` | N6 时间旅行「等 S7 快照采集」 | `施工4:66` 切默认当日完成、`environment/checkpoint.py:80 make_checkpointer` 在位 → 缺的是「采快照 + 回放读取口」这件活本身；改准并把做/⛔ 指回 **C11**（不当已做） |

**顺带修准的计数**：`对照3` §五 首行写 `s4_tools.py`（38 组）→ 实测 `grep -c "^def t"` = **40**。

**根因**：三件事叠出来的——① 台账文档按「本轮发现」写，发现被处置后**只有顶部复核段被更新**，
正文表格与待办清单没人回头改；② 同一个失效前提会在一个文件里出现 3~6 次（表格行 + 待办条 + 复现命令注释），
只改 PLAN 点名的那一处等于留三处假账；③ 治理驱动器 24 笔提交把「等 S6/S7/S8 接线」这类等待条件批量作废了，
但它的账记在自己的 `plan/governance-*.md` 里，不回写这些老 ledger。

**解决方案**：五个文件逐处改成「本轮复核状态 + 依据（commit 号或门禁读数）」；**当轮历史记录不删**，
改成 `~~原文~~ → 2026-09-21 复核：…` 的写法，保留出处可追溯。

**解决后效果**：**已修并实测**——本段每条判据都带本轮命令输出；三件门禁（s7 13/13、s8 10/10、s14 6/6）
为这些改判提供正向读数。**未做**：`docs/` 里其余 ledger 段的同类 stale 没有全仓扫完（本轮只覆盖
PLAN 点名的四处文件 + 同文件同前提的重复处），下一轮若要清可 `grep` 「零引用/未接线/等 S\d」起。

---

## 段 5 · 自纠（本轮我自己引入的错）

1. **写错门禁函数名**：给 `施工2` 补 t13b 时把 `t13b_run_context_clamps_outside_working_directory`
   打成 `clamps_outing_`。发现后立即改回（Edit 一次），此处照实记。
2. **凭印象写行号**：同一笔里写 `registry.py:85`，实测是 **`:98`**；`施工4` 的「有意偏离」段写成 `:65`，
   实测 **`:64`**。两处都已改回。**教训**：行号必须 `grep -n` 现取再写进文档。
3. **计数少一级**：跟着旧文档写「s4 从 35 涨到 38 组」，实测 `grep -c "^def t"` = **40**（末尾自报 len(checks)）。
   已在 `施工2` 与 `对照3` 两处改成 40。
4. **一次为了拆 commit 的临时还原**：README 同时有 D2 与 D1⑥ 的改动，我把 D1⑥ 那行**临时改回原文**
   让 D2 单独成 `b0645c5`，随后立刻改回来（否则一笔提交会混两项）。这一步不是修复，是提交卫生，写清楚免得
   下一轮看到 README 历史里同一行「改了两次」以为是抖动。
5. **用占位时刻糊过去了**：PLAN 状态列与日志初稿我写成 `06:2x` / `06:3x` / 「约 25 分钟」，
   违反 `log/README.md` 约定 1（时刻一律 `date` 取真实值，推断的才写「约」）。已逐条改成真值：
   D3 `06:15`、D4 `06:16`（文件 mtime）、D2 `06:34`、D1 `06:37`（`git log --format=%ad`），
   s3b 那段改成「06:13 起跑、06:39 仍未返回」。

## 相关 commit 与门禁读数（本会话累计）

- 主仓：`052ac88` **D3**（06:15）→ `b0645c5` **D2**（06:34）→ `c195a9f` **D1**（06:37）→ 一笔 D2 时刻表述校正；
  D4 无主仓 commit（未跟踪目录，见段 2）。
- `plan/` 小仓：开工认领锁 `b1d6167`，之后每项一次提交。
- 门禁（本轮真跑，HEAD 起于 `abcd75f`）：`s7_platform` **13/13 全绿（双配置）**、
  `s8_frontend_contract` **10/10 全绿**、`s14_tot` **6/6 全绿**；`s3b_runtime` 06:13 起跑、06:39 未返回（未取到读数，
  治理记的既有红 `t14` 因此仍未复核）。阶段 D 是纯文档面，按 §0.3 矩阵**免跑前端 build/探针**。
- 环境：共享后端 8718 **未运行**；本机 ollama **可用**（`response:"OK"`）；共享 Redis 567 会话零 running。
