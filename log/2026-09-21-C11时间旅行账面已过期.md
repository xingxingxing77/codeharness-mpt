# 2026-09-21 · C11：N6 时间旅行回放 —— 账面证据已过期，缺的是一个范围决定

> 冷会话 `stage-dev-cold-20260921-1006` 第 3 项（也是本窗口最后一项）。改动面 `docs/**` + `plan/**` → 免跑门禁。
> **本项没有 ✅**：判据两支（做 / 改判 ⛔）都不是自动化会话该拍的板，状态记 🟡 并把问题落进 PLAN §9。

## 段 1 · 现象（接手时的账面）

PLAN §4 C11 行写：「成立：`docs/施工4-平台服务与度量-S7-S9.md:109` 仍写『等 S7 快照采集』，而切默认在 `:66`；
另一处 `docs/README.md:218` 同样在等」，判据 = 「要么做，要么改判 ⛔ 并回写这两处」。

## 段 2 · 排查过程

1. **先 grep 那两处现文**（不信行号也不信状态列）：
   - `docs/施工4-平台服务与度量-S7-S9.md:110` 已经是 **D1 改准后的文字**：
     「**等待条件已改准（2026-09-21，D1）**：旧文字写『等 S7 快照采集』，而 S7 本文件 `:66` 当天就收口并切了默认…
     **真正缺的是这件活本身**：按 superstep 采快照 + 回放读取口（端点与前端时间轴都没有）。做还是判 ⛔ 由
     `plan/PLAN.md` §4 **C11** 拍板，别再拿『等 S7』当理由。」
   - `docs/README.md:222` 同样已改准（`~~等 S7 checkpointer 快照采集~~ → 2026-09-21 改准（D1）`）。
   → **判据里「并回写这两处」那一半，D1 在 06:37 `c195a9f` 就做完了**；PLAN 的 C11 证据格是 09-20 写的，比 D1 早。
2. **复核 D1 那段新文字里的三个断言**（改准的文字也会带新错）：
   - `make_checkpointer` 位置：D1 两处都写 `environment/checkpoint.py:80`，实测 `grep -n "def make_checkpointer"` →
     **`:82`**（`:80` 是上一个函数 `sqlite_saver` 的 `return`）。已改回。
   - 「端点与前端时间轴都没有」：**成立**——全仓 `grep -rn "aget_state_history\|get_state_history"` **零命中**、
     `server/`+`codeharness/environment/` 无 `replay|time_travel|superstep` 面、前端只有事件 Timeline
     （`frontend/src/stores/sessions.ts`）没有超步轴。
   - 「S7 已收口切默认」：与 §1 基线一致（`use_redis` 默认 True、s7 双配置常驻）。
3. **采集侧是不是纸面**：`tests/s3b_runtime.py` 的 t7/t8 是**真落断点**用例（C2 那轮整套 **16 组 exit 0**，
   且 C2 为了验白名单专门把整套跑过真落断点分支）→ 「checkpointer 在位且真被用」有门禁读数，不是注释。

## 段 3 · 根因

PLAN 的 C11 行是**汇总时（09-20 22:20）从旧 docs 抄来的**，而 D1（09-21 06:37）已经把那两处改准了——
**D1 收了 docs，没收 PLAN 的账**：PLAN §4 那格的「成立」二字因此是过期的。这与 §0.6「现场读数每轮重取」
是同一件事的另一面：**状态列也得跟着别的会话的提交重取**，不能因为是自己写的计划就当它新鲜。

## 段 4 · 解决方案

1. **不代拍范围**：判据 A 支 = 三件新面（按 superstep 采快照 + 回放读取端点 + 前端超步时间轴，且新端点要按 B7
   同批扩 `s8` t2/t3 词汇表，验收还要一次活体浏览器——而那条授权至今没人答）；B 支 = 砍掉一件
   我们自己列在「源没有的能力」里的平台能力（`施工4:158`，源是轮询循环、根本画不出时间旅行）。
   两支都不是自动化会话该定的 → **C11 记 🟡**，把问题与两条腿的成本写进 **PLAN §9 决策记录**（那里是干这个的，
   原内容是「（空）」），并顺手把 **C1** 与 **两条环境授权**一起列进同一节，凑成一张待答清单。
2. **回写账面**：PLAN 的 C11 证据格整格重写（写清「本行原列的证据已过期 + D1 已改准 + 本轮补的三格实测 +
   别再按旧文字去改准它们」），状态格 🟡 待拍板。
3. **改掉 D1 留下的 `:80` 漂锚点**（`docs/README.md`、`docs/施工4` 各一处）——顺手、零风险，且这正是
   「改准的文字也要 grep」的实证。

## 段 5 · 解决后效果（读数）

- **门禁**：本项改动面纯 `docs/**`+`plan/**` → 按 §0.3 **免跑**（不是"跑过了"，是这一档不需要跑）。
- **账面变化**：C11 由「⬜ + 过期证据」变成「🟡 + 三格实测 + §9 待答」；两处 `make_checkpointer:80` → `:82`。
- **没做的事**：没写一行回放代码、没起端点、没动前端；**因此 N6 时间旅行对用户的可见状态仍是「没有」**。
- **cursor → C12**（§7 原序），但 C12 要改 `codeharness/provider/**`+`tests/` 并吃 s1..s6+s7，
  本窗口剩余时间不足以干净收口 → 交给下一轮，不在 12:00 机械停摆前起半截。

## 段 6 · 自己引入的问题 / 提醒

1. **我的结构校验写得太严，把自己的脚本搞红了**：`log/c11_bookkeep.py` 末尾断言「每条 `| C\d+ |` 行去转义后必须 6 个竖线」，
   结果 C6 在 code span 里用了**裸竖线**（`def recommend|select_tools|pick_tools|tool_select`，实测该行 9 个竖线）→ 断言在 PLAN **已写盘之后**才抛，
   退出码 1。回读确认三处改动都真落地（C11 行 7 竖线含 1 个转义 = 5 格正确；`:80` 残留 0；§9 空行已替换）。
   **下次写校验要么先跑一遍存量再定基线，要么只校验自己动过的那几行。**
2. 改表格单元格一律**整行替换**（`startswith("| C11 |")` 定位），不要再跨行匹配 old_string——上一项刚因此吃掉过 C11 行首。
3. 本轮**没有**给 C11 找「看起来像进展」的写法：既没写 ✅ 也没自作主张 ⛔。如果下一轮拿到答复是 B 支，
   按 §9 那一节回写 `施工4:78`、`施工4:158`、`README:222` 三处说法即可。

---

相关 commit：本项主仓一笔 `docs(C11)`（`docs/README.md` + `docs/施工4-平台服务与度量-S7-S9.md` 锚点更正 + 本日志）；
`plan/PLAN.md`（C11 行 + §9 决策记录）与队列、handoff 走 `plan/` 小仓。取证脚本 `log/c11_bookkeep.py` 按惯例不入库。
