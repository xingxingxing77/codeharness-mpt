# 复制成 .env 后填真值。.env 本身不入库（S5：两个密钥都是 compose 解析期的必填项，缺一个就
# 当场报 "need XXX" 而不是悄悄起一个裸服务）。
#
# 只影响 docker-compose 形态。本机 Windows 日常开发直连 F:\Redis 那个无密码服务，
# 这两个变量留空即可（settings 里 password/api_key 默认空 → 连接串与改动前逐字节相同）。

# Redis：写进 `redis-server --requirepass`，同时作为 backend 的 REDIS__PASSWORD。
# 里面是会话态哈希、事件流、审批台账、trace、配额——裸着等于把伪造回执的门开着。
REDIS_PASSWORD=

# Qdrant：写进 qdrant 容器的 QDRANT__SERVICE__API_KEY，同时作为 backend 的 QDRANT__API_KEY。
# 里面是长期记忆向量与 payload（含 user_id/session_id）。
QDRANT__API_KEY=
