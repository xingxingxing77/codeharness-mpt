services:
  qdrant:
    image: qdrant/qdrant
    volumes: ["qdrant_data:/qdrant/storage"]
    ports: ["6333:6333"]
  backend:
    build: .                     # 构建上下文 = 仓库根（codeharness 包 + server 同仓）
    env_file: .env
    environment: [QDRANT__URL=http://qdrant:6333]
    depends_on: [qdrant]
    command: uvicorn server.app:app --host 0.0.0.0 --port 8000 --workers 1
    volumes: ["workspace:/app/workspace"]
  frontend:
    build: ./frontend            # node build → nginx 托管静态 + 反代 /api
    ports: ["80:80"]
    depends_on: [backend]
volumes: { qdrant_data: {}, workspace: {} }
